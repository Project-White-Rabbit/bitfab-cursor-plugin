{
  "name": "bitfab-cursor-plugin",
  "version": "0.9.24",
  "description": "Automated regression testing and improvements for your prompts, context, and tool calls.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "tsc": "tsc --noEmit",
    "knip": "knip",
    "madge": "madge --ts-config ./tsconfig.json --extensions ts --circular ./src",
    "validate": "pnpm lint && pnpm tsc && pnpm knip && pnpm madge && node ../scripts/check-bitfab-plugin-command-surface.mjs"
  },
  "keywords": [
    "cursor",
    "bitfab",
    "llm",
    "evaluation",
    "tracing",
    "mcp"
  ],
  "author": "Bitfab",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "zod": "^4.4.3",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "semver": "^7.8.5"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
