import type { PlatformConfig } from "../platform.js";
export type ReplayStatusResult = {
    status: "ok";
    testRunId: string;
    testRunStatus: string | null;
    traceIds: Record<string, string>;
    traceCount: number;
    mappedCount: number;
} | {
    status: "error";
    message: string;
};
export declare function fetchReplayStatus(opts: {
    serviceUrl: string;
    apiKey: string;
    testRunId: string;
}): Promise<ReplayStatusResult>;
export declare function runReadReplayStatus(platform: PlatformConfig): Promise<void>;
