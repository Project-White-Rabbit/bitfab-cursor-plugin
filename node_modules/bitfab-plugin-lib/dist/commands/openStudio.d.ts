import type { PlatformConfig } from "../platform.js";
export interface OpenStudioOptions {
    serviceUrl: string;
    apiKey: string;
    sessionId: string;
    initialPath?: string;
    clientHeaders?: Record<string, string>;
    reconnect?: boolean;
}
export interface StudioSession {
    sessionId: string;
    abort: () => void;
}
export declare function openStudio({ serviceUrl, apiKey, sessionId, initialPath, clientHeaders, reconnect, }: OpenStudioOptions): Promise<StudioSession>;
export interface OpenStudioForLoginOptions {
    serviceUrl: string;
    sessionId: string;
    platform: PlatformConfig;
    pluginVersion: string;
    autoClose?: boolean;
}
export interface LoginResult {
    token: string;
    sessionId: string;
}
export declare function openStudioForLogin({ serviceUrl, sessionId, platform, pluginVersion, autoClose, }: OpenStudioForLoginOptions): Promise<LoginResult>;
export declare function runOpenStudio(platform: PlatformConfig): Promise<void>;
