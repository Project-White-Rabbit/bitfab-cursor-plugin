export interface OpenStudioOptions {
    serviceUrl: string;
    apiKey: string;
    sessionId: string;
}
export interface StudioSession {
    sessionId: string;
    abort: () => void;
}
export declare function openStudio({ serviceUrl, apiKey, sessionId, }: OpenStudioOptions): StudioSession;
