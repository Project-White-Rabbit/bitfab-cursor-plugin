import type { PlatformConfig } from "../platform.js";
export interface OpenStudioOptions {
    serviceUrl: string;
    apiKey: string;
    sessionId: string;
    initialPath?: string;
}
export interface StudioSession {
    sessionId: string;
    abort: () => void;
}
export declare function openStudio({ serviceUrl, apiKey, sessionId, initialPath, }: OpenStudioOptions): Promise<StudioSession>;
export declare function runOpenStudio(platform: PlatformConfig): Promise<void>;
