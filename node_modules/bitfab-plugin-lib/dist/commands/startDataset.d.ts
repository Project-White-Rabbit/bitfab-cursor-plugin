import type { PlatformConfig } from "../platform.js";
/**
 * Open the dataset review / labeling page in the browser and block until the
 * user clicks "Confirm dataset". Races the loopback HTTP callback and a
 * server-polled ticket so it works reliably on SSH, Docker, WSL, cloud IDEs,
 * and any other environment where the browser can't reach the CLI's loopback.
 *
 * Shares `runBrowserHandoff` with `runLogin` — same channel abstraction,
 * different ticket kind + completion routes.
 */
export declare function runStartDataset(platform: PlatformConfig, pluginVersion: string): Promise<void>;
