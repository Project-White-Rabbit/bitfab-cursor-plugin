import type { AgentEvent } from "../agentEvent.js";
import type { PlatformConfig } from "../platform.js";
export { StudioNavigationError } from "../errors.js";
export declare function handleStudioError(err: unknown): never;
export interface OpenStudioToResult {
    sessionId: string;
    serviceUrl: string;
    apiKey: string;
    opened: boolean;
    persistent: boolean;
    /** The URL a fresh window was opened to, present only when `opened` is true
     *  and a new window was spawned (not on reuse). Callers surface it so the
     *  user has a durable link to the first Studio page for this run. */
    url?: string;
    abort: () => void;
    done: Promise<void>;
}
export type StudioEventCallback = (event: AgentEvent) => void;
export declare function formatStudioUrlMessage(url: string): string;
/** Sink for the Studio lifecycle events openStudioTo emits itself. */
export type StudioLifecycleEmit = (event: Record<string, unknown>) => void;
export declare function openStudioTo(path: string, opts?: {
    clientHeaders?: Record<string, string>;
    forceLogin?: boolean;
    freshWindowPath?: string;
    onEvent?: StudioEventCallback;
    emit?: StudioLifecycleEmit;
    monitor?: "start" | "wait";
    tail?: boolean;
}): Promise<OpenStudioToResult>;
export declare function runOpenStudioTo(platform: PlatformConfig): Promise<void>;
