import type { PlatformConfig } from "../platform.js";
/**
 * Run each update command, returning the ones that failed. Every command is
 * attempted even after a failure: there is one update command per install
 * scope, so stopping at the first would leave the remaining scopes on the old
 * build.
 */
export declare function runUpdateCommands(commands: readonly string[], run: (command: string) => void): string[];
export type UpdateMode = "all" | "plugin" | "sdk";
export declare function parseUpdateMode(raw: string | undefined): UpdateMode;
export declare function runUpdate(currentVersion: string, platform: PlatformConfig, mode?: UpdateMode): Promise<void>;
