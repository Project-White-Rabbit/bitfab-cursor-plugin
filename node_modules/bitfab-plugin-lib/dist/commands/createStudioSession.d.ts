export interface CreateStudioSessionOptions {
    serviceUrl: string;
    apiKey: string;
    sessionId: string;
    initialPath?: string;
    clientHeaders?: Record<string, string>;
}
export interface StudioSession {
    sessionId: string;
    serviceUrl: string;
    /** Whether the browser process was actually spawned. False only when nothing
     *  launched (rate-limited / spawn failed), so the caller can report an honest
     *  open-failed instead of claiming the window opened. */
    launched: boolean;
    /** Present when `launched` is false: why nothing launched. */
    launchReason?: string;
}
/**
 * Internal: creates a new Studio session, opens the browser, and writes
 * the active-session file. Does NOT start event polling. Called only by
 * openStudioTo. Do not call directly.
 */
export declare function createStudioSession({ serviceUrl, apiKey, sessionId, initialPath, clientHeaders, }: CreateStudioSessionOptions): Promise<StudioSession>;
