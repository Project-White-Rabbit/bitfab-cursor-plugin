import type { PlatformConfig } from "../platform.js";
export declare function parseTestRunIds(args: readonly string[]): string[];
/** Options shared by every experiments-path builder. */
export interface ExperimentsPathOptions {
    /** Turn on the token-cost lens (`?tokens=1`) on the experiments page. */
    showTokens?: boolean;
    /**
     * Which token total the lens shows. Defaults to `all` (input + output, cache
     * reads included); `uncached` counts `(input - cached) + output`. Always
     * emitted as `&tokenType=` when the lens is on. Only meaningful with
     * `showTokens`.
     */
    tokenType?: "all" | "uncached";
}
export declare function buildExperimentsPath(testRunIds: readonly string[], opts?: ExperimentsPathOptions): string;
export declare function buildExperimentsPathForGroup(experimentGroupId: string, opts?: ExperimentsPathOptions): string;
export declare function buildExperimentsPathForDataset(datasetId: string, opts?: ExperimentsPathOptions): string;
export declare function runOpenExperiments(_platform: PlatformConfig): Promise<void>;
