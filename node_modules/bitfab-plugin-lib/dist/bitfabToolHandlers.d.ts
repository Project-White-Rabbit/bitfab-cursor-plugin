import { type ToolContract } from "bitfab-lib/mcp-contract";
import type { BitfabConfig } from "./config.js";
import { McpProxy } from "./mcpProxy.js";
import type { PlatformConfig } from "./platform.js";
export type ToolCallArgs = Record<string, unknown>;
export type ToolCallResult = Awaited<ReturnType<McpProxy["toolCall"]>>;
export interface BitfabToolHandler {
    contract: ToolContract;
    handle: (args: ToolCallArgs) => Promise<ToolCallResult>;
}
export interface BitfabToolHandlers {
    proxy: McpProxy;
    handlers: BitfabToolHandler[];
}
export declare function createBitfabToolHandlers(platform: PlatformConfig, getConfig: () => BitfabConfig, pluginVersion: string): BitfabToolHandlers;
