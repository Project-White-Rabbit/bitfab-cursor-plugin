import net from "node:net";
export declare function reapClosedSessions(now?: number): void;
export declare function startServer(socketPath?: string, pidFilePath?: string, metaPath?: string, worktreeRoot?: string): net.Server;
