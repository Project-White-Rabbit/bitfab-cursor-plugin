/**
 * Short label for the host coding agent, embedded in template-preview
 * `editStarted` events so the browser can say who is editing. Override with
 * `BITFAB_AGENT_LABEL`.
 */
export declare function resolveCodingAgentDisplayName(): string;
