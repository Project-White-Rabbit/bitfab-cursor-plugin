interface ActivePreviewSession {
    sessionId: string;
    serviceUrl: string;
    pid: number;
    startedAt: string;
}
export declare function writeActivePreviewSession(session: Omit<ActivePreviewSession, "pid" | "startedAt">): void;
/**
 * Clear the file only if the recorded pid matches the current process. This
 * prevents a slow-exiting old preview from wiping out a newer one's record.
 */
export declare function clearActivePreviewSession(): void;
/**
 * Return the active preview session, or null if no preview is open. Treats
 * the file as stale when the recorded pid is no longer running.
 */
export declare function readActivePreviewSession(): ActivePreviewSession | null;
export {};
