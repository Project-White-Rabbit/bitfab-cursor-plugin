import { getConfig } from "./config.js";
import { emitJson } from "./output.js";
export function emitPageLink(path) {
    emitJson({
        event: "link",
        url: `${getConfig().serviceUrl.replace(/\/$/, "")}${path}`,
    });
}
