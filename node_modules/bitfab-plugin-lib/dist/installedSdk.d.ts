export type SdkLanguage = "typescript" | "python" | "ruby" | "go";
export interface InstalledSdk {
    language: SdkLanguage;
    packageName: string;
    declaredVersion: string | null;
    resolvedVersion: string | null;
    manifestPath: string | null;
    lockfilePath: string | null;
    /** Relative to the rootDir passed to getInstalledSdks; "." for the root. */
    workspacePath: string;
    /**
     * When the workspace uses the legacy `bitfab` npm package instead of
     * `@bitfab/sdk`, this is set to `"bitfab"` so the update flow can
     * remove the old package and install the new one.
     */
    renameFrom: string | null;
    deprecated?: boolean;
}
export declare function getInstalledSdks(rootDir: string): InstalledSdk[];
export declare function getCurrentVersion(sdk: InstalledSdk): string | null;
