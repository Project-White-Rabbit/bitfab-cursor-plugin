import type { ReplayProgressEvent } from "./replayProgressView.js";
export interface ReplayItemRef {
    traceId?: string | null;
    replayTraceId?: string | null;
    originalTraceId?: string | null;
    itemPath: string;
    error?: string | null;
    durationMs?: number | null;
    carriedOver?: boolean;
}
export declare function externalizeProgressEvent(event: ReplayProgressEvent, itemsDir: string, index: number): {
    event: ReplayProgressEvent;
    itemPath?: string;
};
export declare function externalizeCompleteResult(result: unknown, itemsDir: string): ReplayItemRef[];
export declare function buildCompleteSummary(result: unknown): Record<string, unknown>;
/**
 * Items a resumed replay finished before the stop come back with
 * `carriedOver: true` and no payloads. They count as already finished.
 */
export declare function isCarriedOver(item: unknown): boolean;
