import { CODEX_RESOLVE_PLUGIN_DIR_BASH, Flow } from "bitfab-flow";
import { BITFAB_PLUGIN_COMMANDS } from "./commandWrappers.js";
import { FRAMEWORK_VARIABLES } from "./frameworks.js";
/**
 * Shared fan-out judging pattern, referenced verbatim by the two steps that do
 * per-item LLM judging (Phase 3 `label-self` and Phase 5 `evaluate-results`).
 * Both are embarrassingly parallel: every verdict depends only on its own
 * item's artifacts, and the judge only reasons and returns JSON (it never edits
 * files), so above a small threshold the judging fans out across read-only
 * general-purpose subagents and collects back into the one batched persist call
 * each site already makes. Keeping the mechanics in one constant keeps the two
 * sites (threshold, batch sizing, read-only subagents, collect-then-persist) in
 * lockstep; each site supplies only its own per-item payload, rubric, and
 * persist call around it.
 */
const FAN_OUT_JUDGING = `{{#claude}}**Scale the judging with fan-out when there are many items.** Per-item judging is embarrassingly parallel: each verdict depends only on that one item's own artifacts (plus the fixed rubric and any shared context you gather once below), never on the other items, and the judge only reasons and returns JSON, it never edits files. So pick serial or fan-out by the item count:

- **At or below ~15-20 items: stay serial.** Judge every item yourself, inline in this agent, exactly as described above. Below that threshold the subagent spawn overhead outweighs the parallelism, so serial is faster.
- **Above ~15-20 items: fan out.** Split the items into batches (aim for one batch per subagent, roughly 8-12 items each, so even a large dataset resolves in a handful of subagents) and spawn one read-only subagent per batch with the Agent tool, \`subagent_type: "general-purpose"\`. Each subagent reasons over the payloads you hand it and returns its batch's verdicts as JSON. These judges only read and return data: do **NOT** pass \`isolation: "worktree"\` and do **NOT** depend on bypass permissions (that gating is only for the code-editing experiment fork in \`pick-execution-mode\`). A judge never edits files, runs replay, opens Studio, or calls MCP tools.

Make each subagent prompt fully self-contained: its batch's per-item payloads (each item carries its own artifacts, enumerated below), the fixed rubric, and any shared context you gathered once (so no subagent re-derives it or touches the repo). Tell it to return one verdict entry per item in the exact shape this step persists, and nothing else.

**Then collect and persist once.** Wait for every batch, concatenate their verdict arrays into the single full set covering all items, and make the one batched persist call this step already describes, unchanged. Fan-out changes only how you produce the verdicts, never how they are stored or routed: same call, same shape, same buckets, same downstream steps.{{/claude}}{{^claude}}**Judge serially.** This editor doesn't use subagents for judging, so judge every item yourself, inline in this agent, as described above, regardless of how many there are.{{/claude}}`;
export const assistantFlow = Flow.parse({
    schemaVersion: 1,
    id: "assistant",
    title: "Bitfab Assistant",
    frontmatter: {
        description: "Iterate on a traced function to improve pass rates using failed traces, labeling, and replay; also replays a single trace to check whether a fix worked. TRIGGER when: user explicitly asks Bitfab for help improving or optimizing a traced function in any way (quality, pass rate, token usage, cost: 'use bitfab to ...', 'can bitfab help me ...'), wants to fix failing AI outputs, improve pass rates, reduce token usage or LLM cost of a traced function against a dataset, debug LLM behavior, iterate on prompts, label traces, run experiments, benchmark or score a dataset against the current code, run a regression test, run a dedicated cost-optimization pass that profiles token spend and iterates to cut it, add a trace to a dataset, fix a bug **in a traced function, starting from its failing Bitfab trace**, end to end (edit the code and replay that trace until it passes, then add that trace to a dataset with a validated failing label and ask whether to re-run the full dataset as a Studio experiment or inspect the before/after in Studio), OR replay/re-run one specific trace to verify a change; says anything like 'fix my AI', 'improve pass rate', 'fix this trace', 'fix trace <id>', 'fix this failing trace and lock it in as a test', 'use bitfab to reduce token usage', 'cut tokens on this dataset', 'lower the token cost of my traced function', 'cost optimize generate-email', 'optimize the cost of this function', 'why is this failing', 'iterate on traces', 'debug my agent', 'review traces', 'benchmark my dataset', 'run my dataset as a benchmark', 'how does my code score on this dataset', 'evaluate the dataset without changing anything', 'add a trace to a dataset', 'attach traces to a dataset', 'replay this trace', 'rerun trace <id>', 'did my fix work', 'does this trace pass now', 'check if my change fixed <id>', 'show experiments for a dataset', 'list experiments that ran on this dataset', 'what experiments used dataset <id>', 'open the experiments for <dataset>'. SKIP when: user wants to instrument new code or set up tracing (use bitfab:setup instead), or wants generic token/cost/code optimization that mentions none of Bitfab, traces, or datasets. In particular, a bare 'fix this bug' / 'fix X' that is NOT anchored to a specific Bitfab trace or traced function (an ordinary coding bug, a UI fix, a failing unit test, a stack trace, a code review nit) is normal coding work and must NOT trigger this skill, even though it contains the word 'fix'. The fix mode requires a Bitfab trace (a trace ID, or a failure the user is clearly pointing at in a traced function); without that anchor, do not engage.",
    },
    emitTemplate: '**Studio activity:** If `studioMode` is true, run `{{command:pushActivity}} {action} "{displayName}"`.',
    commands: BITFAB_PLUGIN_COMMANDS.defineCommands({
        status: {
            description: "Check plugin authentication and connection status",
        },
        openStudio: {
            command: "openStudioTo",
            description: "Navigate an existing Studio session or open a new one at the given path",
            args: "<path>",
        },
        pushActivity: {
            description: "Emit activity events to the Studio sidebar",
            args: '{action} "{displayName}"',
        },
        detectCapabilities: {
            description: "Detect the installed SDK's replay capabilities and version-update status, emitted as a <bitfab-replay-capabilities> JSON block",
        },
        persistReplayLabels: {
            description: "Persist replay verdicts from a JSON file to Bitfab via MCP",
            args: "<verdicts-file>",
        },
        replayProgress: {
            description: "Run a replay and emit one ready-to-relay line per trace to stdout (header, then per trace: a pass/fail glyph, running n/total, and that trace's duration, with the error reason inline on failure; a liveness heartbeat line when a slow trace goes quiet; then a summary with total + average time), write a per-run replay event index to .bitfab/replays/<run-id>/events.jsonl, and write full item payloads to .bitfab/replays/<run-id>/items/*.json. Background it and relay each new line to the user",
            args: "[--label <key>] [--total N] [--out <path>] [--run-dir <path>] [--events-log <path>] [--items-dir <path>] -- <replay command...>",
        },
        readTracesBatched: {
            description: "Read many traces at once: fans read_traces out in parallel batches of 10, writes the concatenated result to a temp file, and prints its path as JSON",
            args: "<trace-id...> [--scope summary|full]",
        },
        closeStudio: {
            description: "Close the active Studio session (tab + background event process); no-op when nothing is open",
            args: "[message]",
        },
        clearStudio: {
            command: "clearStudioSession",
            description: "Clear the stale active-Studio pointer so the next open starts fresh",
        },
    }),
    tools: {
        bash: { kind: "builtin", name: "Bash" },
        read: { kind: "builtin", name: "Read" },
        glob: { kind: "builtin", name: "Glob" },
        grep: { kind: "builtin", name: "Grep" },
        edit: { kind: "builtin", name: "Edit" },
        write: { kind: "builtin", name: "Write" },
        agent: { kind: "builtin", name: "Agent" },
        ask: { kind: "builtin", name: "AskUserQuestion" },
        monitor: { kind: "builtin", name: "Monitor" },
        skill: { kind: "builtin", name: "Skill" },
        listFunctions: { kind: "mcp", name: "list_trace_functions" },
        searchTraces: { kind: "mcp", name: "search_traces" },
        readTraces: { kind: "mcp", name: "read_traces" },
        readTraceLabels: { kind: "mcp", name: "read_trace_labels" },
        updateAgentLabels: { kind: "mcp", name: "update_agent_labels" },
        setHumanLabels: { kind: "mcp", name: "set_human_labels" },
        listDatasets: { kind: "mcp", name: "list_datasets" },
        createDataset: { kind: "mcp", name: "create_dataset" },
        addTracesToDataset: { kind: "mcp", name: "add_traces_to_dataset" },
        removeTracesFromDataset: {
            kind: "mcp",
            name: "remove_traces_from_dataset",
        },
        getTracePlan: { kind: "mcp", name: "get_trace_plan" },
        listExperiments: { kind: "mcp", name: "list_experiments" },
        getExperimentTraces: { kind: "mcp", name: "get_experiment_traces" },
        getReplayStatus: { kind: "mcp", name: "get_replay_status" },
        getTemplateReference: { kind: "mcp", name: "get_template_reference" },
        getTemplate: { kind: "mcp", name: "get_template" },
        updateTemplate: { kind: "mcp", name: "update_template" },
    },
    intro: `Use the local plugin MCP tools ({{tool:listFunctions}}, {{tool:searchTraces}}, {{tool:readTraces}}, {{tool:readTraceLabels}}, {{tool:updateAgentLabels}}, {{tool:listDatasets}}, {{tool:createDataset}}, {{tool:addTracesToDataset}}, {{tool:removeTracesFromDataset}}, {{tool:listExperiments}}, {{tool:getExperimentTraces}}, {{tool:getReplayStatus}}) to find what's failing in a traced function, build a dataset of labeled traces, and iterate on the code/prompts using replay until pass rates improve.

**MCP tools:** This skill uses \`list_trace_functions\`, \`search_traces\`, \`read_traces\`, \`read_trace_labels\`, \`update_agent_labels\`, \`set_human_labels\`, \`list_datasets\`, \`create_dataset\`, \`add_traces_to_dataset\`, \`remove_traces_from_dataset\`, \`get_trace_plan\`, \`list_experiments\`, \`get_experiment_traces\`, and \`get_replay_status\` from the **local plugin MCP server** (bundled with this plugin){{#claude}}. Do NOT use the remote Bitfab MCP tools (\`mcp__Simforge__*\` or \`mcp__Bitfab__*\`), use only the \`mcp__plugin_bitfab_Bitfab__*\` variants.{{/claude}}{{^claude}}, exposed under the \`mcp__Bitfab__*\` prefix.{{/claude}}

{{^codex}}**Always use** \`AskUserQuestion\` **when asking questions, reporting results, or presenting choices** (one exception: the \`benchmark\` scorecard is printed as Markdown tables directly in chat, not via \`AskUserQuestion\`, since tables don't render inside that UI, see Phase Benchmark). Never print a question as text and wait. Rules:{{/codex}}{{#codex}}**When the flow branches, always present the options clearly and wait for the user's answer before proceeding.** Number or letter the options so the user can pick by reference. Rules:{{/codex}}

- Recommend an option first, explain why in one line
- Present 2-5 concrete options
- One decision per question, never batch

**Execution style (applies to every phase).** Default to terse, action-first turns when investigating:
- During mechanical work (reading traces, searching code, inspecting instrumentation), run the tools and report only what you found. Do not narrate each command or pre-announce what you are about to do. This does not change how you present results or choices: those still follow the question-and-answer rules above.
- Batch read-only probing: combine related shell checks into one command (separate them with \`;\`, not \`&&\` (a no-match \`grep\` exits non-zero and would abort an \`&&\` chain, skipping later probes)), and read multiple files or traces in a single batch rather than one per turn. Adaptive follow-ups that depend on a prior result are expected and fine; the goal is to collapse only the fixed, independent probes. (Replay, experiment, and test runs are real work, not probing, leave them as-is.)
- Keep prose between tool calls to one line or none. Save fuller explanation for decision points and the result summaries the user acts on.
- Surfacing a risk, ambiguity, or unexpected finding is never the narration to suppress: raise it immediately, even mid-probe (e.g. a trace that contradicts the hypothesis, a labeling call you're unsure about, instrumentation that looks wrong).

**Trace-first debugging rule (hard guard for instrumented AI behavior).** When the user asks why a traced AI workflow failed, whether a behavior is a regression, whether a PR caused an AI failure, or what fix/revert is appropriate for a traced function, do not declare root cause, recommend a revert/no-revert, or propose the fix path until you have either read the relevant Bitfab trace or explicitly proved no relevant trace exists. The required order is:

1. Identify the trace function from code instrumentation first whenever the repo is available (imports such as \`@bitfab/sdk\`, \`withSpan\`, \`getFunction\`, \`traceable\`, provider wrappers, replay scripts, or local \`observability/providers/bitfab.*\` files). Treat \`list_trace_functions\` as confirmation/fallback, not the primary source of truth when code can reveal the key.
2. Search Bitfab traces using the function key plus whatever anchors the user supplied: failing query text, time window, org/session/user metadata, trace/session links, screenshots, labels, or recent activity.
3. Read the candidate trace(s), using \`scope: "full"\` when planner/model outputs, child spans, retrieval counts, tool calls, or errors determine the answer.
4. State what the trace proves separately from any code-based inference. Include the trace ID(s), function key, and the decisive span/input/output fields. If no trace exists, say exactly what function key and filters were searched before falling back to code-only diagnosis.

Do not stop after \`list_trace_functions\`. If the expected function is absent from the list, inspect the code for the actual key and search traces directly; if that also fails, report "no relevant trace found" with the search criteria before making a weaker code-only recommendation.

This skill has nine invocation modes, each a different entry point into the same pipeline. Six of them (\`wizard\`, \`dataset\`, \`investigate\`, \`experiment\`, \`cost-optimize\`, \`fix\`) converge on the shared replay-and-iterate loop (Phase 5) and follow the same path to the end. \`wizard\`, \`dataset\`, and \`investigate\` first build a labeled dataset and diagnose failures (dataset → diagnose → experiments → wrap up); \`experiment\` and \`cost-optimize\` skip dataset-building and enter at Phase 5 against an existing dataset, with \`cost-optimize\` first running its own cost-diagnosis phase (profile token spend, plan reductions) and entering with \`costRun\` forced on. \`fix\` enters at its own Phase Fix: it resolves a single failing trace and diagnoses it, then tails into the Phase 5 loop to make the fix and replay only that target trace first. That targeted replay is tagged with an experiment group but Studio stays closed until after labels persist. Only once the target trace passes does \`fix\` add it to a dataset with a validated failing label; then the user chooses whether to show the fix in Studio, re-run the same change across the full dataset as an experiment (in Studio or terminal-only), keep iterating on the target trace, or stop. \`benchmark\`, \`add-trace\`, and \`replay\` are the exceptions, \`benchmark\` enters at the replay step, runs no diagnosis/experiments/wrap-up, and exits at a terminal scorecard; \`add-trace\` enters at its own phase, attaches the trace(s) to a dataset, and stops; \`replay\` enters its own self-contained phase for lightweight single-trace replay, runs entirely in-chat, and stops. The user can stop early at any decision point, but the default is to continue. Most sub-modes require the trace function key as the argument because they skip the function picker (Phase 1) and instrumentation/replay verification (Phase 2).

| Mode | Invocation | Action |
|---|---|---|
| \`wizard\` | \`{{cmd}}assistant\`, \`{{cmd}}assistant wizard [<key>]\`, or \`{{cmd}}assistant <key>\` (\`<key>\` is a function key; a lone trace UUID is NOT a key and never \`wizard\`, it routes to \`replay\` or \`fix\` by intent, see argument routing) | Full flow: pick function → verify instrumentation → pick or create dataset → label → diagnose → iterate → wrap up |
| \`investigate\` | \`{{cmd}}assistant investigate [<key>]\` | Free-form investigation of an issue the user is describing. Read traces and code as needed to characterize the problem, then offer to stop with a summary, write a written analysis report, or roll into dataset building and continue through experiments. \`<key>\` is optional, the agent picks the function from what the user says when it isn't given |
| \`dataset\` | \`{{cmd}}assistant dataset <key>\` | Build or extend a labeled dataset for one function, then diagnose failures and iterate with experiments. Picks an existing dataset or creates a new one |
| \`experiment\` | \`{{cmd}}assistant experiment <key> [<dataset-id>]\` | **Edits code** to fix failing traces, replays against a labeled dataset, and iterates. Use when the user wants to *change the code and see if it improves*. If \`<dataset-id>\` is omitted, you'll be asked to pick one. If the function has no datasets yet, run \`{{cmd}}assistant dataset <key>\` first |
| \`fix\` | \`{{cmd}}assistant fix [<key>] <trace-id>\` | **Fix one specific bug, end to end.** Resolve and diagnose the failing trace, then edit the code and replay only that trace until it passes. The targeted replay gets an experiment group for before/after inspection, but Studio opens only after the replay verdict is persisted and the user asks to inspect it. Only once the trace passes is it added to a dataset with a **validated failing label**. Then ask whether to show the fix in Studio, re-run the full dataset as an experiment (in Studio or terminal-only), keep iterating on the target trace, or stop. Use when the user points at a specific bug or **failing Bitfab trace** in a traced function and wants it *fixed* and saved in a dataset. **Requires a trace anchor** (a trace ID, or a failure the user is clearly pointing at in a traced function): a bare "fix this bug" about ordinary code with no trace involved is NOT this mode (see the hard guard in argument routing). \`<key>\` is optional (inferred from the trace). Also triggered by natural language ("fix this trace", "fix trace abc123", "find and fix the trace where <bad behavior happened>", "this trace is wrong, make it pass") |
| \`cost-optimize\` | \`{{cmd}}assistant cost-optimize <key> [<dataset-id>]\` | **Cuts token cost.** Profiles where the dataset's tokens go (prompt bloat, redundant context, output shape, model choice) in a dedicated cost-diagnosis phase, then **edits code** and replays against a labeled dataset to lower cost while holding the pass rate. Runs with \`costRun\` always on, so the token-cost lens and token-delta reporting are automatic; the \`costBasis\` (all vs uncached) is still inferred per run. A dedicated front door for what \`experiment\` does when its goal is cost, and unlike \`experiment\` the dataset only needs \`≥1 trace\` (cost runs often target an already-passing-but-expensive function). If \`<dataset-id>\` is omitted, you'll be asked to pick one |
| \`benchmark\` | \`{{cmd}}assistant benchmark <key> [<dataset-id>] [studio]\` | **No edits to the function under test.** Replay a labeled dataset against the current code as-is, evaluate each trace against its labels, and report a pass/fail scorecard, then stop. Use when the user wants to *measure the current code* (regression test, baseline, score after unrelated changes), not improve it. Infra fixes that unblock the replay (SDK / replay-script upgrade, \`mockOnReplay\` on a failing span) are allowed (they don't change the behavior being measured); what benchmark never does is make experiment-style edits to the traced function. If \`<dataset-id>\` is omitted, you'll be asked to pick one. **Terminal-only by default (no Studio).** Append the \`studio\` keyword to open Studio's experiments page and stream verdicts live as the replay runs |
| \`add-trace\` | \`{{cmd}}assistant add-trace [<key>] <trace-id...> [<dataset-id>]\` | Lightweight: attach one or more existing traces to a dataset (pick or create one), then stop. No labeling, diagnosis, experiments, or Studio. \`<key>\` is **optional**: it's inferred from the traces when omitted, so \`add-trace <trace-uuid>\` (trace IDs only) is valid. The request can also be phrased in natural language (e.g. "add trace abc123 to a dataset") |
| \`replay\` | \`{{cmd}}assistant replay <key> <trace-id>\` | Minimal single-trace replay, entirely in-chat: find the replay script, run it against one trace, report pass/fail in one line. No Studio/browser, no dataset, no labeling, no diagnosis, no server-side verdict persistence. For when you already made a fix and just want to know if it worked |

**Argument routing.** If the argument is free-form text (not a mode name or bare function key), infer the best mode and extract the trace function key if mentioned. A lone UUID with no mode keyword is a **trace ID, never a function key** (never \`wizard\`). Route a bare trace ID by intent: \`replay\` to *verify* a change the user already made ("did my fix work on \`<id>\`"), or \`fix\` to *make* the fix and lock the trace in as a regression test ("fix \`<id>\`", "this trace is wrong"); see the \`fix\` disambiguation note below. Confirm your pick in one line before entering the flow (e.g. "Starting investigate for \`generate-email\`."). If you can't pick a single mode, ask {{presentMode}}. Natural-language requests to attach a specific trace to a dataset (e.g. "add this trace to a dataset", "put trace abc123 in my dataset") route to \`add-trace\`; extract the trace IDs and any function key or dataset ID mentioned. In \`benchmark\` mode, a trailing \`studio\` token (e.g. \`benchmark generate-email studio\`) or a natural-language "with studio" / "open studio" sets the **Studio opt-in** for that run: strip it from the positional args before resolving the function key and dataset ID, and hold it as a working-context flag. It applies only to \`benchmark\` (every other mode always opens Studio). **When the opt-in is set, treat \`studioMode\` as true for the rest of the run** (Studio is open), so the **Studio activity** (\`{{command:pushActivity}}\`) steps fire just as in the always-Studio modes; a terminal-only benchmark leaves \`studioMode\` false and those steps no-op.

**Token-cost goal (\`costRun\`).** Separately from the mode, classify the run's *optimization goal* (**except in \`cost-optimize\` mode, where \`costRun\` is always true by definition**, don't classify it, just confirm the mode and pick the \`costBasis\` below). Set a working-context flag \`costRun = true\` whenever the user's goal, **however they phrase it**, is to spend fewer tokens or less money. Match on intent, not keywords: "cut tokens", "reduce cost", "make it cheaper", "trim the prompt to save tokens", "we're over budget", "lower spend", or a token/cost budget the dataset is built around are **examples, not an exhaustive list**: "this is burning too much", "the prompt got huge", "what's this costing us", "make it leaner", and the like all count. When you genuinely can't tell whether cost is a goal, default \`costRun = false\` (the goal is output quality / pass rate). **Say which way you read it in your one-line entry confirmation** (the same line where you confirm the mode), e.g. "Starting experiment for \`generate-email\`, tracking token cost." A visible guess is one correction away; a silent one isn't. \`costRun\` is orthogonal to the mode: an \`experiment\` that trims a prompt and a \`benchmark\` that measures current token cost are both cost runs, and it is independent of the benchmark \`studio\` opt-in. Holding this flag once, here, is what makes the token-cost lens deterministic downstream: when \`costRun\` is set, the experiments page opens with \`&tokens=1\` and the \`evaluate-results\`, \`share-results\`, and benchmark scorecard steps report token deltas alongside pass/fail, instead of each step re-guessing mid-run whether the run "feels like" a cost run. (Latency, the per-item \`durationMs\` in the replay output, can ride along in the same delta reporting when the user also cares about speed.)

**Token basis (\`tokenType\`), set only when \`costRun\` is.** A cost run also has to pick *which* tokens to count, because prompt caching splits them and cache reads cost far less than fresh input. So when you set \`costRun\`, also fix a working-context \`costBasis\`:
- \`uncached\` when the goal is **money / spend**: "make it cheaper", "lower the bill", "we're over budget", "what's this costing", "stop wasting money". The page counts \`(input - cached) + output\`, which tracks real dollars and surfaces a regression that grew the fresh-token work while prompt caching kept the all-token total looking flat.
- \`all\` (the default) when the goal is **raw token count / context size**: "the prompt got huge", "cut tokens", "trim the context", "we're near the context limit". The page counts \`input + output\`, cache reads included.

When you genuinely can't tell, default \`all\`. State the basis in the same one-line confirmation as \`costRun\`, e.g. "Starting experiment for \`generate-email\`, tracking token cost (uncached)." As with \`costRun\`, deciding this once here is what lets \`<tokensSuffix>\` resolve deterministically downstream instead of each open re-guessing the basis. \`costBasis\` is meaningless when \`costRun\` is false (no lens shows at all), so only carry it on a cost run.

**Reset \`costRun\` if the user changes their mind after entry** (set it true on any later cost signal, not just an explicit "show me tokens": asking what a run costs, noting token counts look high, worrying about spend; set it false when they say to stop). The same intent-not-keywords reading applies here as at entry. The entry value is the default, not a lock; resetting on an explicit request is not the autonomous step-to-step re-guessing you must avoid. On a reset, re-open whatever token-capable surface is currently open so the live view matches: the experiments page in any form (\`?experimentGroupId=\`, \`?testRunIds=\`, or \`?datasetId=\`), re-navigated with or without \`&tokens=1\`. If nothing relevant is open, just hold the new value and the next open picks it up. One asymmetry to know: re-navigating with \`&tokens=1\` turns the lens on, but a param-less re-nav cannot turn it off (the page keeps the token columns sticky once shown); resetting to false still correctly drops the chat-side token-delta reporting in \`evaluate-results\` / \`share-results\` / the scorecard. **\`costBasis\` resets the same way.** A later signal can flip the basis without touching \`costRun\` (e.g. a token-count run where the user then says "what's the bill" flips \`costBasis\` to \`uncached\`; "I just mean total tokens" flips it back to \`all\`). On a basis change, re-open the open experiments surface with the matching suffix (\`&tokens=1&tokenType=all\` for \`all\`, \`&tokens=1&tokenType=uncached\` for \`uncached\`) so the live trend recolors, and the chat-side reports pick up the new basis on the next report. The toggle in the page header is the user's own live override of the same basis.

**Disambiguating \`benchmark\` from \`experiment\`** (both replay a dataset, so free-form text is easy to misroute):

- Pick **\`benchmark\`** when the user wants to *measure the current code as-is*: "benchmark", "score", "baseline", "regression test", "how does it do right now", "evaluate the dataset without changing anything", "just run the dataset and tell me the pass rate". Benchmark makes **no edits to the traced function under test** and stops after the scorecard (it may still upgrade the SDK / replay script or add \`mockOnReplay\` to unblock the replay, those are infra, not the behavior being measured).
- Pick **\`experiment\`** when the user wants to *change the code and see if it improves*: "fix", "improve", "iterate", "try a prompt change", "make these traces pass". Free-form token/cost-reduction asks ("reduce token usage", "cut tokens", "lower the cost") prefer **\`cost-optimize\`** instead (see the cost-optimization routing note below), the dedicated cost front door; \`experiment\` still handles cost when invoked explicitly, and either way they are \`costRun\` runs (see the \`costRun\` note above) so the token-cost lens (\`&tokens=1\`) and token-delta reporting come on automatically. Experiment edits code and loops.

When in genuine doubt between the two, default to **\`benchmark\`** (it's non-destructive, no edits, and the user can roll into \`experiment\` afterward), but say which you picked and why in one line so they can redirect.

**Disambiguating \`replay\` from \`experiment\`** (both re-run one or more traces against the code, so free-form text is easy to misroute):

- Pick **\`replay\`** when the user wants to *re-run one specific trace and hear whether it's fixed/passing now*, with nothing persisted: a UUID (trace ID) appears in the message (alone or with a function key); verify-a-fix phrasing like "did my fix work", "does this trace pass now", "rerun/replay this trace", "check if my change fixed it", "is \`<id>\` good now"; or the message is scoped to a single trace and a change the user already made, with no mention of datasets, labeling, pass rates across many traces, or "experiments." \`replay\` is in-chat only and makes no edits of its own.
- Pick **\`experiment\`** (or **\`cost-optimize\`** for a cost goal, see the cost-optimization routing note below) when the user wants to *change the code and see if it improves a whole labeled dataset*: "fix", "improve", "iterate", "try a prompt change", "make these traces pass", "improve the pass rate", "reduce token usage on the dataset", "cut tokens", "lower the cost", "run experiments on the failures." Token/cost-reduction asks pick an editing mode **even when a trace ID is present**: \`replay\` makes no edits, so it can't optimize anything (the mentioned trace just helps pick the function/dataset). The editing modes edit code, replay a dataset, and loop.

When in genuine doubt between the two, default to **\`replay\`** if a single trace ID is present AND the intent is verify-only (not "fix it", not token/cost reduction): it's the lighter, in-chat path and the user can roll into \`fix\` or \`experiment\` afterward. If the single-trace intent is to actually fix the bug, prefer **\`fix\`** (see its disambiguation note below); otherwise default to **\`cost-optimize\`** for a token/cost-reduction intent (see the next note) and **\`experiment\`** for a quality intent across a dataset. Either way, say which you picked and why in one line so the user can redirect.

**Disambiguating \`fix\` from \`replay\` and \`experiment\`** (all three involve a trace and the code, so a "fix" ask with a trace ID is easy to misroute):

- Pick **\`fix\`** when the user points at **one specific failing trace and wants it actually fixed and saved in a dataset**: "fix this bug", "fix trace \`<id>\`", "find and fix the trace where <bad behavior happened>", "this trace is wrong, make it pass", "this output is broken, fix it and save it as a test." The trace anchor can be a trace ID **or a smoking-gun description to search for** ("the trace where Peek remembered a random config value", "the one that answered with the old policy"). If the user says **"find and fix"** / **"search ... and fix"** / **"fix the trace where ..."**, route to \`fix\`, not \`investigate\`: the search is just how Phase Fix resolves the target trace. \`fix\` edits code, but unlike \`experiment\` it bootstraps its own dataset from that one trace (adding it with a validated failing label once the fix proves out) instead of needing an existing labeled dataset, and unlike \`replay\` it makes the change itself instead of only verifying one you already made.
- Pick **\`replay\`** when the user only wants to **verify a change they already made** on one trace, nothing persisted: "did my fix work", "does \`<id>\` pass now", "rerun this trace." No editing, no dataset, no saved label.
- Pick **\`experiment\`** when the scope is a **whole labeled dataset** ("improve the pass rate", "fix the failures", "run experiments on the dataset"), not a single named trace, and a dataset with validated failing labels already exists.

When in genuine doubt with a single trace ID present: a "fix it / make it pass" intent goes to \`fix\`; a "did it work / check it" intent goes to \`replay\`. Say which you picked and why in one line so the user can redirect.

**Hard guard on \`fix\` (do not over-trigger):** \`fix\` is only for a bug in a **traced function**, anchored to a **Bitfab trace** (a trace ID, or a failure the user is clearly pointing at in a function Bitfab has traced). A bare "fix this bug" / "fix X" about ordinary code with no trace involved (a normal code bug, a UI issue, a failing unit test, a stack trace, a lint/review nit) is regular coding work, **not** \`fix\` and not this skill at all. The word "fix" alone is not enough: if there's no trace anchor, do not enter \`fix\` (or any mode), let the request fall through to ordinary coding. When the user explicitly typed \`{{cmd}}assistant fix\` but gave no trace, ask which trace (Phase Fix's \`resolve\` does this); when they did not invoke the skill and there's no trace anchor, don't engage at all.

**Routing cost-optimization asks (\`cost-optimize\` vs \`experiment\`).** When the user's explicit goal is to spend fewer tokens or less money on a function ("cost optimize \`generate-email\`", "make this cheaper", "cut the token cost of this function", "reduce spend on \`<key>\`", "trim tokens without breaking it"), route to **\`cost-optimize\`**: it's the dedicated front door that profiles token spend first, then iterates with \`costRun\` forced on. \`experiment\` still accepts cost goals (it sets \`costRun\` from intent the same way), so honor an explicit \`{{cmd}}assistant experiment <key>\` even when the goal is cost, but for free-form cost asks prefer \`cost-optimize\`. Both edit code and replay a labeled dataset; the differences are \`cost-optimize\`'s upfront cost-diagnosis phase, its always-on cost lens, and its lighter \`≥1 trace\` dataset gate (\`experiment\` needs \`≥1 validated failing label\`, which a pure cost run on a passing function won't have). As with the other sub-modes, a lone trace UUID routes to \`replay\` or \`fix\` by intent, never \`cost-optimize\`.

For \`replay\`, resolve its two arguments:

- **Function key:** use it if the user named it. If only a trace ID was given, you don't have to ask, Phase Replay's \`setup\` step reads the trace (\`read_traces\`), which returns the function key; derive it there.
- **Trace ID:** the UUID from the message.

In sub-modes that take a function key, grep the codebase for \`<key>\` early so labeling and experiments are grounded in the actual instrumented function (the full flow does this in Phase 2; sub-modes skip Phase 2 entirely). \`investigate\` mode does its own function lookup and code grep in Phase Investigate. \`replay\` mode does its own grep in Phase Replay's setup step. \`add-trace\` mode skips code grounding entirely, it never greps the codebase; it only resolves the trace's function key (via \`read_traces\` when not supplied) to scope the dataset.

**Studio** is the companion browser surface for the assistant flow. In every mode **except \`benchmark\`, \`add-trace\`, \`replay\`, and the initial \`fix\` pass** it opens automatically at the start and stays open throughout all phases, and individual phases navigate it to the relevant page (dataset review, experiment viewer, etc.). \`add-trace\` and \`replay\` are terminal-only and never open Studio. \`fix\` starts terminal-only while it fixes and replays the target trace; that targeted replay is tagged with an experiment group, but Studio opens only if, after the target trace passes and is added to a dataset, the user chooses to show the fix in Studio or re-run the full dataset experiment **in Studio** (the full-dataset re-run can also be run terminal-only, which keeps Studio closed). \`benchmark\` is terminal-only **by default** too, but opting in with the \`studio\` keyword opens Studio and navigates to the experiments page for this test run so verdicts stream in live as the replay runs.

**Opening a trace plan, when asked.** Opening trace plans is part of this skill, not a separate primitive, but only do it when the user asks (or the context clearly implies it, e.g. they said "show me what's captured"). Never auto-open. When triggered, run two sequential calls (step 2 needs the planId from step 1, so they can't be batched): (1) {{tool:getTracePlan}} with \`{ traceFunctionKey: "<key>" }\` returns the plan id, then (2) \`openStudioTo.js "/studio/trace-plan/<planId>"\` (substituting the id from step 1) routes Studio there in-place. The command finds an active session or opens a new one automatically. The Studio chrome (header, session indicator, agent activity) stays mounted around the trace plan content. No questions, no preamble, no summary up-front. If no plan exists for the key, say so in one line and offer \`{{cmd}}setup modify <key>\` to build one.

**Opening a dataset's experiments, when asked.** Listing the experiments that have run against a dataset is part of this skill, not a separate primitive, but only do it when the user asks (e.g. "show experiments for dataset \`<id>\`", "what experiments ran on this dataset", "list experiments for \`<name>\`"). Never auto-open. **Establish the dataset first** (never open before the dataset is resolved): if the user gave a dataset UUID, use it; if they named a dataset or gave only a function key, call {{tool:listDatasets}} to resolve it, and {{askVerb}} so the user can pick when more than one matches. Then run \`{{command:openStudio}} "/studio/experiments?datasetId=<datasetId>"\` (substituting the resolved id) to route Studio there in-place. Gate the lens on the \`costRun\` flag, not on this request's wording: when \`costRun\` is set (established in argument routing, whether from the entry classification or because this very request frames the work around tokens, cost, or a token budget), open \`...?datasetId=<datasetId>&tokens=1&tokenType=<all|uncached>\` (the basis from \`costBasis\`, always explicit; see the token-cost lens note in \`open-experiments-before-replay\`) so the token-cost lens shows the original → replay total-token trend per experiment; when \`costRun\` is false, open without \`&tokens=1\`. This keeps a neutral "show experiments for this dataset" mid-cost-run from dropping the lens. The page lists every experiment (test run) that replayed a trace belonging to the dataset (derived server-side from the shared traces, so it covers past runs too). The command finds an active session or opens a new one automatically. No preamble or summary up-front; confirm in one line, identifying the dataset by name when known and id otherwise. If the dataset has no experiments yet, the page says so, offer \`{{cmd}}assistant experiment <key> <datasetId>\` or \`{{cmd}}assistant benchmark <key> <datasetId>\` to run one.
{{#codex}}

🚨 **Studio command foreground rule:** Plugin CLIs that open or navigate Studio can stay alive while the user acts in the browser. Always launch Studio-opening commands with the runtime's background / long-running exec mechanism and read their JSONL stdout incrementally, but do **not** block the conversation foreground waiting for the browser action to finish.

- For \`openStudioTo.js\` / dataset / experiment / trace-plan opens: read until you have the handshake lines you need (especially \`monitor.eventFile\`, or a gate such as \`not-responding\` / \`open-failed\`), then continue according to the step. Do not keep polling until the user clicks Done in Studio.
- User actions after the page is open arrive through the durable \`eventFile\`; steps that need those actions explicitly tail that file. A "Show in Studio" navigation only opens the view and returns to the next in-chat prompt.
- If a command exits quickly with a completion or error JSONL line, handle it immediately. If it remains running after the needed handshake, leave it in the background and move on.{{/codex}}`,
    entries: {
        wizard: {
            default: "studio/open",
            codex: "preamble/resolve-plugin-dir",
        },
        dataset: {
            default: "studio/open",
            codex: "preamble/resolve-plugin-dir",
        },
        experiment: {
            default: "studio/open",
            codex: "preamble/resolve-plugin-dir",
        },
        "cost-optimize": {
            // Mirrors `experiment` entry: open Studio, then pick an existing dataset.
            // The cost-diagnosis phase runs after pick-dataset, before the loop.
            default: "studio/open",
            codex: "preamble/resolve-plugin-dir",
        },
        investigate: {
            default: "studio/open",
            codex: "preamble/resolve-plugin-dir",
        },
        benchmark: {
            // Route through studio/open so the `studio` opt-in can open Studio; the
            // step self-skips (no command) and falls through to pick-dataset when the
            // flag is absent.
            default: "studio/open",
            codex: "preamble/resolve-plugin-dir",
        },
        "add-trace": "add-trace/resolve-context",
        replay: {
            default: "quick-replay/setup",
            codex: "preamble/resolve-plugin-dir",
        },
        fix: {
            // Fix starts terminal-only: replay only the target trace, and only after
            // it passes add it to a dataset, then ask whether to open Studio for
            // the full-dataset experiment.
            default: "fix/resolve",
            codex: "preamble/resolve-plugin-dir",
        },
    },
    defaultMode: "wizard",
    compile: {
        // All targets ship as a single monolith today. The flow is also kept
        // split-chain-compatible (semantic phase ids = sub-skill names, and Phase 5
        // is pre-split so every cross-phase dispatch lands on a phase's first step):
        // flipping `claude` to `"split-chain"` here compiles an orchestrator + one
        // sub-skill per phase. The split path is exercised by a test (see
        // assistant.test.ts "split-chain compatibility") so it can't silently rot.
        // Nothing is inlined: cleanup, identify-function, and every shared phase are
        // re-entered across mode chains, so under split-chain each must be its own
        // sub-skill (an inline phase cannot be re-entered).
        mode: "monolith",
        inlinePhases: [],
    },
    variables: {
        ...FRAMEWORK_VARIABLES,
        /**
         * How a question is presented to the user. Claude / Cursor have the
         * `AskUserQuestion` tool ("via AskUserQuestion"); Codex doesn't and
         * presents options inline ("as a numbered choice"). Used wherever the
         * body says "Present the … to the user {{presentMode}}: …".
         */
        presentMode: {
            default: "via `AskUserQuestion`",
            codex: "as a numbered choice",
        },
    },
    phases: [
        {
            id: "preamble",
            title: "Phase 0: Setup",
            stepStyle: "list",
            steps: [
                {
                    id: "resolve-plugin-dir",
                    kind: "action",
                    preamble: true,
                    toolCalls: ["bash"],
                    title: "Resolve BITFAB_PLUGIN_DIR",
                    body: `First, resolve \`BITFAB_PLUGIN_DIR\` if it isn't already exported in this shell. Run this block verbatim, it auto-detects dev / prod / custom-\`CODEX_HOME\` installs:

${CODEX_RESOLVE_PLUGIN_DIR_BASH}`,
                    next: "preamble/run-status",
                },
                {
                    id: "run-status",
                    kind: "action",
                    toolCalls: ["bash"],
                    commandCalls: ["status"],
                    title: "Check for updates",
                    body: `Run the status command to check for plugin updates:

\`\`\`bash
{{command:status}}
\`\`\`

If a \`v<X> available\` upgrade notice appeared, pass it through to the user verbatim, but don't block on it; surface the notice once and move on. Ignore the authentication status (Studio handles login inline).`,
                    next: {
                        byMode: {
                            wizard: "studio/open",
                            dataset: "studio/open",
                            experiment: "studio/open",
                            fix: "fix/resolve",
                            "cost-optimize": "studio/open",
                            investigate: "studio/open",
                            benchmark: "studio/open",
                            "add-trace": null,
                            replay: "quick-replay/setup",
                        },
                    },
                },
            ],
        },
        {
            id: "studio",
            title: "Studio Lifecycle",
            intro: `The Studio is the companion browser surface for the assistant flow. In every mode that uses it, it opens once at the start and stays open throughout all phases, with individual phases navigating it to the relevant page (dataset review, experiment viewer, etc.) using \`openStudioTo.js\`. **\`benchmark\` is the exception:** it opens Studio only when the run passed the \`studio\` opt-in. A terminal-only \`benchmark\` run (no \`studio\` keyword) opens no Studio at all, and the \`open\` step below self-skips for it.

**\`openStudioTo.js\` handles session resolution automatically.** It takes a single \`<path>\` argument and reads auth from your local config. The active Studio session is the single source of truth on disk:
1. If an active session is recorded, it navigates that window to the path and reuses it.
2. If none is recorded, it opens a **new** Studio window at the path.

It never opens a second window while a session is recorded: it either reuses it or gates. A clean tab close or a deliberate end clears the record, so the next open is simply a fresh window.

Output events:
- \`{"event":"navigated","sessionId":"...","path":"..."}\`, reused an existing session.
- \`{"event":"window-opened","url":"..."}\`, opened a fresh Studio window. Immediately surface the URL to the user in a normal chat message (for example, \`Studio opened: <url>\`) so it is copyable from the transcript; this is required even if the browser opened successfully.
- \`{"event":"started","sessionId":"..."}\`, opened a new Studio window.
- \`{"event":"monitor","sessionId":"...","eventFile":"..."}\`, the durable event stream path. Tail \`eventFile\` for the live in-session events (the daemon appends them there for the whole session, independent of any running command).
- \`{"event":"not-responding","sessionId":"..."}\`, a recorded session exists but the window did not respond (the navigation retries via ping-pong before reporting this, so the tab was pinged twice and never answered). **Every** Studio-opening command emits this on a stale session (\`openStudioTo.js\` and the dataset/experiment/trace-plan commands alike), and none of them opens a duplicate window. **This is a gate.** Recommend the user refresh or reopen the Studio tab in their browser, then {{askVerb}} with two options: **Try again** (re-run the command that gated, the record is still on disk, so a window that came back gets reused) or **Open a new Studio** (run \`{{command:clearStudio}}\` to drop the stale record, then re-run the command, which now opens a fresh window). Only clear the record after the user approves.
- \`{"event":"open-failed","sessionId":"...","reason":"..."}\`, failed to open a new Studio. Surface the error.

The gate fires only when a recorded window went unreachable with **no close signal**: a crash, sleep, or a tab close no process witnessed. A cleanly closed or deliberately ended session leaves no record, so the next open just opens fresh (no handshake, no prompt).

**Never use Playwright, \`open\`, \`chrome-testing\`, or any other browser automation to open Studio pages.** Always use \`openStudioTo.js\` which handles auth and session management.`,
            stepStyle: "list",
            steps: [
                {
                    id: "open",
                    kind: "action",
                    toolCalls: ["bash"],
                    commandCalls: ["openStudio"],
                    title: "Open the Studio",
                    body: `**In \`benchmark\` mode, first check the Studio opt-in flag** (set during argument routing when the \`studio\` keyword was passed). If benchmark did NOT opt in, skip this entire step without running any command and continue to the \`pick-dataset\` step (Phase 5 Setup). In all other modes, and in \`benchmark\` with the \`studio\` flag, proceed.

Open Studio at the initial path for this mode. \`openStudioTo.js\` is the single entry point for all Studio operations: it navigates an existing session or opens a new one automatically.

\`\`\`bash
{{command:openStudio}} <path>
\`\`\`

The command resolves this agent's active session on its own and reads auth from local config, no session id or credentials to pass.

**The Studio daemon is the durable event buffer, not this process.** \`openStudioTo.js\` opens or navigates the session and prints handshake JSONL, including a \`monitor\` line with the path to a durable event file. Studio-opening commands may stay alive until the user acts in Studio, so always launch them with the host's background / long-running process mechanism, read stdout incrementally until you capture the handshake, and never block the conversation foreground waiting for the user. Events are appended to the \`eventFile\` by the daemon for the whole session, whether or not any command is still running, so you can never miss Done / Edit-with-agent / session-ended. **Capture the \`eventFile\` path from this step's \`monitor\` line; the \`await-event\` step tails that file, not this process's output.** Every later \`{{command:openStudio}}\` call (dataset page, experiments page, trace plans) uses the same background/long-running process pattern against the same session and event file: you do not start a second monitor for them.

**The path MUST start with \`/studio\`.** Never pass \`/\`, a bare URL, or any path outside the \`/studio/\` route tree.

- **\`wizard\` mode:** pass \`/studio\`
- **\`dataset <key>\` mode:** pass \`/studio\` (Phase 3's "Open the dataset review page" step navigates to the chosen dataset's own page once the datasetId is held; there is no function-level dataset page)
- **\`experiment <key>\` mode:** pass \`/studio\`
- **\`fix [<key>] <trace-id>\` mode:** this mode normally skips the initial Studio open and starts at Phase Fix. If you are here because the user later chose to inspect the single-trace before/after or run the full dataset experiment, pass \`/studio\` and continue with the experiment viewer flow.
- **\`cost-optimize <key>\` mode:** pass \`/studio\`
- **\`investigate [<key>]\` mode:** pass \`/studio\`
- **\`benchmark <key>\` mode:** only when the run opted in with the \`studio\` keyword (the working-context flag from argument routing): pass \`/studio\`. Without the flag, benchmark is terminal-only: do NOT run \`openStudioTo.js\` at all, skip straight to the \`pick-dataset\` step (the step's \`next\` already routes there)

\`replay\` mode never reaches this step (it runs entirely in-chat with no Studio session), see Phase Replay.

{{#claude}}Run it with \`run_in_background: true\` on the Bash tool so you can read its handshake output without blocking. **Do NOT append \`&\` to the command string.**{{/claude}}{{#cursor}}Run it as a background process and capture its stdout.{{/cursor}}{{#codex}}Run it via your runtime's "long-running exec session" mechanism and capture its stdout.{{/codex}} On the daemon path it prints the handshake and exits immediately; in the rare no-daemon fallback it stays alive writing the event file itself. Either way you do the same thing: read the handshake, then tail the \`eventFile\` in \`await-event\`.

The script outputs these handshake JSON lines on stdout (see the Studio Lifecycle intro for the full event reference):

- \`{"event":"window-opened","url":"..."}\`, fresh Studio window opened. Immediately tell the user \`Studio opened: <url>\` in a normal chat message before continuing to poll.
- \`{"event":"started","sessionId":"..."}\`, new Studio opened. The session is written to disk; all subsequent \`openStudioTo.js\` and \`pushActivity.js\` calls resolve it automatically. You do not need to track the sessionId.
- \`{"event":"navigated","sessionId":"...","path":"..."}\`, navigated an existing session.
- \`{"event":"auth-required","sessionId":"..."}\`, user needs to sign in. Wait for \`authenticated\`.
- \`{"event":"authenticated","sessionId":"..."}\`, user signed in. Continue.
- \`{"event":"monitor","sessionId":"...","eventFile":"..."}\`, the durable event stream lives at \`eventFile\`. **Record this path**, the \`await-event\` step tails it for the live stream (Done / Edit-with-agent / session-ended).

Status messages go to stderr. Filter to JSON lines only. The live in-session events (Done, Edit-with-agent, session-ended) do NOT appear on this process's stdout, they go to the \`eventFile\`; tail it in \`await-event\`.

**Recovering after compaction:** Automatic. \`openStudioTo.js\` and \`pushActivity.js\` read the active-session file on disk.`,
                    next: {
                        byMode: {
                            wizard: "identify-function/list-or-arg",
                            dataset: "dataset/list-datasets",
                            experiment: "load-dataset/pick-dataset",
                            fix: "fix/resolve",
                            "cost-optimize": "load-dataset/pick-dataset",
                            investigate: "investigate/gather-context",
                            benchmark: "load-dataset/pick-dataset",
                            "add-trace": null,
                            replay: "quick-replay/setup",
                        },
                    },
                },
            ],
        },
        {
            id: "identify-function",
            title: "Phase 1: Identify the Trace Function",
            intro: "If a `traceFunctionKey` was provided as an argument, skip the listing and the user prompt, but still cross-check the provided key against the local codebase before moving on. Otherwise, work through all four steps below:",
            stepStyle: "list",
            steps: [
                {
                    id: "list-or-arg",
                    kind: "action",
                    toolCalls: ["listFunctions"],
                    title: "List trace functions",
                    emit: "Identifying trace function",
                    body: `**Skip this step if a \`traceFunctionKey\` argument was provided**: use the argument directly and continue to cross-check. Otherwise, call {{tool:listFunctions}} to list all available trace functions. Use **only** the keys and metadata returned (trace counts, last activity), do NOT invent or infer descriptions of what each function does from its key name. Key names are often ambiguous or misleading, and guessing produces hallucinated descriptions that confuse the user.`,
                    next: "identify-function/cross-check",
                },
                {
                    id: "cross-check",
                    kind: "action",
                    toolCalls: ["grep", "glob"],
                    title: "Cross-check against the local codebase",
                    body: `**Cross-check each key against the local codebase** before presenting. For each returned key, \`grep\` the repo for string-literal uses of that exact key (across \`*.ts\`, \`*.tsx\`, \`*.py\`, \`*.rb\`, \`*.go\`, \`*.baml\`). Mark each function in the presented list as:

- **✅ instrumented here**: found in this repo, with the file path
- **⚠️ not found in this repo**: traces exist on Bitfab but the key isn't in this codebase (likely another repo or a renamed key)`,
                    next: "identify-function/present-list",
                },
                {
                    id: "present-list",
                    kind: "action",
                    title: "Present the list",
                    body: `**Skip this step if a \`traceFunctionKey\` argument was provided**: there's no list to present. Otherwise, present the full list in the question text showing ONLY: \`<key>\` · \`<trace count>\` · \`<last activity>\` · \`<instrumented-here marker + path, or not-found marker>\`. No invented summaries.`,
                    next: "identify-function/ask-function",
                },
                {
                    id: "ask-function",
                    kind: "action",
                    toolCalls: ["ask"],
                    title: "Ask which function to improve",
                    body: `**Skip this step if a \`traceFunctionKey\` argument was provided**: the function is already chosen. Otherwise, {{askVerb}} with 2 options: the recommended function (prefer one that is ✅ instrumented here AND has recent activity) and a free-text "Type a function key" option. If nothing is instrumented here, say so explicitly in the question, don't hide it.`,
                    next: "verify-instrumentation/check-instrumentation",
                },
            ],
        },
        {
            id: "verify-instrumentation",
            title: "Phase 2: Verify Instrumentation & Replay",
            intro: "Check that this trace function has both instrumentation and a replay script.",
            stepStyle: "list",
            steps: [
                {
                    id: "check-instrumentation",
                    kind: "branch",
                    toolCalls: ["bash", "grep", "ask", "skill"],
                    title: "Check instrumentation",
                    emit: "Verifying instrumentation",
                    body: `Search the codebase for the trace function key to {{^codex}}find where the SDK is used{{/codex}}{{#codex}}confirm SDK usage{{/codex}}:

- TypeScript: \`grep -r "<traceFunctionKey>" --include="*.ts" --include="*.tsx"\`
- Python: \`grep -r "<traceFunctionKey>" --include="*.py"\`
- Ruby: \`grep -r "<traceFunctionKey>" --include="*.rb"\`
- Go: \`grep -r "<traceFunctionKey>" --include="*.go"\`

If the key is found, note the file location, this is the code you'll iterate on in later phases.

If the key is NOT found in the codebase, the function is instrumented elsewhere (the traces exist on Bitfab). {{^codex}}Use \`AskUserQuestion\` to ask:{{/codex}}{{#codex}}Ask:{{/codex}}

> "I can't find \`<traceFunctionKey>\` in this codebase, it may be instrumented in another repo or under a different key."
>
{{branches}}

If the user chooses **"Instrument now"**, tell the user to run \`{{cmd}}setup instrument\` first, then come back with \`{{cmd}}assistant wizard <key>\`. Do NOT invoke the setup skill from within this flow; it will break the assistant flow's continuity. If **"Continue anyway"**, skip the replay-script check and start building the dataset, there's no local code to iterate on yet.`,
                    branches: [
                        {
                            when: "key found in codebase",
                            next: "verify-instrumentation/check-replay-script",
                        },
                        {
                            option: {
                                letter: "A",
                                label: "Instrument now",
                                description: "set up tracing in this codebase",
                            },
                            recommended: true,
                            next: "cleanup/close-studio",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Continue anyway",
                                description: "work with the traces even without local code",
                            },
                            next: "dataset/list-datasets",
                        },
                        {
                            option: {
                                letter: "C",
                                label: "Pick a different function",
                            },
                            next: "identify-function/list-or-arg",
                        },
                        {
                            option: { letter: "D", label: "Stop" },
                            next: "cleanup/close-studio",
                        },
                    ],
                },
                {
                    id: "check-replay-script",
                    kind: "branch",
                    toolCalls: ["read", "glob", "ask", "skill"],
                    title: "Check replay script",
                    body: `Search for a replay script that covers this trace function:

- Look for files matching \`scripts/replay.*\`, \`scripts/*replay*\`, or any file that imports \`bitfab.replay\` / \`client.replay\`
- Read the script and check that it maps the target trace function key

If a replay script exists but targets a different function key, do NOT modify the existing script or suggest changing the code's function key. Instead, treat it as "no replay script for this function" and offer to create a new one.

If no replay script exists or it doesn't cover this function, {{askVerb}}:

> "No replay script found for \`<traceFunctionKey>\`."
>
{{branches}}

If the user chooses **"Create replay now"**, create the replay script inline: fetch the SDK replay reference (\`https://docs.bitfab.ai/reference/typescript.md\` or the equivalent for the project language) and the script template (\`https://docs.bitfab.ai/typescript-sdk.md\`), then write a new replay script following the template. The script must accept \`--limit N\`, \`--trace-ids\`, \`--name <name>\`, \`--code-change <path>\`, \`--experiment-group-id <uuid>\`, and \`--dataset-id <uuid>\` flags, and emit the full \`ReplayResult\` as JSON to stdout per the Replay Output Contract. Pass the SDK's ready-made progress reporter into the replay's progress callback (\`onProgress: reportReplayProgress\` in TS, \`on_progress=report_replay_progress\` in Python, \`on_progress: Bitfab.method(:report_replay_progress)\` in Ruby, the template already does this) so it streams \`@@bitfab:progress {json}\` lines that \`{{command:replayProgress}}\` turns into one relayable line per trace, and have the script print the full \`ReplayResult\` JSON to stdout so \`{{command:replayProgress}}\` can append it as a \`type: "complete"\` row in that run's \`events.jsonl\`. Do NOT invoke \`{{cmd}}setup replay\` as a separate skill. After creating the script, check its capabilities.

**Handler-instrumented keys (no decorated root function) are replayable too.** If the key is registered via a framework handler ({{keyedHandlerKeyRefs}}) rather than \`@span\`/\`withSpan\`, follow the docs' "Replaying handler-instrumented functions" section: pass the handler's key plus a plain callable to \`replay()\` (the SDK wraps it internally), re-invoking the framework entrypoint with a freshly constructed environment (safe no-op substitutes for billing callbacks and other side-effectful wiring). On SDKs that predate explicit-key replay, wrap the callable under the same key yourself. Never report a handler-instrumented function as not replayable.`,
                    branches: [
                        {
                            when: "replay script covers this function",
                            next: "verify-instrumentation/detect-replay-capabilities",
                        },
                        {
                            option: {
                                letter: "A",
                                label: "Create replay now",
                                description: "create the replay script inline",
                            },
                            recommended: true,
                            next: "verify-instrumentation/detect-replay-capabilities",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Pick a different function",
                            },
                            next: "identify-function/list-or-arg",
                        },
                        {
                            option: { letter: "C", label: "Stop" },
                            next: "cleanup/close-studio",
                        },
                    ],
                },
                {
                    id: "detect-replay-capabilities",
                    kind: "branch",
                    toolCalls: ["bash", "grep", "ask", "skill"],
                    title: "Detect replay script capabilities",
                    body: `**Detect replay script capabilities.** Check what the replay script supports. These flags determine how experiment results are tracked and displayed in Phase 5. **If you already ran this step for the same trace function earlier in this session, skip it and continue. Re-run if the user switched functions via "Pick a different function".**

**1. Use the replay script located in the previous step** (or grep for \`scripts/replay.*\` / files importing \`bitfab.replay\` / \`client.replay\`).

**2. Grep the replay script for the flags it forwards:**

| Grep the script for | Flag | What it enables |
|----------|------|-----------------|
| \`code-change\` or \`code_change\` | \`supportsCodeChanges\` | Code diffs attached to each experiment in the dashboard |
| \`experiment-group-id\` or \`experiment_group_id\` | \`supportsExperimentGroups\` | Live streaming of results in Studio as replay runs |
| \`dataset-id\` or \`dataset_id\` | \`supportsDatasetId\` | Durable attribution of the experiment to its dataset (shows under the dataset's experiments) |
| \`--name\` plus \`name\` / \`name:\` forwarded to \`replay()\` | \`supportsExperimentNames\` | Human-readable experiment/test-run names in the UI |
| \`traceId\` or \`trace_id\` in the output/print section | \`supportsReplayTraceIds\` (re-confirmed post-replay in \`check-trace-id-support\`) | Verdict persistence, cross-iteration comparison, Studio experiments page |

\`supportsInputAdapters\` is **not** a script-grep flag (the script gains an \`adaptInputs\` / \`adapt_inputs\` argument only after a signature actually drifts, in \`adapt-replay-inputs\`). It comes solely from the installed SDK in step 3.

**3. Confirm the installed SDK supports each flag.** A flag the script forwards is silently ignored when the installed SDK predates it, so each flag is gated on the SDK too. Run the capability probe (it resolves the installed SDK version from the lockfile/manifest and gates every capability by version, with no dist-file grepping across package-manager layouts):

\`\`\`bash
cd <project-dir> && {{command:detectCapabilities}}
\`\`\`

Read the \`<bitfab-replay-capabilities>\` block. Each line is a JSON object for one detected SDK with \`language\`, \`workspacePath\`, \`current\` (resolved version), \`versionResolved\`, \`updateAvailable\`, \`latest\`, and a \`capabilities\` object holding \`supportsExperimentGroups\`, \`supportsDatasetId\`, \`supportsCodeChanges\`, \`supportsReplayTraceIds\`, \`supportsInputAdapters\`, \`supportsExperimentNames\`. Pick the line whose \`language\` (and \`workspacePath\`, in a monorepo) matches the replay script's project.

- **Combine the two sources:** a flag is true only when the script forwards it (step 2) **and** that SDK's matching \`capabilities.*\` is true. Take \`supportsInputAdapters\` straight from \`capabilities.supportsInputAdapters\` (it has no script side).
- \`supportsReplayTraceIds\` from the probe is a definitive **pre-replay** signal; the later \`check-trace-id-support\` step still re-confirms from the actual replay output.
- If \`versionResolved\` is \`false\`, the probe couldn't pin the installed version, so every capability defaulted false and is **unverified**. Check that one SDK by hand before relying on the flags (TypeScript: grep \`node_modules/@bitfab/sdk/dist/index.d.ts\` for the option names and \`ReplayItem.traceId\`; Python: the installed \`bitfab/replay.py\`; Ruby: the installed gem's \`replay.rb\`), or resolve the version and re-run.

If the script has a flag but the SDK's \`capabilities.*\` is false, mark that flag **false**. Prioritize upgrading the SDK over using fallbacks: without replay trace IDs, verdict labels can't be persisted (benchmark/experiment results stay in-agent only).

**4. Route on the result.**

If all flags are true, skip the question and continue silently.

If one or more flags are false, tell the user which capabilities are missing and what they affect, then {{askVerb}}. List the missing capabilities in the question text:

> "Your replay script is missing support for:
>
> [if !supportsCodeChanges] **Code changes**: edits won't appear in the experiment dashboard
> [if !supportsExperimentGroups] **Experiment groups**: no live streaming; results appear in Studio after each run
> [if !supportsDatasetId] **Dataset attribution**: the experiment won't be durably linked to its dataset (still findable via the trace-lineage fallback; fixed by regenerating the script / upgrading the SDK)
> [if !supportsExperimentNames] **Experiment names**: runs will show as generated IDs instead of readable names
> [if !supportsReplayTraceIds] **Replay trace IDs**: experiment results can't be persisted or compared across iterations (your SDK needs an upgrade)
>
> [if !supportsInputAdapters] **Input adapters**: replay can't recover traces when the function's signature drifts after capture (fixed by upgrading the SDK)"

{{branches}}`,
                    branches: [
                        {
                            when: "all flags are true",
                            description: "full capability. Hold the flags in working context and continue silently",
                            next: {
                                byMode: {
                                    wizard: "dataset/list-datasets",
                                    dataset: "dataset/list-datasets",
                                    experiment: "iterate/make-change",
                                    fix: "iterate/make-change",
                                    "cost-optimize": null,
                                    investigate: "dataset/list-datasets",
                                    benchmark: "iterate/replay-against-dataset",
                                    "add-trace": null,
                                    replay: "cleanup/close-studio",
                                },
                            },
                        },
                        {
                            option: {
                                letter: "A",
                                label: "Upgrade the replay script",
                                description: "regenerate the script with full support, then continue",
                            },
                            recommended: true,
                            next: "verify-instrumentation/upgrade-replay-script",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Continue without",
                                description: "run experiments with the current script; missing features are skipped",
                            },
                            next: {
                                byMode: {
                                    wizard: "dataset/list-datasets",
                                    dataset: "dataset/list-datasets",
                                    experiment: "iterate/make-change",
                                    fix: "iterate/make-change",
                                    "cost-optimize": null,
                                    investigate: "dataset/list-datasets",
                                    benchmark: "iterate/replay-against-dataset",
                                    "add-trace": null,
                                    replay: "cleanup/close-studio",
                                },
                            },
                        },
                    ],
                },
                {
                    id: "upgrade-replay-script",
                    kind: "action",
                    toolCalls: ["skill", "bash", "grep"],
                    title: "Upgrade SDK and replay script",
                    body: `**Upgrade the SDK and replay script.** The replay script references SDK APIs (\`name\`, \`experimentGroupId\`, \`codeChangeDescription\`, per-item \`traceId\`, \`adaptInputs\` / \`adapt_inputs\`) that require a recent SDK. Upgrade the SDK first, then regenerate the script.

**1. Upgrade the SDK.** Run the capability probe to read the installed version and update status (skip if you still have its block from \`detect-replay-capabilities\`):

\`\`\`bash
cd <project-dir> && {{command:detectCapabilities}}
\`\`\`

For the SDK matching this project, the \`<bitfab-replay-capabilities>\` block reports \`current\` (resolved version), \`latest\`, \`updateAvailable\`, and \`renameFrom\`. If \`updateAvailable\` is false, the SDK is already current, skip to step 2. Otherwise run the package manager's update command:
- TypeScript: \`pnpm update @bitfab/sdk\` (in monorepos, scope with \`--filter <pkg>\`). **If \`package.json\` pins an exact version (e.g. \`"@bitfab/sdk": "0.13.4"\` with no \`^\`/\`~\`), \`pnpm update\` will NOT move past the pin, bump the spec in \`package.json\` to the reported \`latest\` first (e.g. \`"@bitfab/sdk": "0.13.6"\`), then \`pnpm install\`.**
- Python: \`uv lock --upgrade-package bitfab-py && uv sync\` or \`poetry update bitfab-py\`
- Ruby: \`bundle update bitfab --conservative\`

If \`renameFrom\` is set (the SDK is on the legacy \`bitfab\` package instead of \`@bitfab/sdk\`), remove the old package and install \`@bitfab/sdk\`.

**2. Regenerate the replay script.** Locate the replay script for this trace function (found in \`detect-replay-capabilities\`). Fetch the SDK replay reference (\`https://docs.bitfab.ai/reference/typescript.md\` or the equivalent for the project language) and the script template (\`https://docs.bitfab.ai/typescript-sdk.md\`). Then edit the script to add the missing flags:
- **\`--code-change <path>\`**: parse the JSON file, pass \`codeChangeDescription\` and \`codeChangeFiles\` to \`replay()\`
- **\`--experiment-group-id <uuid>\`**: pass \`experimentGroupId\` to \`replay()\`
- **\`--name <name>\`**: pass \`name\` to \`replay()\` so the resulting experiment/test run has a readable title
- **\`--dataset-id <uuid>\`**: pass \`datasetId\` to \`replay()\`. This is the **preferred way to replay a dataset**: passed alone (no \`--trace-ids\`) the server replays exactly the dataset's traces and durably attributes the experiment to the dataset. Adding this flag is what lets the replay step drop the hand-enumerated \`--trace-ids\` list.
- **Replay Output Contract**: emit the full \`ReplayResult\` as one \`JSON.stringify(result, null, 2)\` block to stdout (including every item's \`traceId\`, \`durationMs\`, \`tokens\`, \`model\`). Human-readable summary goes to stderr.
Do NOT invoke \`{{cmd}}setup replay\` as a separate skill; edit the script inline here.

**3. Re-check capabilities.** After upgrading and editing, re-run \`{{command:detectCapabilities}}\` and re-read the \`capabilities\` object for this SDK (the probe now sees the upgraded version). Combine again with the script-side grep from step 2 and update the flags in working context. If any are still missing after the upgrade, note it but continue.`,
                    next: {
                        byMode: {
                            wizard: "dataset/list-datasets",
                            dataset: "dataset/list-datasets",
                            experiment: "iterate/make-change",
                            fix: "iterate/make-change",
                            "cost-optimize": null,
                            investigate: "dataset/list-datasets",
                            benchmark: "iterate/replay-against-dataset",
                            "add-trace": null,
                            replay: "cleanup/close-studio",
                        },
                    },
                },
            ],
        },
        {
            id: "investigate",
            title: "Phase Investigate: Free-form Investigation",
            intro: `Reached only from \`investigate\` mode. The user is describing an issue they want to understand (a customer complaint, a suspected failure pattern, a regression, or an open-ended "is something off with this function" question). Read traces and code as needed to characterize the problem, then hand the user a choice: stop with the in-chat summary, write a markdown analysis report, or roll into building a labeled dataset (Phase 3).`,
            stepStyle: "list",
            steps: [
                {
                    id: "gather-context",
                    kind: "action",
                    toolCalls: ["listFunctions", "grep", "glob", "ask"],
                    title: "Gather context",
                    body: `Read what the user typed when they invoked \`{{cmd}}assistant investigate\`. Two cases:

- **They passed a function key as the argument:** use it. Call {{tool:listFunctions}} once to confirm the key exists and capture trace count + last activity for the explore step. Then grep the codebase for the key (\`grep -r "<key>" --include="*.ts" --include="*.tsx" --include="*.py" --include="*.rb" --include="*.go" --include="*.baml"\`) and note the file path. Hold both in working context.
- **They didn't pass a key:** read their description (failure pattern, customer complaint, "something seems off with X", etc.). First inspect the local instrumentation to infer likely function keys: grep for Bitfab SDK usage and wrappers (\`@bitfab/sdk\`, \`withSpan\`, \`getFunction\`, \`traceable\`, \`observability/providers/bitfab\`, replay scripts) plus domain terms from the user's description. If one key is clearly tied to the described workflow, hold it as the candidate and grep for its exact string. Then call {{tool:listFunctions}} to confirm whether the candidate has traces and to capture trace count + last activity. If code does not reveal a candidate, or multiple candidates remain plausible, use \`list_trace_functions\` as a fallback picker (recommend 2-4 alternatives by key, trace count, last activity, and code path when known). If nothing matches, ask the user to clarify or pass a key explicitly.

Do NOT invent or infer descriptions of what each function does from its key name. Use only what {{tool:listFunctions}} returns plus what's in the codebase.`,
                    next: "investigate/explore",
                },
                {
                    id: "explore",
                    kind: "action",
                    toolCalls: [
                        "listFunctions",
                        "searchTraces",
                        "readTraces",
                        "grep",
                        "read",
                    ],
                    title: "Explore the issue",
                    body: `Free-form investigation: use whatever combination of MCP and local tools fits the user's described concern, subject to the Trace-first debugging rule above. Typical moves:

- **Trace evidence:** call {{tool:searchTraces}} with filters that match the user's description (failure shape, recency, label state, user / session if mentioned), then {{tool:readTraces}} with \`scope: "summary"\` or \`scope: "full"\` on the most informative ones.
- **Code context:** read the instrumented function and its call chain. If BAML files, related prompts, or upstream / downstream functions matter to the question, read those too.
- **Quantify if useful:** if the user asked something like "how often does X happen", run targeted {{tool:searchTraces}} calls with different filters to count.

Stop exploring once you can give the user a clear, evidence-backed account: what's going wrong (or "nothing obvious is going wrong"), when, how often, what the failure shape is, what code path is implicated, and one or two leading hypotheses. Hold the findings in working context for the next step. Cite specific trace IDs and code locations rather than vague summaries. If you could not read a relevant trace, cite the function key and search filters you tried and mark the diagnosis as code-only / lower confidence.`,
                    next: "investigate/present-and-branch",
                },
                {
                    id: "present-and-branch",
                    kind: "branch",
                    toolCalls: ["ask"],
                    title: "Present findings and pick a next step",
                    body: `Share the findings inline with the user first, in chat, structured roughly as:

> **What I looked at:** \`<traceFunctionKey>\` · \`<N traces examined>\` · \`<filter criteria used>\`
>
> **What I found:**
>
> - [Finding with cited trace IDs / code locations]
> - [Finding with cited trace IDs / code locations]
>
> **Leading hypotheses:**
>
> - [Hypothesis, what would confirm it]

Then {{askVerb}} for the next step. Recommend based on what the investigation surfaced: option C (dataset) if the findings include reproducible failures worth labeling and iterating on, option B (report) if the user will need to share or revisit the findings later, option A (stop) if the question was a one-off and the chat summary already answers it.

{{branches}}

Options A and B end at the cleanup step, which closes Studio. Option C continues through dataset building, diagnosis, and experiments, with Studio staying open throughout until cleanup at wrap-up.`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Stop here",
                                description: "the in-chat summary is enough; no further artifact",
                            },
                            next: "cleanup/close-studio",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Write an analysis report",
                                description: "save the findings to a markdown file I can share or revisit later",
                            },
                            next: "investigate/write-report",
                        },
                        {
                            option: {
                                letter: "C",
                                label: "Build a labeled dataset",
                                description: "use these traces as seed candidates and label them so we can iterate against them later",
                            },
                            recommended: true,
                            next: "dataset/list-datasets",
                        },
                    ],
                },
                {
                    id: "write-report",
                    kind: "action",
                    toolCalls: ["write"],
                    title: "Write the analysis report",
                    body: `Write a markdown report capturing the investigation. Path: \`.bitfab/analysis/<traceFunctionKey>-<YYYY-MM-DD-HHmm>.md\` (create the \`.bitfab/analysis/\` directory if missing; fall back to a path under the repo root or \`os.tmpdir()\` if the project root isn't writable). Use the {{tool:write}} tool with this structure:

\`\`\`markdown
# Investigation: <traceFunctionKey>

**Date:** <YYYY-MM-DD>
**Question / concern:** <one-paragraph recap of what the user asked>

## What I looked at

<filters used, trace counts, time window>

## Findings

<bulleted findings, each citing trace IDs and code locations>

## Leading hypotheses

<bulleted, each paired with what would confirm or refute it>

## Recommended next steps

<concrete actions: build a dataset around hypothesis X, instrument span Y, ship a code fix for Z, etc.>
\`\`\`

After writing, tell the user the file path so they can open or share it, then stop (the cleanup step closes Studio). Do NOT roll into dataset building automatically; that is option C, not option B.`,
                    next: "cleanup/close-studio",
                },
            ],
        },
        {
            id: "fix",
            title: "Phase Fix: Capture the Failing Scenario",
            intro: `Reached only from \`fix\` mode. The user has a specific bug, a failing trace, and wants it fixed and saved in a dataset. This phase resolves THIS trace and its function and diagnoses the failure (deciding the acceptance criterion), then tails into the Phase 5 loop, where the fix itself is made and replayed against only the target trace first. **Nothing is added to a dataset here:** adding it to a dataset happens later (in \`fix-add-to-dataset\`), only after the fix proves the target trace green. The single-trace replay is still tagged with an experiment group so Studio can later show the before/after if the user asks, but Studio stays closed during this initial pass.`,
            stepStyle: "list",
            steps: [
                {
                    id: "resolve",
                    kind: "action",
                    toolCalls: [
                        "bash",
                        "grep",
                        "read",
                        "searchTraces",
                        "readTraces",
                        "ask",
                    ],
                    title: "Resolve the failing trace and its function",
                    emit: "Resolving the bug",
                    body: `**Resolve what to fix.** The user invoked \`{{cmd}}assistant fix [<key>] <trace-id>\` or phrased it in natural language ("fix this bug", "fix trace abc123", "this trace is wrong, make it pass"). A UUID is the **trace ID**; a non-UUID slug is the function key.

1. **Identify the trace ID.** **First, sanity-check the anchor:** if the request is an ordinary code bug with no Bitfab trace involved (nothing points at a traced function, no trace ID, no captured failure), this is the wrong skill, say so in one line and stop. Do not hunt for or invent a trace to justify entering, and do not treat a generic "fix this" as a trace. It's the UUID in the message (or the trace the conversation is about). If there is no UUID but the user gave a **smoking-gun description** ("find and fix the trace where <bad behavior happened>", "search <terms> and fix", "the trace where Peek remembered a random config value"), resolve that description into a target trace before asking: inspect the local instrumentation and repo terminology to infer the likely function key even if the user did not name one, call {{tool:searchTraces}} with the descriptive terms and any inferred/named function key / recency / user / session anchors from the request, read the best candidates with {{tool:readTraces}} (\`scope: "summary"\` first, then \`"full"\` for the chosen trace), and pick the single trace whose output matches the described bad behavior. If multiple candidates match, present the trace IDs and one-line output summaries and {{askVerb}} which one to fix. If none match, say what filters you searched and {{askVerb}} for a trace ID or narrower search terms. If the user explicitly invoked \`{{cmd}}assistant fix\` but gave neither a trace ID nor a descriptive trace anchor, {{askVerb}} which trace and wait, do not guess.
2. **Read the trace.** Call {{tool:readTraces}} with the trace ID and \`scope: "full"\`. This is the failing scenario: hold its inputs, output, any existing label/annotation, and its \`traceFunctionKey\` in working context. **If the user gave only a trace ID and no function key, take the key from the trace itself**, don't ask.
3. **Locate the code.** Grep the codebase for the function key (\`grep -r "<key>" --include="*.ts" --include="*.tsx" --include="*.py" --include="*.rb" --include="*.go" --include="*.baml"\`) and note the file path. This is the code you'll fix in \`make-change\`.

4. **Diagnose the failure (no dataset yet).** From the trace + code you just read, decide the failure verdict and write a one-or-two-sentence \`annotation\` describing the bug: what the output got wrong and what correct behavior looks like. This annotation is the acceptance criterion the first replay will check. **Do not label the trace and do not touch any dataset yet.** Adding the trace to a dataset happens only after the fix proves it green (in \`fix-add-to-dataset\`), so nothing is added to a dataset until the fix has been tested.

   **Confirm why the trace is wrong before editing.** If the trace already has a validated failing label/annotation, or the user already stated what is wrong in this conversation, use that as the starting acceptance criterion and say so. If the trace + code make the defect obvious (for example, an error span, malformed output, wrong tool args, or a clear mismatch between requested and returned content), state the specific trace evidence in one line and proceed. If you cannot confidently explain **why** the original trace is wrong, {{askVerb}} with a short confirmation question: what is wrong, and what correct behavior should be? Then wait. Do not make a speculative code change, do not replay, and do not later call {{tool:setHumanLabels}} until the acceptance criterion is concrete.

   **If you can't find a real defect** (you read the trace and the code and the output actually looks correct for the inputs), do **not** invent a fix. {{askVerb}} with a short question asking what specifically is wrong with this trace, the output they expected versus what they got, then wait. Only proceed once you have a concrete defect to target; if there genuinely isn't one, say the trace looks correct in one line and stop rather than editing code to "fix" a non-bug.
5. **Set the replay scope.** Hold \`fixReplayScope = "single-trace"\` and \`fixSkipMakeChange = false\` in working context (initialize both now so later steps never read \`fixSkipMakeChange\` unset). The first replay uses only this trace ID, not a dataset, and Studio stays closed.

Hold the trace ID, function key, code path, the failure annotation, and the trace's failure details in working context. Tell the user in one line what you'll do, and **the first time you mention "replay" to the user, explain it in one plain-English clause**: a replay re-runs the traced function on this trace's exact recorded inputs against your current code, so you can see whether your change fixes the captured output, without making a real production call. For example: "I'll fix the code and replay trace \`<traceId>\` (re-run it on its recorded inputs against the new code) to confirm the fix, then add it to a dataset once it passes." Then continue into the replay loop, where \`make-change\` is the fix.`,
                    next: "load-dataset/pick-execution-mode",
                },
            ],
        },
        {
            id: "add-trace",
            title: "Phase Add: Attach a Trace to a Dataset",
            intro: `Reached only from \`add-trace\` mode. This is the lightweight path: attach one or more existing traces to a dataset (picking or creating one), then stop. No labeling, no diagnosis, no experiments, and **no Studio**: this mode never opens the Studio browser surface and runs no Studio commands (\`openStudioTo.js\` / \`closeStudio.js\`). It does still run the \`readTracesBatched\` CLI command to resolve trace function keys. The traces attach **raw**; the user labels and approves them later wherever they review datasets.`,
            stepStyle: "list",
            steps: [
                {
                    id: "resolve-context",
                    kind: "action",
                    toolCalls: ["bash", "read", "ask"],
                    commandCalls: ["readTracesBatched"],
                    title: "Resolve the trace(s) and function key",
                    body: `**Resolve what to attach and where.** The user invoked \`{{cmd}}assistant add-trace\`, either with the signature \`add-trace [<key>] <trace-id...> [<dataset-id>]\` (the leading \`<key>\` is optional) or in natural language (e.g. "add trace abc123 to a dataset"). **Tokens are typed, not positional:** a UUID is always a trace ID (or, only when the user explicitly calls it the dataset, the \`<dataset-id>\`); a non-UUID slug is the function key. So \`add-trace <trace-uuid>\` is a trace-only invocation, never treat a bare UUID as a function key, and never conclude "no trace IDs were given" just because no slug preceded them. Parse out three things:

- **Trace IDs**: one or more. Trace IDs are UUIDs. If you can't identify at least one, {{askVerb}} for the trace ID(s) and wait, do not guess.
- **Trace function key**: the non-UUID slug, if the user gave one. Datasets are scoped per function, so the key is required and **all trace IDs in the batch must belong to the same function** ({{tool:addTracesToDataset}} silently skips IDs whose function key doesn't match the dataset, so a mixed batch would partially fail with no obvious error). Resolve and verify the key up front:
  - Run \`{{command:readTracesBatched}} <trace-id...> --scope summary\` **once** with **every** trace ID in the batch (not just the first). It fans the reads out in parallel batches of 10 and writes the combined result to a temp file; the command prints \`{"status":"ok","outputFile":"..."}\` as JSON, so \`Read\` that \`outputFile\` and pull each trace's \`traceFunctionKey\`. **Use this command here, not the \`read_traces\` MCP tool directly:** \`read_traces\` caps at 10 IDs, so calling it for this batch would re-introduce the serial per-batch fan-out \`readTracesBatched\` exists to replace.
  - **If all traces share one key:** use it. (If the user also supplied a \`<key>\` and it disagrees with what the traces report, trust the traces' key and note the discrepancy in one line.)
  - **If the traces span more than one function key:** stop and {{askVerb}}, list the distinct keys and which trace IDs map to each, and ask the user which single function's traces to attach (the others are dropped this run, since one \`add-trace\` invocation targets one function's dataset). Do not attach a mixed batch.
- **Dataset ID**: optional. If the user named a specific dataset (a UUID they identify as the dataset, not a trace), hold it for the next step.

Hold the (single, verified) function key, the trace IDs that belong to it, and any dataset ID in working context. Do **not** grep the codebase, this mode never touches local code.`,
                    next: "add-trace/pick-dataset",
                },
                {
                    id: "pick-dataset",
                    kind: "branch",
                    toolCalls: ["listDatasets", "createDataset", "ask"],
                    title: "Pick or create a dataset",
                    body: `**Pick or create the dataset to attach to.** If no dataset ID was supplied, call {{tool:listDatasets}} with the function key first. Hold the chosen \`datasetId\` in working context.

{{whens}}`,
                    branches: [
                        {
                            when: "a dataset ID was supplied in the invocation",
                            description: "use it directly, but first confirm it's scoped to the verified function key, call {{tool:listDatasets}} with the key and check the supplied id is in the result. If it is, continue to attach. If it isn't (the dataset belongs to a different function, so {{tool:addTracesToDataset}} would skip every trace), stop and {{askVerb}}, name the mismatch and offer to pick a correctly-scoped dataset instead",
                            next: "add-trace/attach",
                        },
                        {
                            when: "no datasets exist for this function (`list_datasets` returned empty)",
                            description: `**don't ask**: silently call {{tool:createDataset}} with \`traceFunctionKey: <key>\` and \`name: <key>\`. Hold the returned \`datasetId\` and continue`,
                            next: "add-trace/attach",
                        },
                        {
                            when: "one or more datasets already exist",
                            description: `present them {{presentMode}}, one option per existing dataset (name · id · current trace count) plus a "Create new" option. Recommend the most recently used dataset that has traces. On an existing pick, hold its id. On "Create new", silently call {{tool:createDataset}} with \`name: "<key> #N"\` (N one more than the existing count), don't ask for a name. Hold the id and continue`,
                            next: "add-trace/attach",
                        },
                    ],
                },
                {
                    id: "attach",
                    kind: "action",
                    toolCalls: ["addTracesToDataset"],
                    title: "Attach the trace(s) and stop",
                    body: `**Attach raw, then stop.** Call {{tool:addTracesToDataset}} once with the \`datasetId\` from the previous step and the full array of trace IDs. The call is idempotent, so re-attaching IDs already in the dataset is a safe no-op. Do **not** call {{tool:updateAgentLabels}}, this mode attaches traces without labels.

**Report the tool's actual result, not the input count.** {{tool:addTracesToDataset}} returns how many traces were added vs skipped (IDs not in the org, or not on the dataset's function, are silently skipped). Read those counts from the response and report the **added** count, not \`N = trace-IDs-you-passed\`. If any were skipped, say so in the same line (e.g. "2 added, 1 skipped, not on this function").

Then tell the user in one line, identifying the dataset by its \`datasetId\` (always known) and including its name only when you have one: "Added \`<added>\` trace(s) to dataset \`<datasetId>\`\`<skipped-note>\`." (When a name is available, from \`list_datasets\`, or the \`<key>\`/\`<key> #N\` you just created it with, use "Added \`<added>\` trace(s) to dataset \`<name>\` (\`<datasetId>\`)." On the supplied-dataset-ID path you called \`list_datasets\` to validate scope, so its name is available too.) This is the end of the lightweight \`add-trace\` flow: no labeling, no diagnosis, no experiments, and **no Studio**. Do not open a browser or run any Studio command (\`openStudioTo.js\` / \`closeStudio.js\`).`,
                    next: null,
                },
            ],
        },
        {
            id: "dataset",
            title: "Phase 3: Pick a Dataset and Label Traces",
            intro: `A **dataset** is the named bucket of labeled traces an experiment replays against. This phase picks (or creates) one for the trace function, labels candidate traces, attaches them to the dataset, then hands off to the per-dataset review page where the user approves labels and can ask the agent to add or remove traces.

In \`dataset\` mode this phase is the entry point, Phase 1 (function picker) and Phase 2 (instrumentation/replay verification) are skipped, so the trace function key comes from the argument. Before calling any MCP tools, grep the codebase for the key (e.g. \`grep -r "<traceFunctionKey>" --include="*.ts" --include="*.tsx" --include="*.py" --include="*.rb" --include="*.go" --include="*.baml"\`) and note the file path, every later step ("Label them yourself", and Phase 4 "Read the code" in \`wizard\` mode) needs it.`,
            stepStyle: "list",
            steps: [
                {
                    id: "list-datasets",
                    kind: "branch",
                    toolCalls: ["listDatasets", "createDataset", "ask"],
                    title: "Pick or create a dataset",
                    emit: "Building dataset",
                    body: `**Pick or create a dataset**: Call {{tool:listDatasets}} with the trace function key. Then branch on whether any exist. Hold the chosen \`datasetId\` in working context, every step from here on uses it.

{{whens}}`,
                    branches: [
                        {
                            when: "no datasets exist for this function (list_datasets returned empty)",
                            description: `**don't ask**: silently call {{tool:createDataset}} with \`traceFunctionKey: <key>\` and \`name: <key>\` (just the trace function key as the name; the user can rename it later in the UI if they want). Hold the returned \`datasetId\` and continue. The first-time user shouldn't have to answer a name prompt before they've even seen the dataset.`,
                            next: "dataset/open-page",
                        },
                        {
                            when: "one or more datasets already exist",
                            description: `present them to the user {{presentMode}}, with one option per existing dataset (name · id · current trace count) plus a "Create new" option. Recommend the most recently used dataset that has traces. If the user picks an existing dataset, hold its id and continue. If the user picks "Create new", silently call {{tool:createDataset}} with \`name: "<key> #N"\` where N is one more than the number of existing datasets (e.g. \`eval-assistant #2\`), don't ask for a name. Hold the new id and continue.`,
                            next: "dataset/open-page",
                        },
                    ],
                },
                {
                    id: "open-page",
                    kind: "branch",
                    toolCalls: ["bash", "searchTraces"],
                    commandCalls: ["openStudio"],
                    title: "Open the dataset review page",
                    emit: "Reviewing dataset",
                    body: `Open the dataset review page for the user **immediately** after picking or creating the dataset.

**First, derive the function's current input shape** so the page can flag traces that won't replay against today's code (the dataset rows and trace detail show a "Can't replay" badge when a trace's recorded inputs no longer fit the current signature). Find the function registered under \`<functionKey>\` in the codebase (the value passed to \`getFunction(...)\` / the traced function), read its parameters, and build a compact JSON shape:

\`\`\`json
{"fields":[{"name":"query","type":"string"},{"name":"limit","type":"number","required":false}]}
\`\`\`

- \`name\`: each top-level input field, for a single object argument, its keys; for positional params, the parameter names.
- \`type\` (optional): one of \`string\` / \`number\` / \`boolean\` / \`object\` / \`array\` / \`null\` / \`unknown\`. Omit if unsure.
- \`required\` (optional): defaults to true; set \`false\` for optional params.

This is best-effort. If you can't confidently determine the shape (no clear signature, dynamic args), **skip it** and open the bare path, the page falls back to flagging only traces that captured no inputs. Never block or ask the user about this.

Then base64-encode the shape and pass it as a \`?shape=\` query param (no shape -> open the bare path):

\`\`\`bash
SHAPE=$(printf %s '{"fields":[{"name":"query","type":"string"}]}' | base64 | tr -d '\\n')
{{command:openStudio}} "/studio/trace-functions/<functionKey>/datasets/<datasetId>?shape=$SHAPE"
\`\`\`

The command navigates an existing session or opens a new one automatically.

**This navigation is mandatory even though Studio is already open.** The initial mode open lands on \`/studio\` (the home), not this dataset's page. Labels and traces stream live only into the per-dataset page above, so a Studio session sitting anywhere else does NOT satisfy this step. Always navigate with the \`<datasetId>\` path once you hold the id.

**After opening, check whether the dataset already has traces.** Call {{tool:searchTraces}} with \`traceFunctionKey: <key>\`, \`datasetId: <datasetId>\`, \`limit: 1\` to see if the dataset is populated.

{{whens}}`,
                    branches: [
                        {
                            when: "the dataset already has traces (search returned results)",
                            description: `The dataset is not empty. Tell the user the dataset page is open with the existing traces, and they can review, approve, or edit labels there. Then go straight to waiting for their review. Do NOT ask how to source new candidates or offer to find more traces. The user should review what's already in the dataset first; they can request more traces via the "Edit with agent" button if needed.`,
                            next: "dataset/await-event",
                        },
                        {
                            when: "the dataset is empty (search returned no results)",
                            description: `The dataset has no traces yet. Tell the user the dataset page is open in a "waiting for traces" state, and that traces will appear there live as you search and add them. Then proceed to find candidate traces.`,
                            next: "dataset/ask-search-mode",
                        },
                    ],
                },
                {
                    id: "ask-search-mode",
                    kind: "branch",
                    toolCalls: ["searchTraces", "ask"],
                    title: "Ask how to source candidate traces",
                    body: `**Ask how to source candidate traces.** Before searching, decide *where* the candidate traces come from. Three real options:

1. **Define new criteria**: agent searches unlabeled traces shaped by what the user wants to surface. Best when the user has a hypothesis or a specific failure pattern in mind.
2. **Reuse existing labels for this function**: pull traces that already have a validated human or approved-agent label (from any prior dataset on this function) and seed the new dataset with them. Best when the user wants to hill-climb off prior labeling work, same labels, different cut, add more later.
3. **Open / you decide**: agent samples broadly with no hypothesis, ignoring prior labels for the search shape. Best for discovery passes.

**Probe for prior label volume first** so the recommendation is grounded. Call {{tool:searchTraces}} with \`traceFunctionKey: <key>\`, \`validated: true\`, \`limit: 50\` to see roughly how many validated labels already exist for this function. Note the count, you'll need it for the recommendation and for option 2.

Then {{askVerb}} with the three options below. Recommend:
- Option **2 (Reuse)** if the function has 5+ validated labels AND the picked dataset is freshly created or empty (the user is starting a new cut and prior work is the right baseline)
- Option **1 (Define)** if the user has a hypothesis or the function has < 5 validated labels (not enough prior signal to reuse)
- Option **3 (Open)** if the user explicitly says they don't have a hypothesis yet and there's not much prior labeling

Hold the chosen mode in working context, the next steps branch on it.

{{branches}}`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Define new criteria",
                                description: "tell me what to find (failure pattern, customer reports, etc.) and I search unlabeled traces",
                            },
                            next: "dataset/ask-search-intent",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Reuse existing labels for this function",
                                description: "seed the dataset with traces that already have validated labels, then optionally add more",
                            },
                            recommended: true,
                            next: "dataset/seed-from-existing-labels",
                        },
                        {
                            option: {
                                letter: "C",
                                label: "Open, you decide",
                                description: "broad sample with no hypothesis; ignore prior labels for the search shape",
                            },
                            next: "dataset/find-unlabeled",
                        },
                    ],
                },
                {
                    id: "seed-from-existing-labels",
                    kind: "branch",
                    toolCalls: [
                        "searchTraces",
                        "readTraceLabels",
                        "addTracesToDataset",
                        "ask",
                    ],
                    title: "Seed dataset from existing validated labels",
                    body: `**Seed dataset from existing validated labels.** Reachable only when the user picked Option B in \`ask-search-mode\`. Pull traces that already have a validated label (human-authored, or agent-authored and human-approved) for this function, attach them to the picked dataset, and route on whether the user also wants to add more.

1. Call {{tool:searchTraces}} with \`traceFunctionKey: <key>\`, \`validated: true\`, and a generous \`limit\` (50 is the cap). Both \`labelResult: true\` and \`labelResult: false\` matter, failures are the hill-climbing signal, but passes anchor the regression boundary. If 50 isn't enough to cover the function's labeled history, run a second call with \`labelResult: false\` only to bias toward fails first, then a third with \`labelResult: true\`. De-dupe trace IDs across calls.
2. Call {{tool:readTraceLabels}} on the resulting trace IDs so the labels + annotations are in working context. Don't re-label them, these are already validated. {{tool:readTraceLabels}} returns only label + annotation + approved (no span content) and takes up to 100 IDs per call, so the deduped set from step 1 is usually a single call; if it deduped more than 100 IDs, split them into chunks of 100 and call {{tool:readTraceLabels}} once per chunk (sequential is fine, the responses are small).
3. Call {{tool:addTracesToDataset}} once with \`datasetId\` (the one picked in \`list-datasets\`) and the full deduped trace ID array. The call is idempotent, so re-attaching IDs already in the dataset is a safe no-op.
4. Briefly summarize for the user: "Seeded the dataset with N reused labels (M fails, K passes). Want me to find more candidates to label, or is this set enough to move on?"

{{branches}}`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Find more candidates to label",
                                description: "go through the regular intent + search + label flow on top of the reused set",
                            },
                            next: "dataset/ask-search-intent",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Move on with just the reused set",
                                description: "skip further labeling; the dataset page is already open with the reused traces streamed in",
                            },
                            recommended: true,
                            next: "dataset/await-event",
                        },
                    ],
                },
                {
                    id: "ask-search-intent",
                    kind: "action",
                    toolCalls: ["ask"],
                    title: "Ask what kinds of traces to find",
                    body: `**Ask what kinds of traces to find**: The user picked "Define new criteria" (or arrived here from the reuse path wanting more). Find out what they're actually trying to surface. The trace function may have thousands of traces; "what should I label?" is the question that makes the rest of this phase useful.

When asking, {{askVerb}} with these options (and a free-text fallback so the user can describe something specific):

- **A, Failures of a certain kind** *(recommended when the user already has a hypothesis)*, they tell you the pattern (empty outputs, hallucinated tool args, regressions on a specific input shape, etc.) and you search for matching traces
- **B, Recent customer complaints / reports**: they paste or describe specific incidents and you find the matching traces by user, session, or time window
- **C, Open-ended, you decide**: no hypothesis yet; you sample broadly across recent traces, look for diversity, and surface anything that looks like a candidate failure or interesting edge case

Hold the user's answer (the chosen option **and** any free-text detail) in working context, the next step uses it to shape the {{tool:searchTraces}} filters and which traces to prioritise reading. If they pick C, default to recent + diverse + non-empty outputs.`,
                    next: "dataset/find-unlabeled",
                },
                {
                    id: "find-unlabeled",
                    kind: "action",
                    toolCalls: ["searchTraces", "readTraces"],
                    title: "Find unlabeled traces",
                    emit: "Searching traces",
                    body: `**Find unlabeled traces**: Search without label filters to find unlabeled traces for the trace function. **Shape the search by the intent captured in the previous step** (or by the prior dataset's existing labels, if any): Option A = filter to traces matching the user's described failure pattern; Option B = filter by the user, session, or time window of the reported incidents; Option C = default sweep (recent, diverse inputs, non-empty outputs). Use {{tool:searchTraces}} with the relevant filters, then {{tool:readTraces}} with \`scope: "summary"\` to read candidates and identify which are worth labeling, look for diverse inputs, traces that produced output (not empty), and traces that cover different scenarios under the chosen intent. Filter out near-duplicates and uninteresting traces. If every trace is already labeled and attached to this dataset, you can move straight on with no new candidates.`,
                    next: "dataset/ask-labeling-mode",
                },
                {
                    id: "ask-labeling-mode",
                    kind: "branch",
                    toolCalls: ["ask"],
                    title: "Ask how the user wants to label",
                    body: `**Ask how the user wants to label**: Before any verdicts go on these candidate traces, {{askVerb}} how the user wants to label them. There are exactly two modes, and the answer determines whether you call {{tool:updateAgentLabels}} at all:

{{branches}}

Recommend Option A, an agent first pass turns the labeling page into a quick approve/edit review. But respect the user's choice: if they pick B, do **not** call {{tool:updateAgentLabels}} for any of these candidates. They want to label from scratch in the labeling page, with no agent verdicts pre-filled. If no new candidate traces were found in the previous step, skip this question and continue.`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Agent labels first, I approve / edit",
                                description: "agent makes a first pass; you approve or edit each verdict in the labeling page",
                            },
                            recommended: true,
                            next: "dataset/label-self",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "I'll label them manually",
                                description: "no agent verdicts; you label every trace from scratch in the labeling page",
                            },
                            next: "dataset/attach-to-dataset",
                        },
                    ],
                },
                {
                    id: "label-self",
                    kind: "action",
                    toolCalls: ["bash", "read", "updateAgentLabels", "agent"],
                    commandCalls: ["readTracesBatched"],
                    title: "Agent first pass: label them yourself before opening the labeling page",
                    emit: "Labeling traces",
                    body: `**Agent first pass: label them yourself before opening the labeling page**: Reachable only when the user picked Option A in the previous step. **You** label the approved candidate traces so the labeling page becomes an approve/edit review instead of a blank labeling session. Run \`{{command:readTracesBatched}} <trace-id...> --scope full\` **once** with all the approved trace IDs. It fans the reads out in parallel batches of 10 and writes the combined result to a temp file; the command prints \`{"status":"ok","outputFile":"..."}\` as JSON, so \`Read\` that \`outputFile\`. **Use this command here, not the \`read_traces\` MCP tool directly:** \`read_traces\` caps at 10 IDs, so calling it for the approved set would re-introduce the serial per-batch fan-out \`readTracesBatched\` exists to replace. Read each trace's inputs / output / spans yourself, and decide for each one whether it looks like a PASS or a FAIL. **Ground your judgment in the codebase, not just the trace text.** Before you start labeling, read the instrumented function in the user's source (located in Phase 2 in \`wizard\` mode, or via the grep step in this phase's intro in \`dataset\` mode) and any nearby code that explains intent, comments, docstrings, README sections, related tests, BAML files, so you know what the function is *supposed* to do and what "good" looks like for it. Apply the same context to every trace: does this output achieve the function's goal as expressed in the code? Does it match the patterns in the already-validated traces? **{{#claude}}First decide how you'll produce the verdicts, judge serially yourself or fan the judging out per the fan-out block immediately below, based on how many candidates there are. Do not persist yet.{{/claude}}{{^claude}}Judge each candidate as the block immediately below describes. Do not persist yet.{{/claude}}** However you produce them, the verdicts then land in a single {{tool:updateAgentLabels}} call with an array of \`{ traceId, label, annotation }\` objects, **both \`label\` (true for pass, false for fail) and \`annotation\` (a one-or-two-sentence explanation written for the human reviewer, ideally referencing what the code is trying to do) are required for every trace**. Commit to a verdict, if you genuinely cannot decide, you didn't read the trace or the code carefully enough. The labels you save here start unapproved; they only become part of the validated dataset once a human approves them in the labeling page.

${FAN_OUT_JUDGING}

**Per-item inputs for this step (however you produce the verdicts):** each candidate trace's own artifacts, its input and output (from the \`{{command:readTracesBatched}} --scope full\` load above), judged against the shared context, what the function is supposed to do and what "good" looks like for it (the instrumented function and the nearby intent you read above, plus the patterns from the already-validated traces). The verdict for each trace is PASS or FAIL plus the one-or-two-sentence annotation, the \`{ traceId, label, annotation }\` shape {{tool:updateAgentLabels}} takes. {{#claude}}When you fan out, each subagent's prompt carries its batch's trace inputs/outputs plus that shared context and returns its batch's \`{ traceId, label, annotation }\` array, which you concatenate across batches. {{/claude}}However the verdicts are produced, you make the single {{tool:updateAgentLabels}} call (one array, all traces) just as described above. The labels still land unapproved for human review in the labeling page no matter how they were produced.

**The cross-trace failure-pattern synthesis stays separate.** Phase 4 (\`understand-failures\`) is a deliberate join: it reads all the labels at once so the holistic "these N traces fail the same way" view is never lost. Per-trace labeling here is mechanical and independent, {{#claude}}and when you fan out, each subagent sees only its own batch, so {{/claude}}do not fold cross-trace synthesis into the labeling, that recognition is Phase 4's job on the full set.

> 🚨 **HARD RULE, DO NOT SKIP (agent-first mode only):** When the user picked Option A, you MUST call {{tool:updateAgentLabels}} with verdicts for every approved trace BEFORE navigating Studio to the labeling page. Sending the user into an agent-first review with no pre-labeled verdicts is a process violation. (In manual mode this step is unreachable, and the rule does not apply.)

> **Made a mistake?** If you realize a verdict was wrong (e.g., you mislabeled a trace or want to re-evaluate), call {{tool:updateAgentLabels}} again with \`{ traceId, archive: true }\` for those traces. The previous label is hidden (kept for audit), and you can re-label the trace from scratch with another \`update_agent_labels\` call.`,
                    next: "dataset/attach-to-dataset",
                },
                {
                    id: "attach-to-dataset",
                    kind: "action",
                    toolCalls: ["addTracesToDataset"],
                    title: "Attach labeled traces to the dataset",
                    body: `**Attach candidate traces to the dataset**: Call {{tool:addTracesToDataset}} with the \`datasetId\` chosen earlier and the array of approved candidate trace IDs (in agent-first mode, the ones you just labeled; in manual mode, the candidates the user approved in find-unlabeled). The call is idempotent, re-adding traces already in the dataset is a no-op, so it's safe to include the full set. If no new candidate traces were approved (the dataset was already populated), skip this step.

The dataset review page is already open in Studio (opened earlier in \`open-page\`). Each trace you attach streams in live via real-time events, so the user sees them appear instantly. After attaching, tell the user the dataset is populated and ready for their review, then proceed to \`await-event\`.`,
                    next: "dataset/await-event",
                },
                {
                    id: "await-event",
                    kind: "branch",
                    toolCalls: ["bash", "monitor"],
                    title: "Wait for user to finish dataset review",
                    body: `🚨 **MANDATORY: Set up a Monitor IMMEDIATELY.** Do not skip this step or defer it. The user is reviewing traces in Studio right now and will click Done or Edit with agent. If you don't monitor, you will miss the event.

Use the **Monitor tool** to tail the durable Studio event file for new JSON events:

\`\`\`bash
tail -f -n +<NEXT_LINE> <eventFile> | grep -E --line-buffered '"event"'
\`\`\`

\`<eventFile>\` is the path from the \`monitor\` line emitted by \`openStudioTo.js\` in the \`open\` step (the daemon appends events here for the whole session, so tailing it replays anything that happened before you attached). \`<NEXT_LINE>\` is one past the last line you read (e.g. if you read 5 lines, use \`-n +6\`).

The Monitor streams ALL events from Studio. Route on the \`event\` field in each JSON line:

- \`{"event":"return-to-agent",...}\`, user clicked **Done**. Dataset review is complete.
- \`{"event":"edit-with-agent",...,"datasetId":"..."}\`, user clicked **Edit with agent**. Go to the modify loop, then come back here.
- \`{"event":"session-ended",...}\`, user closed Studio entirely.
- \`{"event":"navigated",...}\`, Studio navigated to a new page (informational).
- \`{"event":"element-clicked",...}\` / \`{"event":"focusChanged",...}\`, user interaction events (used during template editing).

**Stay silent while monitoring.** Do not narrate each event. Only speak when you reach a branch point or hit an error.

**Template editing during labeling.** The user may ask to edit a template in chat while the Monitor is running (e.g. "change the LLM view"). This arrives as a user message, not a Studio event. If so, go to the edit-template-loop step. **Do NOT invoke \`{{cmd}}setup templates\`**: that navigates Studio away from the dataset page.

{{whens}}`,
                    branches: [
                        {
                            when: "`event: edit-with-agent`",
                            description: "user clicked Edit with agent on the dataset page. Go to the modify loop, then come back here to read the next event",
                            next: "dataset/modify-loop",
                        },
                        {
                            when: "`event: return-to-agent`",
                            description: "user clicked Done on the dataset page. Dataset review is complete, move on to build + confirm the dataset",
                            next: "dataset/build-dataset",
                        },
                        {
                            when: "`event: session-ended`",
                            description: "user closed Studio. Stop the flow",
                            next: "cleanup/close-studio",
                        },
                        {
                            when: "user asks to edit a template in chat",
                            description: "user wants to change how traces render (e.g. 'edit the llm template', 'change the function view'). Go to the edit-template-loop, then come back here",
                            next: "dataset/edit-template-loop",
                        },
                    ],
                },
                {
                    id: "modify-loop",
                    kind: "action",
                    toolCalls: [
                        "searchTraces",
                        "readTraces",
                        "updateAgentLabels",
                        "addTracesToDataset",
                        "removeTracesFromDataset",
                    ],
                    title: "Modify loop: add or remove traces in chat",
                    body: `**Modify loop: add or remove traces in chat**: The dataset page is still open in Studio and the user wants you to add or remove traces. Ask in plain chat:

> What would you like to add or remove? You can describe by criteria (e.g. "drop empty-output traces", "add 5 more from last week with errors") or paste explicit trace IDs.

Then wait for the user's next message. It will contain their answer. Do NOT {{askVerb}} here (the answer is free-form and options would just add an extra step before the user can type).

Then act on it:

- **Adding traces:** find candidates with {{tool:searchTraces}} / {{tool:readTraces}}, then respect the labeling mode the user chose earlier in this phase (the ask-labeling-mode step). In **agent-first mode (Option A)**, label them yourself with {{tool:updateAgentLabels}} (same rigor as label-self: every trace gets a verdict + annotation, grounded in the code) before attaching. In **manual mode (Option B)**, do NOT call {{tool:updateAgentLabels}}. **If no labeling mode was selected** (the user took the Reuse → Move-on path that bypasses ask-labeling-mode, or find-unlabeled returned no candidates so ask-labeling-mode self-skipped), default to **agent-first mode (Option A)**: match the recommended default and label new candidates yourself before attaching. Either way, call {{tool:addTracesToDataset}} to attach.
- **Removing traces:** call {{tool:removeTracesFromDataset}} with the trace IDs to remove. The traces themselves aren't deleted, only their membership in the dataset.

The dataset page reflects each add/remove live (SSE), so the user sees changes flow in as you make them. When you're done, summarize what changed in chat and **return to the await-event step to read the next event**. The user can click Edit with agent again for another modify round, or Done to finalize.`,
                    next: "dataset/await-event",
                },
                {
                    id: "edit-template-loop",
                    kind: "action",
                    toolCalls: ["getTemplateReference", "getTemplate", "updateTemplate"],
                    title: "Edit a trace view template inline during labeling",
                    body: `**Edit a trace view template inline.** The user wants to change how a span type renders. Handle this with MCP tools; do NOT invoke \`{{cmd}}setup templates\`.

1. Call {{tool:getTemplateReference}} if you haven't already this conversation. It documents the Nunjucks engine, variables, and filters.
2. Identify the span type (\`llm\`, \`agent\`, \`function\`, \`guardrail\`, \`handoff\`, \`custom\`). If ambiguous, ask.
3. Call {{tool:getTemplate}} with \`spanType\` and \`traceFunctionKey\` (from Phase 1) to read the current template.
4. Edit the template. Stay inside the documented variables and filters. Do not use \`{%raw%}{% extends %}{%endraw%}\`.
5. Call {{tool:updateTemplate}} with the full edited body. The dataset page re-renders automatically via SSE.
6. Acknowledge in one line. Do not paste the template body back.

Then return to the await-event step. If the user wants more edits, they'll ask again and you'll re-enter this step.`,
                    next: "dataset/await-event",
                },
                {
                    id: "build-dataset",
                    kind: "action",
                    toolCalls: ["readTraceLabels", "bash", "read"],
                    commandCalls: ["readTracesBatched"],
                    title: "Build the dataset",
                    body: `**Build the dataset**: You already know the trace IDs in this dataset (you attached them in earlier steps and tracked any add/remove from modify rounds). Load each trace's label + annotation into context. **Which tool depends on \`costRun\`:**

- **\`costRun\` is false (the common case):** call {{tool:readTraceLabels}} with all of them. It returns only the verdict fields (label + annotation + approved, no span content) and takes up to 100 IDs per call, so for almost every dataset this is a single call. Only if the dataset has more than 100 traces, split the IDs into chunks of 100 and call {{tool:readTraceLabels}} once per chunk; the responses are small, so plain sequential calls are fine.
- **\`costRun\` is set:** run \`{{command:readTracesBatched}} <trace-id...> --scope full\` **once** with all the trace IDs instead, because the Phase 5 cost-delta step reads each original trace's recorded token usage from this load and {{tool:readTraceLabels}} does not carry it. The command fans the \`scope: "full"\` reads out in parallel batches of 10 and writes the combined result to a temp file; it prints \`{"status":"ok","outputFile":"..."}\` as JSON, so \`Read\` that \`outputFile\`. **Use this command here, not the \`read_traces\` MCP tool directly:** \`read_traces\` caps at 10 IDs, so calling it for the dataset would re-introduce the serial per-batch fan-out \`readTracesBatched\` exists to replace.

**Re-read them fresh here even if you read them earlier in this phase:** the Studio labeling review persists human approvals and label/annotation edits to the DB, so cached context from the find / label steps can be stale. This is the working set for confirm + every Phase 5 experiment.`,
                    next: "dataset/confirm-dataset",
                },
                {
                    id: "confirm-dataset",
                    kind: "branch",
                    toolCalls: ["searchTraces", "ask"],
                    title: "Confirm the dataset",
                    body: `**Confirm the dataset**: Present the dataset {{presentMode}}: each entry showing (trace ID, label, annotation summary). The dataset must contain at least one **validated failing label**: i.e. at least one trace where a human either authored or approved a \`false\` label. To check, call {{tool:searchTraces}} restricted to the dataset trace IDs with \`validated: true\` and \`labelResult: false\`. Two outcomes:

{{whens}}

Unapproved agent labels do **not** satisfy this gate by design, \`validated: true\` excludes them.`,
                    branches: [
                        {
                            when: "gate fails (no validated failing label, search returns nothing)",
                            description: "tell the user and loop back to find or label more unlabeled traces",
                            next: "dataset/find-unlabeled",
                        },
                        {
                            when: "gate passes (at least one validated failing label)",
                            description: "get explicit approval, then continue",
                            next: "dataset/hold-in-context",
                        },
                    ],
                },
                {
                    id: "hold-in-context",
                    kind: "action",
                    title: "Hold in-context",
                    body: `**Hold in-context**: This approved dataset is the benchmark for all experiments in Phase 5. Keep both the \`datasetId\` and the trace IDs in your working context throughout.`,
                    next: {
                        byMode: {
                            wizard: "diagnose/understand-failures",
                            dataset: "diagnose/understand-failures",
                            experiment: "cleanup/close-studio",
                            fix: "cleanup/close-studio",
                            "cost-optimize": null,
                            investigate: "diagnose/understand-failures",
                            benchmark: "cleanup/close-studio",
                            "add-trace": null,
                            replay: "cleanup/close-studio",
                        },
                    },
                },
            ],
        },
        {
            id: "diagnose",
            title: "Phase 4: Diagnose & Plan",
            stepStyle: "list",
            steps: [
                {
                    id: "understand-failures",
                    kind: "action",
                    toolCalls: ["readTraces"],
                    title: "Understand failures",
                    emit: "Diagnosing failures",
                    body: `**Understand failures.** Using the failed traces you read in Phase 3 (or read them now if you haven't):

- Call {{tool:readTraces}} on 3–5 failed traces with \`scope: "full"\`

Synthesize the failure patterns, what's going wrong, what the common threads are.`,
                    next: "diagnose/read-code",
                },
                {
                    id: "read-code",
                    kind: "action",
                    toolCalls: ["read"],
                    title: "Read the code",
                    body: `**Read the code.**

- Find the instrumented function in the codebase (in \`wizard\` mode you found it in Phase 2; in \`dataset\` mode you grepped for the key in Phase 3's intro; in \`investigate\` mode you found it in Phase Investigate's gather-context step)
- Read the full implementation, follow the call chain to understand the logic
- Identify **iteration targets**: prompts, system messages, parameters, preprocessing, postprocessing
- If BAML files are involved, read the relevant \`.baml\` files`,
                    next: "diagnose/categorize-fixes",
                },
                {
                    id: "categorize-fixes",
                    kind: "action",
                    toolCalls: ["ask"],
                    title: "Categorize fixes based on failure annotations",
                    body: `**Categorize fixes based on failure annotations.** Based on the failure patterns, the code, and the labeled dataset from Phase 3, categorize proposed changes into three buckets:

**Bucket 1, Code fixes**: Deterministic bugs (off-by-one, type mismatch, missing null check, wrong variable). These won't recur once fixed. Bundle all code fixes into a single experiment unless they are large feature changes. These are applied first as a foundation that all subsequent experiments build on.

**Bucket 2, Judgment-based fixes**: Prompt changes, context truncation, search tuning, output formatting, etc. These require the user's judgment to evaluate correctness. Each gets its own experiment.

**Bucket 3, Infrastructure proposals**: Larger changes that require new infrastructure, architectural changes, or significant feature work. These are separated out because experiments become harder to compare when some include large infra changes and others don't, apples-to-apples comparison requires a consistent baseline. Do not run experiments for these. Instead, if the user has integrations (Linear, Notion, Jira), propose creating a task with a clear writeup for future work.

Present the categorized plan {{presentMode}}:

> "Based on the N traces in the dataset, here's what I see:
>
> **Code fixes** (experiment #1, bundled):
>
> - [Fix]: [What and why, which traces it addresses]
>
> **Judgment-based experiments** (#2, #3, ...):
>
> - [Experiment]: [What change, which traces it targets, hypothesis]
>
> **Future infrastructure** (not experiments):
>
> - [Proposal]: [What it would require, which traces it would help]
>
> I'll replay each experiment against the labeled dataset and evaluate using the annotations as acceptance criteria."

Get the user's confirmation before proceeding.`,
                    next: {
                        byMode: {
                            wizard: "iterate/detect-replay-capabilities",
                            dataset: "iterate/detect-replay-capabilities",
                            experiment: "cleanup/close-studio",
                            fix: "cleanup/close-studio",
                            "cost-optimize": null,
                            investigate: "iterate/detect-replay-capabilities",
                            benchmark: "cleanup/close-studio",
                            "add-trace": null,
                            replay: "cleanup/close-studio",
                        },
                    },
                },
            ],
        },
        {
            id: "cost",
            title: "Phase Cost: Diagnose Token Spend",
            intro: `Reached only from \`cost-optimize\` mode, after \`load-dataset/pick-dataset\` has picked the dataset and located the code. \`costRun\` is always true in this mode (the mode exists to cut token cost), and \`costBasis\` was fixed at entry per the usual rules. The goal is to **lower token cost while holding the pass rate**: the labeled dataset is the regression guard, the token delta is the score. This phase profiles where the dataset's tokens actually go and turns that into a concrete, ordered list of token-reduction experiments, then hands off to \`load-dataset/pick-execution-mode\` so the shared replay loop (Phase 5) runs each one, persists verdicts, and reports the per-item and dataset-wide token delta on the run's basis so you can confirm cost dropped without a quality regression.`,
            stepStyle: "list",
            steps: [
                {
                    id: "diagnose",
                    kind: "action",
                    toolCalls: ["readTraces", "read", "grep", "ask"],
                    commandCalls: ["readTracesBatched"],
                    title: "Profile token spend and plan reductions",
                    emit: "Diagnosing token spend",
                    body: `**Profile where the tokens go, then plan reductions.** Ground every proposal in the actual token breakdown of this dataset's traces and in the code under test (located in \`load-dataset/pick-dataset\`).

**1. Read the token breakdown.** The dataset's full traces were already loaded in \`load-dataset/pick-dataset\` (via \`{{command:readTracesBatched}} ... --scope full\`); reuse that \`outputFile\`. Sort by recorded token usage and study the most expensive 3-5 traces. For each, see where the tokens go: \`input\` vs \`output\`, and how much of \`input\` is \`cached\` vs fresh. A run on \`costBasis = uncached\` cares about fresh \`(input - cached) + output\`; a run on \`all\` cares about raw \`input + output\`. Note the dominant cost driver per trace.

**2. Read the code.** Read the instrumented function and the prompt/template it builds (follow the call chain; read any \`.baml\` files). Map each token sink from step 1 to a place in the code: a system prompt, retrieved/injected context, few-shot examples, the output schema, a verbose instruction block, an over-large \`max_tokens\`, or a model whose pricing dominates.

**3. Categorize token-reduction experiments.** Turn the drivers into concrete experiments, ordered cheapest-to-try / highest-expected-saving first. Common levers (pick what the data supports, do not apply blindly):

- **Prompt / context trimming**: drop redundant instructions, dedupe repeated context, shorten few-shot examples, cap or summarize retrieved context. Biggest lever when fresh \`input\` dominates.
- **Prompt caching**: reorder the prompt so the large stable prefix (system prompt, schema, static context) is cacheable and the variable part comes last. Moves tokens from fresh to cached, which matters on the \`uncached\` basis even when the \`all\` total looks flat.
- **Output shape**: tighten the output schema / instructions so the model emits less (the \`output\` side of the bill). Watch for quality loss.
- **Model choice**: a smaller / cheaper model for this function, when the dataset shows the task is within its reach. A per-experiment swap, validated by the same pass-rate guard.

Keep deterministic code cleanups (dead context assembly, accidental double-injection) as their own bundled first experiment, the same way Phase 4 treats code fixes: a foundation later experiments build on.

**4. Present the plan and confirm.** Present the categorized, ordered plan {{presentMode}}, leading with the cost framing:

> "Token spend on \`<key>\` is concentrated in [driver, e.g. 'a 2k-token system prompt re-sent uncached every call']. Ranked experiments to cut it without regressing pass rate:
>
> 1. [Experiment]: [lever, expected token saving, which traces, hypothesis]
> 2. ...
>
> I'll replay each against the labeled dataset and report the token delta (on the <all|uncached> basis) alongside pass/fail, so we only keep changes that cut cost without breaking quality."

If the dataset has no validated labels (only the \`≥1 trace\` minimum), say so in this confirmation: the token delta is still measured, but pass-rate is then a weaker guard, recommend labeling a few traces (\`{{cmd}}assistant dataset <key>\`) for a stronger guard. Get the user's confirmation, then continue to \`load-dataset/pick-execution-mode\`, which picks parallel vs serial execution and runs the first experiment.`,
                    next: "load-dataset/pick-execution-mode",
                },
            ],
        },
        {
            id: "load-dataset",
            title: "Phase 5 Setup: Pick Dataset & Execution Mode",
            intro: `Entry for \`experiment\`, \`cost-optimize\`, and \`benchmark\` modes, which skip the function picker and the dataset-building phases. Pick the dataset to run against, locate the code, and (in \`experiment\` / \`cost-optimize\` mode) choose parallel vs serial execution, then tail into Phase 5's replay loop at \`detect-replay-capabilities\`. \`cost-optimize\` first detours through its cost-diagnosis phase (\`cost/diagnose\`) between dataset-pick and execution-mode. \`wizard\` / \`dataset\` / \`investigate\` modes never pass through here, they reach \`detect-replay-capabilities\` directly from Phase 4. \`fix\` mode tails in at \`pick-execution-mode\` (it already resolved its code and diagnosed the failure in Phase Fix, so it skips \`pick-dataset\`; the dataset is picked or created later, in \`fix-add-to-dataset\`, only after the target trace passes).`,
            stepStyle: "list",
            steps: [
                {
                    id: "pick-dataset",
                    kind: "branch",
                    toolCalls: [
                        "bash",
                        "grep",
                        "read",
                        "listDatasets",
                        "listExperiments",
                        "ask",
                    ],
                    commandCalls: ["readTracesBatched"],
                    title: "Pick a dataset and locate the code",
                    emit: "Running experiments",
                    body: `The trace function key comes from the argument and no prior phase has run. Pick the dataset to run against (\`experiment\` and \`cost-optimize\` modes iterate against it; \`benchmark\` mode replays it once to measure the current code), then locate the code:

1. **Grep the codebase** for the trace function key (e.g. \`grep -r "<traceFunctionKey>" --include="*.ts" --include="*.tsx" --include="*.py" --include="*.rb" --include="*.go" --include="*.baml"\`) and note the file path. This is the code under test (the code you'll iterate on in \`experiment\` / \`cost-optimize\` mode, or measure as-is in \`benchmark\` mode).
2. **Pick the dataset.** If a \`<dataset-id>\` argument was provided, use it directly. Otherwise call {{tool:listDatasets}} with the trace function key, present the result to the user {{presentMode}}, and use their choice. Hold the chosen \`datasetId\` in working context.
3. **Load it.** Run \`{{command:readTracesBatched}} <trace-id...> --scope full\` **once** with the dataset's trace IDs; it fans the reads out in parallel batches of 10 and writes the combined result to a temp file, then prints \`{"status":"ok","outputFile":"..."}\` as JSON, so \`Read\` that \`outputFile\` to load labels + annotations into context. **Use this command, not the \`read_traces\` MCP tool directly** (\`read_traces\` caps at 10 IDs).
4. **Branch on the result. The usability gate depends on the mode:**
   - In \`experiment\` mode, the dataset must have **≥1 validated failing label** (there has to be something to fix).
   - In \`benchmark\` mode, the dataset just needs **≥1 trace**: benchmark replays the entire dataset against the current code regardless of label mix (an all-passing dataset is a valid regression baseline).
   - In \`cost-optimize\` mode, the dataset just needs **≥1 trace**: the goal is to cut tokens while holding quality, so an all-passing-but-expensive dataset is the common, valid case. The labeled traces (if any) guard the pass rate; the token usage on every trace is what the run optimizes.

{{whens}}`,
                    branches: [
                        {
                            when: "no datasets exist for this function (`list_datasets` returned empty), or the picked dataset fails the mode's usability gate (experiment: no validated failing labels; benchmark / cost-optimize: no traces at all)",
                            description: `tell the user the function has no usable dataset yet and recommend running \`{{cmd}}assistant dataset <key>\` first; then stop the flow (the cleanup step closes Studio if one was opened)`,
                            next: "cleanup/close-studio",
                        },
                        {
                            when: "dataset loaded (experiment: ≥1 validated failing label; benchmark / cost-optimize: ≥1 trace)",
                            description: `summarize the dataset for the user (counts of pass/fail) and the failure annotations. In \`experiment\` mode, pick a first experiment from the failure patterns. In \`benchmark\` mode, confirm the dataset and proceed to replay the full set. In \`cost-optimize\` mode, confirm the dataset and proceed to the cost-diagnosis phase`,
                            next: {
                                byMode: {
                                    wizard: "load-dataset/pick-execution-mode",
                                    dataset: "load-dataset/pick-execution-mode",
                                    experiment: "load-dataset/pick-execution-mode",
                                    fix: "load-dataset/pick-execution-mode",
                                    "cost-optimize": "cost/diagnose",
                                    investigate: "load-dataset/pick-execution-mode",
                                    benchmark: "iterate/detect-replay-capabilities",
                                    "add-trace": null,
                                    replay: null,
                                },
                            },
                        },
                    ],
                },
                {
                    id: "pick-execution-mode",
                    kind: "branch",
                    toolCalls: ["bash"],
                    title: "Pick parallel or serial execution",
                    body: `**Decide once: parallel worktree subagents, or serial in this main agent.** The check is whether subagent worktree sessions would inherit bypass permissions.

{{#claude}}\`.claude/settings.local.json\` is gitignored and does NOT propagate into subagent worktrees, so it can't grant bypass. The two locations that DO propagate are committed \`.claude/settings.json\` and user-global \`~/.claude/settings.json\`. Run:

\`\`\`bash
python3 -c "
import json, os
def has_bypass(p):
    if not os.path.exists(p): return False
    try: d = json.load(open(p))
    except Exception: return False
    return (d.get('permissions') or {}).get('defaultMode') == 'bypassPermissions'
p = has_bypass('.claude/settings.json')
g = has_bypass(os.path.expanduser('~/.claude/settings.json'))
print('parallel' if (p or g) else 'serial')
"
\`\`\`{{/claude}}{{^claude}}This editor doesn't support worktree-isolated subagents. Skip the bash check and take the serial branch.{{/claude}}

Hold the chosen mode in working context. Every iteration below (\`make-change\`, \`replay-against-dataset\`, \`evaluate-results\`) honors it.

{{whens}}`,
                    branches: [
                        {
                            when: "{{#claude}}bash output is `parallel` (bypass found in committed or user-global settings){{/claude}}{{^claude}}(unreachable on this editor){{/claude}}",
                            description: `**Parallel mode.** For each independent experiment, fork to a subagent using the Agent tool with \`isolation: "worktree"\` and \`subagent_type: "general-purpose"\`. The subagent edits its worktree, runs replay, returns its scored items + \`testRunId\` to this main agent`,
                            next: "iterate/detect-replay-capabilities",
                        },
                        {
                            when: "{{#claude}}bash output is `serial` (no bypass found){{/claude}}{{^claude}}always{{/claude}}",
                            description: `**Serial mode.** Iterate experiments one at a time in this main agent. Subagent worktrees wouldn't inherit bypass permissions, so their Edit tool would be denied`,
                            next: "iterate/detect-replay-capabilities",
                        },
                    ],
                },
            ],
        },
        {
            id: "iterate",
            title: "Phase 5: Iterate with Replay",
            intro: `In \`experiment\` mode this is an iterative improvement loop (each iteration makes a change and replays). In \`fix\` mode the first pass replays only the target trace and tags that replay with an experiment group; once it passes the trace is added to the dataset (in \`fix-add-to-dataset\`), and after that replay's labels are persisted the user can choose to show the fix in Studio, re-run the full dataset as an experiment (in Studio or terminal-only, chosen in \`fix-rerun-dataset-mode\`), keep iterating, or stop. In \`benchmark\` mode it is a single replay of the current code followed by a terminal scorecard, no changes, no iteration.

This phase begins at \`detect-replay-capabilities\`. \`experiment\` / \`benchmark\` modes arrive from Phase 5 Setup (dataset already picked); \`wizard\` / \`dataset\` / \`investigate\` modes arrive from Phase 4 (dataset built in Phase 3); \`fix\` arrives from Phase Fix with \`fixReplayScope = "single-trace"\`. \`openStudioTo.js\` resolves the active session automatically. \`benchmark\` mode opens Studio only when the run opted in with the \`studio\` keyword; without it, benchmark opens no Studio and runs terminal-only. \`fix\` opens Studio only after the user explicitly chooses to inspect the single-trace before/after or run the full-dataset experiment.`,
            stepStyle: "list",
            steps: [
                {
                    id: "detect-replay-capabilities",
                    kind: "branch",
                    toolCalls: ["bash", "grep", "ask", "skill"],
                    title: "Detect replay script capabilities",
                    body: `**Detect replay script capabilities.** Check what the replay script supports. These flags determine how experiment results are tracked and displayed. **If you already ran this step in Phase 2 earlier in this session, skip it and continue to \`make-change\` (or \`replay-against-dataset\` in benchmark mode).**

**1. Locate the replay script** (you found it in Phase 2 in \`wizard\` mode, or grep for \`scripts/replay.*\` / files importing \`bitfab.replay\` / \`client.replay\` now).

**2. Grep the replay script for the flags it forwards:**

| Grep the script for | Flag | What it enables |
|----------|------|-----------------|
| \`code-change\` or \`code_change\` | \`supportsCodeChanges\` | Code diffs attached to each experiment in the dashboard |
| \`experiment-group-id\` or \`experiment_group_id\` | \`supportsExperimentGroups\` | Live streaming of results in Studio as replay runs |
| \`dataset-id\` or \`dataset_id\` | \`supportsDatasetId\` | Durable attribution of the experiment to its dataset (shows under the dataset's experiments) |
| \`--name\` plus \`name\` / \`name:\` forwarded to \`replay()\` | \`supportsExperimentNames\` | Human-readable experiment/test-run names in the UI |
| \`traceId\` or \`trace_id\` in the output/print section | \`supportsReplayTraceIds\` (re-confirmed post-replay in \`check-trace-id-support\`) | Verdict persistence, cross-iteration comparison, Studio experiments page |

\`supportsInputAdapters\` is **not** a script-grep flag (the script gains an \`adaptInputs\` / \`adapt_inputs\` argument only after a signature actually drifts, in \`adapt-replay-inputs\`). It comes solely from the installed SDK in step 3.

**3. Confirm the installed SDK supports each flag.** A flag the script forwards is silently ignored when the installed SDK predates it, so each flag is gated on the SDK too. Run the capability probe (it resolves the installed SDK version from the lockfile/manifest and gates every capability by version, with no dist-file grepping across package-manager layouts):

\`\`\`bash
cd <project-dir> && {{command:detectCapabilities}}
\`\`\`

Read the \`<bitfab-replay-capabilities>\` block. Each line is a JSON object for one detected SDK with \`language\`, \`workspacePath\`, \`current\` (resolved version), \`versionResolved\`, \`updateAvailable\`, \`latest\`, and a \`capabilities\` object holding \`supportsExperimentGroups\`, \`supportsDatasetId\`, \`supportsCodeChanges\`, \`supportsReplayTraceIds\`, \`supportsInputAdapters\`, \`supportsExperimentNames\`. Pick the line whose \`language\` (and \`workspacePath\`, in a monorepo) matches the replay script's project.

- **Combine the two sources:** a flag is true only when the script forwards it (step 2) **and** that SDK's matching \`capabilities.*\` is true. Take \`supportsInputAdapters\` straight from \`capabilities.supportsInputAdapters\` (it has no script side).
- \`supportsReplayTraceIds\` from the probe is a definitive **pre-replay** signal; the later \`check-trace-id-support\` step still re-confirms from the actual replay output.
- If \`versionResolved\` is \`false\`, the probe couldn't pin the installed version, so every capability defaulted false and is **unverified**. Check that one SDK by hand before relying on the flags (TypeScript: grep \`node_modules/@bitfab/sdk/dist/index.d.ts\` for the option names and \`ReplayItem.traceId\`; Python: the installed \`bitfab/replay.py\`; Ruby: the installed gem's \`replay.rb\`), or resolve the version and re-run.

If the script has a flag but the SDK's \`capabilities.*\` is false, mark that flag **false**. Prioritize upgrading the SDK over using fallbacks: without replay trace IDs, verdict labels can't be persisted (benchmark/experiment results stay in-agent only).

**4. Route on the result.**

If all flags are true, skip the question and continue silently.

If one or more flags are false, tell the user which capabilities are missing and what they affect, then {{askVerb}}. List the missing capabilities in the question text:

> "Your replay script is missing support for:
>
> [if !supportsCodeChanges] **Code changes**: edits won't appear in the experiment dashboard
> [if !supportsExperimentGroups] **Experiment groups**: no live streaming; results appear in Studio after each run
> [if !supportsDatasetId] **Dataset attribution**: the experiment won't be durably linked to its dataset (still findable via the trace-lineage fallback; fixed by regenerating the script / upgrading the SDK)
> [if !supportsExperimentNames] **Experiment names**: runs will show as generated IDs instead of readable names
> [if !supportsReplayTraceIds] **Replay trace IDs**: experiment results can't be persisted or compared across iterations (your SDK needs an upgrade)
>
> [if !supportsInputAdapters] **Input adapters**: replay can't recover traces when the function's signature drifts after capture (fixed by upgrading the SDK)"

{{branches}}`,
                    branches: [
                        {
                            when: "all flags are true",
                            description: "full capability. Hold the flags in working context and continue silently",
                            next: "iterate/open-experiments-before-replay",
                        },
                        {
                            option: {
                                letter: "A",
                                label: "Upgrade the replay script",
                                description: "regenerate the script with full support, then continue",
                            },
                            recommended: true,
                            next: "iterate/upgrade-replay-script",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Continue without",
                                description: "run experiments with the current script; missing features are skipped",
                            },
                            next: "iterate/open-experiments-before-replay",
                        },
                    ],
                },
                {
                    id: "upgrade-replay-script",
                    kind: "action",
                    toolCalls: ["skill", "bash", "grep"],
                    title: "Upgrade SDK and replay script",
                    body: `**Upgrade the SDK and replay script.** The replay script references SDK APIs (\`name\`, \`experimentGroupId\`, \`codeChangeDescription\`, per-item \`traceId\`, \`adaptInputs\` / \`adapt_inputs\`) that require a recent SDK. Upgrade the SDK first, then regenerate the script.

**1. Upgrade the SDK.** Run the capability probe to read the installed version and update status (skip if you still have its block from \`detect-replay-capabilities\`):

\`\`\`bash
cd <project-dir> && {{command:detectCapabilities}}
\`\`\`

For the SDK matching this project, the \`<bitfab-replay-capabilities>\` block reports \`current\` (resolved version), \`latest\`, \`updateAvailable\`, and \`renameFrom\`. If \`updateAvailable\` is false, the SDK is already current, skip to step 2. Otherwise run the package manager's update command:
- TypeScript: \`pnpm update @bitfab/sdk\` (in monorepos, scope with \`--filter <pkg>\`). **If \`package.json\` pins an exact version (e.g. \`"@bitfab/sdk": "0.13.4"\` with no \`^\`/\`~\`), \`pnpm update\` will NOT move past the pin, bump the spec in \`package.json\` to the reported \`latest\` first (e.g. \`"@bitfab/sdk": "0.13.6"\`), then \`pnpm install\`.**
- Python: \`uv lock --upgrade-package bitfab-py && uv sync\` or \`poetry update bitfab-py\`
- Ruby: \`bundle update bitfab --conservative\`

If \`renameFrom\` is set (the SDK is on the legacy \`bitfab\` package instead of \`@bitfab/sdk\`), remove the old package and install \`@bitfab/sdk\`.

**2. Regenerate the replay script.** Locate the replay script for this trace function (found in \`detect-replay-capabilities\`). Fetch the SDK replay reference (\`https://docs.bitfab.ai/reference/typescript.md\` or the equivalent for the project language) and the script template (\`https://docs.bitfab.ai/typescript-sdk.md\`). Then edit the script to add the missing flags:
- **\`--code-change <path>\`**: parse the JSON file, pass \`codeChangeDescription\` and \`codeChangeFiles\` to \`replay()\`
- **\`--experiment-group-id <uuid>\`**: pass \`experimentGroupId\` to \`replay()\`
- **\`--name <name>\`**: pass \`name\` to \`replay()\` so the resulting experiment/test run has a readable title
- **\`--dataset-id <uuid>\`**: pass \`datasetId\` to \`replay()\`. This is the **preferred way to replay a dataset**: passed alone (no \`--trace-ids\`) the server replays exactly the dataset's traces and durably attributes the experiment to the dataset. Adding this flag is what lets the replay step drop the hand-enumerated \`--trace-ids\` list.
- **Replay Output Contract**: emit the full \`ReplayResult\` as one \`JSON.stringify(result, null, 2)\` block to stdout (including every item's \`traceId\`, \`durationMs\`, \`tokens\`, \`model\`). Human-readable summary goes to stderr.
Do NOT invoke \`{{cmd}}setup replay\` as a separate skill; edit the script inline here.

**3. Re-check capabilities.** After upgrading and editing, re-run \`{{command:detectCapabilities}}\` and re-read the \`capabilities\` object for this SDK (the probe now sees the upgraded version). Combine again with the script-side grep from step 2 and update the flags in working context. If any are still missing after the upgrade, note it but continue.`,
                    next: "iterate/open-experiments-before-replay",
                },
                {
                    id: "open-experiments-before-replay",
                    kind: "action",
                    toolCalls: ["bash"],
                    commandCalls: ["openStudio"],
                    title: "Prepare experiment group before replay",
                    body: `**Generate the experiment group ID before making changes or running replay.** In normal dataset/experiment modes, also open the experiments page before replay so the user can watch results stream in live from the moment replay starts.

**Fix-mode single-trace pass:** if mode is \`fix\` and \`fixReplayScope = "single-trace"\`, generate the \`experimentGroupId\` but do **not** open Studio yet. Hold this UUID as \`fixSingleTraceExperimentGroupId\` as well as the current \`experimentGroupId\`, then continue to \`make-change\`. This first pass is a targeted replay of the target trace, and the group exists only so the user can inspect the before/after in Studio after labels are persisted. If the user later chooses the full-dataset run (after the trace passed and \`fix-add-to-dataset\` attached it to the dataset), set \`fixReplayScope = "dataset"\` and return here; then generate a fresh \`experimentGroupId\` for the dataset experiment. Whether Studio opens for that dataset run depends on \`fixDatasetStudio\` (chosen in \`fix-rerun-dataset-mode\`): open the experiment page when it is true, keep Studio closed and report results in chat when it is false.

**Generate an experiment group ID.** Generate a fresh UUID to use as the \`experimentGroupId\` for this iteration. This groups all test runs from this iteration together so the experiments page can stream results live as the replay runs.

Treat the UUID as a literal value: substitute it directly into the \`<experimentGroupId>\` slot of each command below (and into \`--experiment-group-id\` in \`replay-against-dataset\`), exactly like \`<tokensSuffix>\`. **Do not assign it to a shell variable named \`GID\`, \`UID\`, \`EUID\`, or \`EGID\`** (in zsh these are read-only/integer special parameters bound to the process IDs, so \`GID="$(uuidgen)"\` makes the shell evaluate the UUID as arithmetic and throws \`bad math expression: operator expected at '<hex>...'\`). If you keep it in a variable at all, use a plain lowercase name like \`gid\`; a double-quoted experiments URL with \`&\` is otherwise shell-safe. If you ever see that \`bad math expression\` error, it is the variable name, not the \`&\` or the query string: rename the variable, do not rewrite the command.

**Open the experiments page.** Pick exactly one case (they are mutually exclusive):

- **\`fix\` mode with \`fixReplayScope = "single-trace"\`:** do NOT run any \`openStudio\` navigation yet. Keep Studio closed until the user explicitly chooses the post-replay "Show in Studio" option. Continue to \`make-change\`.
- **\`fix\` mode with \`fixReplayScope = "dataset"\` and \`fixDatasetStudio\` false (terminal-only re-run):** do NOT run any \`openStudio\` navigation. Generate the experiment group ID above only to tag the test run on the server, then continue. The dataset run reports its results in chat (\`share-results\`), no Studio window.
- **\`benchmark\` mode WITHOUT the \`studio\` opt-in:** do NOT run any \`openStudio\` navigation (no Studio is open). Just generate the experiment group ID above for tagging the test run on the server, then continue to \`replay-against-dataset\`.
- **\`benchmark\` mode WITH the \`studio\` flag** (and \`supportsExperimentGroups\` is true): navigate Studio to the experiments page using the group ID **and** \`&mode=benchmark\`, so the page relabels its copy as "Benchmark" (the underlying run is still an experiment; only the displayed noun changes):

  \`\`\`bash
  {{command:openStudio}} "/studio/experiments?experimentGroupId=<experimentGroupId>&mode=benchmark<tokensSuffix>"
  \`\`\`
- **All other modes** (and \`supportsExperimentGroups\` is true): navigate Studio to the experiments page using the group ID (no \`mode\` parameter):

  \`\`\`bash
  {{command:openStudio}} "/studio/experiments?experimentGroupId=<experimentGroupId><tokensSuffix>"
  \`\`\`

\`<tokensSuffix>\` is a real placeholder, like \`<experimentGroupId>\`: resolve it from \`costRun\` (and, when set, \`costBasis\`) before you open the URL:
- \`costRun\` false: substitute the empty string.
- \`costRun\` set, \`costBasis = all\`: substitute \`&tokens=1&tokenType=all\`.
- \`costRun\` set, \`costBasis = uncached\`: substitute \`&tokens=1&tokenType=uncached\`.

Always emit \`&tokenType=\` explicitly when the lens is on (never a bare \`&tokens=1\`): the page's tokenType is sticky, so a bare \`&tokens=1\` after a prior \`uncached\` view would keep counting uncached against an all-basis intent. Pinning the basis on every tokens-bearing nav keeps the live lens matching the run. Because the slot is driven by the flags, the lens lands on for token/cost runs (on the basis you fixed at entry) and stays off for quality runs, with nothing to remember to add or strip per run. The only way to get it wrong is to leave a literal \`<tokensSuffix>\` in the command.

**Token-cost lens (\`&tokens=1&tokenType=<all|uncached>\`).** When the URL carries \`&tokens=1\` the page turns on the token-cost lens: each trace and the experiment header show the original → replay total-token trend, streaming in next to pass/fail. \`&tokenType=all\` counts **all** tokens (\`input + output\`, cache reads included), tinted indigo (cheaper) / amber (costlier); \`&tokenType=uncached\` switches the same trend to the **uncached** basis (\`(input - cached) + output\`), tinted cyan / orange so it reads apart from the all-token view. A "Token count: All | Uncached" toggle in the page header flips it live too, and overrides the URL until the next tokens-bearing nav. On a quality run (\`costRun\` false) \`<tokensSuffix>\` resolves to nothing and the page looks exactly as it does today, so a non-cost run never carries the lens.

This is a Studio-opening navigation call. Launch it as a background/long-running process and read its JSONL stdout incrementally; do not wait in the foreground for the user to act in Studio. The existing Studio session handles it. If \`supportsExperimentGroups\` is false, skip this navigation (the \`open-experiments\` fallback will navigate with \`testRunIds\` after the replay completes).`,
                    next: {
                        byMode: {
                            wizard: "iterate/make-change",
                            dataset: "iterate/make-change",
                            experiment: "iterate/make-change",
                            fix: "iterate/make-change",
                            "cost-optimize": "iterate/make-change",
                            investigate: "iterate/make-change",
                            benchmark: "iterate/replay-against-dataset",
                            "add-trace": null,
                            replay: null,
                        },
                    },
                },
                {
                    id: "make-change",
                    kind: "action",
                    toolCalls: ["ask", "edit", "agent"],
                    title: "Make the change",
                    emit: "Making changes",
                    body: `**Make the change.**

- **In \`fix\` mode, this is THE fix, and diagnosis was already done in Phase Fix \`resolve\` (refine it here as needed).** Re-read the failing trace (held from Phase Fix \`resolve\`) and the function code together, pinpoint why the output is wrong, and make the change that addresses it. The failure annotation you wrote in \`resolve\` is the acceptance criterion the first replay will check. If this step is being reached after the user chose to re-run the full dataset with the already-made fix (\`fixReplayScope = "dataset"\`, \`fixSkipMakeChange = true\`, and a previous single-trace fix already produced snapshots), do not edit again or ask for another change; reuse the last change description and before/after snapshots, then continue to replay the dataset.
- Unless the previous bullet told you to reuse the existing fix, {{^codex}}use \`AskUserQuestion\` to explain{{/codex}}{{#codex}}explain to the user{{/codex}} what you're changing and why, and confirm before editing
- For every file you intend to edit in this experiment: **read the file with the Read tool first** and keep its full contents in working memory as the **before** snapshot. Then edit. Then **read the file again** to capture the **after** snapshot. Both snapshots are required by the next step (\`replay-against-dataset\`) so the experiment dashboard can render the literal edit alongside the results, this is per-experiment, not cumulative
- Hold a one-line **change description** in working memory too (e.g. "fix off-by-one in retry logic", "tighten extraction prompt"). It will be the experiment's title in the viewer
- If a file is newly created, the before snapshot is the empty string \`""\`. If a file is deleted, the after snapshot is \`""\`. The path is always the repo-relative file path, no \`repo\`, \`commit\`, or other context fields`,
                    next: "iterate/replay-against-dataset",
                },
                {
                    id: "replay-against-dataset",
                    kind: "action",
                    toolCalls: ["bash", "read", "write"],
                    commandCalls: ["replayProgress", "persistReplayLabels"],
                    title: "Replay against the selected scope",
                    emit: "Running replay",
                    body: `**Replay against the selected scope.** In normal dataset modes, collect the trace IDs from the labeled dataset (built in Phase 3 in \`wizard\` and \`dataset\` modes, or rehydrated in Phase 5 Setup's \`pick-dataset\` step in \`experiment\` and \`benchmark\` modes). In \`fix\` mode, branch on \`fixReplayScope\`: \`"single-trace"\` means replay only the target trace ID from Phase Fix; \`"dataset"\` means replay the full dataset after the user explicitly opted into the Studio experiment. The experiment group ID was already generated in the \`open-experiments-before-replay\` step. For the initial \`fixReplayScope = "single-trace"\` pass, the group tags the targeted replay for optional before/after inspection but Studio is not opened yet.

**In \`benchmark\` mode, skip the code-change payload entirely.** Benchmark makes no experiment-style edits to the traced function, so there is no code diff to capture. Omit \`--code-change\` from the invocation. The replay evaluates the current code as-is against the labeled dataset. Use \`"Benchmark: current code baseline"\` as the change description for display purposes. (Infra fixes are still allowed when a gap blocks the run, upgrading the SDK / replay script in \`detect-replay-capabilities\`, or adding \`mockOnReplay\` to a failing child span below, since none of those change the function's measured behavior. What you must not do is edit the traced function to alter its output.)

**Write the code-change payload first (skip this entire block in \`benchmark\` mode, \`make-change\` never ran, there are no snapshots, and \`--code-change\` is omitted per the benchmark note above).** Before running the script, write a tmp JSON file (e.g. \`/tmp/bitfab-code-change-<experimentN>.json\`) using the snapshots captured in \`make-change\`:

\`\`\`json
{
  "description": "<the one-line change description from make-change>",
  "files": [
    { "path": "<repo-relative path>", "before": "<full file contents before edit>", "after": "<full file contents after edit>" }
  ]
}
\`\`\`

The schema is flat, every file object is exactly \`{ path, before, after }\`. Do **not** add \`repo\`, \`commit\`, or any other context fields; \`path\` is the sole identifier. Use \`""\` for newly created or deleted files. One JSON file per experiment, never reuse last iteration's payload.

**Check the \`supportsCodeChanges\` flag** (from \`detect-replay-capabilities\`). If false, skip writing the code-change JSON file and omit \`--code-change\` from the invocation. The replay itself is unaffected; only the code-change metadata is missing from the experiment viewer.

**Check the \`supportsExperimentGroups\` flag** (from \`detect-replay-capabilities\`). If true and an \`experimentGroupId\` exists, pass \`--experiment-group-id <experimentGroupId>\` (from \`open-experiments-before-replay\`) so the test run is tagged with the group. This includes the initial \`fixReplayScope = "single-trace"\` pass. If false, skip the flag; the post-replay Studio inspection can fall back to \`testRunId\` when available.

**Check the \`supportsExperimentNames\` flag** (from \`detect-replay-capabilities\`). If true, pass \`--name "<experimentName>"\` so the resulting experiment/test run is readable in the UI. Use the one-line change description from \`make-change\` as \`<experimentName>\`; in \`benchmark\` mode use \`Benchmark: current code baseline\`. Keep it 120 characters or fewer. If false, omit \`--name\`; this is cosmetic and the replay still runs.

**Choose trace selection.**

- **Initial \`fix\` pass (\`fixReplayScope = "single-trace"\`)**: pass \`--trace-ids <targetTraceId>\` (the trace ID held from Phase Fix \`resolve\`) and do NOT pass \`--dataset-id\`. This is the user's requested targeted replay. It proves the bug turned green before anything is added to a dataset or the rest of the dataset is run.
- **Full dataset runs**: check the \`supportsDatasetId\` flag (from \`detect-replay-capabilities\`). When true, this is the **preferred way to replay the dataset**: pass \`--dataset-id <datasetId>\` (the dataset id held in working context: from \`pick-dataset\` in \`experiment\` / \`cost-optimize\` / \`benchmark\` modes, or from \`fix-add-to-dataset\` in \`fix\` mode, which skips \`pick-dataset\`) and **omit \`--trace-ids\` entirely**. The server replays exactly the dataset's traces and durably attributes the experiment to the dataset, so it shows under the dataset's experiments even when trace lineage can't be reconstructed, and you don't have to enumerate the dataset's trace IDs by hand. Only when \`supportsDatasetId\` is false do you fall back to \`--trace-ids <the dataset's resolved trace ids>\` (attribution then relies on the derived trace-lineage join). If the script lacks \`--dataset-id\`, prefer upgrading it (see \`upgrade-replay-script\`) over the trace-ids fallback.

**Run the replay through \`{{command:replayProgress}}\` in the background, then relay the progress lines it prints.** Before starting, choose a unique run directory for this replay, for example \`.bitfab/replays/<experimentN>-<timestamp>-<short-random>\`, pass it with \`--run-dir\`, and hold that path in context; do not use a shared \`--events-log\` path. A foreground run blocks you for the whole replay and the user just sees "a shell is running" with no detail. \`{{command:replayProgress}}\` runs the replay, turns the SDK's per-trace \`@@bitfab:progress\` events into one self-contained line per trace on its stdout (a header, then for each trace as it settles a pass/fail glyph, the running \`n/total\`, and that trace's duration, with the error reason inline on failure, plus a liveness heartbeat line when a slow trace goes quiet so the run never looks frozen, then a final summary with total + average time), and writes a small JSONL event log at \`<run-dir>/events.jsonl\`: \`type: "progress"\` rows as items settle, then a final \`type: "complete"\` row. Large per-item payloads are written atomically under \`<run-dir>/items/\` and referenced by \`item.itemPath\` / \`items[].itemPath\`, so outputs are not duplicated in the event log. It also tees every human line to \`<run-dir>/progress.log\`, so the user can \`tail -f\` it in a separate terminal for a live view that never collapses the way a tool card does. The wrapper writes \`<run-dir>/run.json\`; once the server test run ID is known it also writes \`<parent-of-run-dir>/by-test-run/<testRunId>/run.json\` pointing back to the run directory. If the replay settles no traces at all (an \`@bitfab/sdk\` too old to carry the progress reporter, or a script that never wired \`onProgress\`), it closes with a \`⚠ done · replay finished but reported no progress\` line instead of ending silently, so a missing stream of per-trace lines never reads as a hang, fix it via \`upgrade-replay-script\` (or an SDK upgrade) and re-run. You do NOT decide what or when to print: it does the formatting; you just show each new line.

1. **Launch it in the background** (use the Bash tool's background mode; never append a trailing \`&\`, that detaches and kills it). Pass \`--label <pipeline-name>\` for the header. Use whichever replay flags the script supports (omit unsupported ones):

\`\`\`bash
# The exact replay command depends on the script, adapt to what exists
# Fix initial pass: replay only the target trace, no dataset-id, but tag it with an experiment group
cd <project-dir> && {{command:replayProgress}} --label <pipeline-name> -- npx tsx scripts/replay.ts <pipeline-name> --trace-ids <targetTraceId> --name "<experimentName>" --code-change /tmp/bitfab-code-change-<experimentN>.json --experiment-group-id <experimentGroupId>
# Preferred (supportsDatasetId true): --dataset-id alone replays the dataset; no --trace-ids needed
cd <project-dir> && {{command:replayProgress}} --label <pipeline-name> --run-dir .bitfab/replays/<experimentN>-<timestamp>-<short-random> -- npx tsx scripts/replay.ts <pipeline-name> --dataset-id <datasetId> --name "<experimentName>" --code-change /tmp/bitfab-code-change-<experimentN>.json --experiment-group-id <experimentGroupId>
# Fallback (older script/SDK without dataset-id support): pass the dataset's resolved trace IDs
cd <project-dir> && {{command:replayProgress}} --label <pipeline-name> --run-dir .bitfab/replays/<experimentN>-<timestamp>-<short-random> -- npx tsx scripts/replay.ts <pipeline-name> --trace-ids <id1>,<id2>,<id3>,... --name "<experimentName>" --code-change /tmp/bitfab-code-change-<experimentN>.json --experiment-group-id <experimentGroupId>
\`\`\`

2. **Relay its output to the user as it runs.** Poll the background command's output every few seconds and **show the user each new line it prints, verbatim** (they are already formatted, one line per trace: \`▶ generate-email · 20 traces\`, \`✓ 5/20 · 1.1s\`, \`✗ 7/20 · 0.3s · missing OPENAI_API_KEY\`, \`… 7/20 running · 24s elapsed\` (a heartbeat while a slow trace runs), \`⚠ done · 18 ok · 2 failed · 14.3s · avg 0.9s\`). Do not parse, summarize, or re-decide cadence, just relay. Your relay is bursty (only between tool calls) and the tool card collapses; if the user wants a continuous, never-collapsing view, point them at \`tail -f <logPath>\` from this run's \`run.json\`, which shows every line the instant it is written. (If you are running this inside a parallel experiment subagent, your chat is not shown to the user, so skip relaying and just report at the end.)

3. **Run the live replay/evaluation loop while the replay is still running.** Use the run directory you passed with \`--run-dir\` and poll \`<run-dir>/events.jsonl\` alongside the human output. Each \`type: "progress"\` JSONL row is a normalized SDK progress event; \`item.traceId\` is the **source/original trace id**. Treat a new successful progress row as "this original trace's replay item settled" and start evaluation for that item as soon as the row carries \`item.itemPath\` and a local \`item.replayTraceId\`: read \`item.itemPath\` for the full input, replay output, original output, and metadata. Failed progress rows become unreplayable candidates immediately and should be carried forward with their error string. If the row is progress-only (old SDK/current basic reporter: no \`item.itemPath\`/local replay trace id), do the cheap prep only and defer judging to the final \`type: "complete"\` row's item refs. If the JSONL file is missing or empty (old SDK, old replay script, unwired \`onProgress\`, or an unwritable log path), do not block or treat it as a replay failure: continue from the final complete event exactly as older scripts did from the old result file.

   For every item you can judge during the run, keep the verdict keyed by its **local** replay trace id. Periodically call {{tool:getReplayStatus}} once you know the \`testRunId\` (enriched progress rows may carry \`event.testRunId\`; older scripts may only reveal it in the final \`ReplayResult\`). It returns the current replay test run status plus the local replay trace id → server replay trace id mapping for traces already persisted by the SDK. As soon as a judged item has a server replay trace id in that mapping, persist that small batch with \`{{command:persistReplayLabels}}\`. Keep a set of server replay trace IDs already persisted so the final reconciliation never double-writes a verdict.

4. **When the background command finishes, read the final \`type: "complete"\` row from this run's \`events.jsonl\`**. Its \`result\` carries run metadata (\`testRunId\`, \`testRunUrl\`, \`itemCount\`) and its \`items\` array carries item refs. Read each needed \`items[].itemPath\` for the full replay item (trace ID, duration, tokens, model, and full original/new outputs). Read from the **files**, not from the captured command output, which the harness truncates in the middle.

**Before running: verify the replay script prints the full original and new output values AND the replay trace ID (\`item.traceId\`) for every item** (not just lengths, counts, hashes, or truncated previews) so the run's \`items/*.json\` files carry them. If it doesn't, fix the script first, the Replay Output Contract and example script live in the SDK reference at \`https://docs.bitfab.ai/<language>-sdk.md\`. Subagents can't evaluate an improvement from \`5 → 7 (+2)\`, and missing trace IDs block verdict persistence.

**Capture the \`testRunId\` from the replay output**: the SDK prints it (alongside \`testRunUrl\`) when the run completes. Track every \`testRunId\` produced across all iterations of this phase for the \`open-experiments\` fallback.

**If a child span fails during replay, tag it with \`mockOnReplay\` instead of debugging it.** When a non-root span throws (missing API key for a paid call, flaky external service, deleted/moved dependency, env not reproducible), it usually blocks the whole trace from completing, even though the failure is environmental, not a bug in the function you're iterating on. The short-term fix is to mark that span as replayable from its recorded output:

1. Find the failing span's call site in the codebase (\`withSpan("<spanName>", ...)\` in TS, \`@bitfab.span\` / \`bitfab.span\` equivalents in other SDKs).
2. Add the flag to its span declaration (TypeScript and Python today; Ruby and Go as they land):
   \`\`\`ts
   // TypeScript: SpanOptions.mockOnReplay
   bitfab.withSpan("expensive-llm-call", { mockOnReplay: true }, async () => { ... })
   \`\`\`
   \`\`\`python
   # Python: mock_on_replay kwarg on @client.span(...)
   @client.span("expensive-llm-call", mock_on_replay=True)
   def expensive_llm_call(...):
       ...
   \`\`\`
3. Re-run the replay script passing \`mock: "marked"\` to \`client.replay(...)\` (or \`mock="marked"\` in Python). That child will return its historical output; the root function still runs real code.
4. Flag the tag to the user: it's a replay-only escape hatch, has no effect on prod execution, and is worth removing once the underlying issue is fixed.

Use this when the goal is to unblock iteration on the root function, not when the child itself is what you're trying to improve.

**After the run, check whether replay trace IDs are populated.** Check whether \`item.traceId\` is a non-null string for every completed item. Hold the result as a boolean flag (\`hasTraceIds\`) for the \`check-trace-id-support\` step. If any are \`null\`, the user's SDK version or server does not support the replay trace ID mapping yet. Do NOT stop here, just flag it.

**After the run, classify items before evaluating.** A failed item means one of three things: the new code produced a bad output (real signal), the wrapped fn threw on infra (missing DB row, stale FK, rejected write, missing env), or the recorded inputs no longer fit the function's current signature (the code's SHAPE drifted since the trace was captured: params renamed, reordered, collapsed into an options object, a new required arg added). Infra failures are not regressions; shape mismatches are not regressions either, they mean replay couldn't even call the function with the captured inputs.

From the JSON compute:

- \`completed\`, \`item.error\` unset
- \`shapeErrored\`: \`item.error\` set AND the message reads like a signature/shape mismatch rather than infra. Tell-tale shapes: a \`TypeError\` about reading a property of \`undefined\` / \`X is not a function\` off the input, wrong argument count, Python \`TypeError: ... missing N required positional argument(s)\` / \`unexpected keyword argument\` / \`takes N positional arguments but M were given\`, or a Pydantic/zod validation error on the input itself. These usually fire on (almost) every item identically, because the cause is the signature, not the row.
- \`infraErrored\`: \`item.error\` set for any other (DB/env/external-service-shaped) reason
- \`total\`, \`result.items.length\`; \`0\` or non-zero exit code = whole-replay crash

If \`completed === 0\`, do not score pass/fail on an empty set, branch to \`check-replay-health\`. Carry \`shapeErrored\` forward so \`check-replay-health\` routes shape mismatches to input adaptation instead of burying them as infra noise.`,
                    next: "iterate/check-replay-health",
                },
                {
                    id: "check-replay-health",
                    kind: "branch",
                    toolCalls: ["bash", "read", "edit", "ask"],
                    title: "Check replay health",
                    body: `**Route on the counts and exit code.** Goal: keep infra noise out of evaluation. Read a sample of \`item.error\` strings (and stderr on crash) first to identify the DB-shaped pattern (missing record, FK / unique constraint, write rejected, connection refused, missing env).

**If the errors are signature/shape mismatches (\`shapeErrored\`), that is NOT an infra problem**: it has its own route below (\`adapt-replay-inputs\`), which maps the recorded inputs onto the function's current signature. The DB/infra guidance in the rest of this step applies only to environment-shaped failures.

**🚨 Do not silently work around DB issues.** Do not drop affected trace IDs, stub the read in the script, gate writes behind a script-only flag, wrap the function in a rollback transaction, or edit the instrumented function to skip DB calls. Those all hide infra problems as fake passing or fake failing results and corrupt the experiment.

**Instead: tell the user what's wrong and offer exactly two workarounds.** Use {{askVerb}} to surface a clear summary first, the failing trace ID(s), the error pattern, the function and span where it happens, then present the two options below. Pick a representative failing trace and call {{tool:readTraces}} with \`scope: "summary"\` to read its \`environment\` field (production / staging / development), so option B can name the source environment concretely.

- **Workaround A: \`mockOnReplay\`** *(recommended for spans whose side effects shouldn't run during experimentation)*, apply the \`mockOnReplay\` recipe from step \`replay-against-dataset\` above (find the failing span, add \`mockOnReplay: true\` to its \`SpanOptions\`, re-run with \`{ mock: "marked" }\`). Edit only the span options, never the function body. Use this when the span is a DB read/write the experiment isn't testing and the captured output can stand in for it.
- **Workaround B: Point replay at the trace's source database**: the trace's \`environment\` field names where it was captured (e.g. \`production\`). Tell the user that's the only environment whose DB has the rows the trace references, then offer to (i) update the replay env to point at that environment's DB (env vars, connection string) or (ii) ask which environment they want to use if multiple are valid. Apply the change to env / config, not to the function under test.

After whichever workaround the user picks, re-run \`replay-against-dataset\` and re-check health. If the user can't or won't do either, stop and report, don't fabricate a workaround on your own.

{{whens}}`,
                    branches: [
                        {
                            when: "errors are shape mismatches, not infra (`shapeErrored` dominates the errored items: the recorded inputs don't fit the function's current signature)",
                            description: `the function's shape drifted since these traces were captured, so replay can't call it with the recorded inputs. This is recoverable: route to \`adapt-replay-inputs\` to map the recorded inputs onto the current signature, then re-run`,
                            next: "iterate/adapt-replay-inputs",
                        },
                        {
                            when: "whole replay crashed (non-zero exit, total is 0, or unparseable stdout)",
                            description: `show stderr / exit code, diagnose, confirm a script fix with the user, apply, loop back to \`replay-against-dataset\``,
                            next: "iterate/replay-against-dataset",
                        },
                        {
                            when: "every item errored with INFRA errors (completed is 0, total non-zero, and the errors are NOT predominantly `shapeErrored`, those take the shape-mismatch branch above)",
                            description: `systemic infra failure (usually env mismatch). Diagnose, confirm a script fix with the user, loop back`,
                            next: "iterate/replay-against-dataset",
                        },
                        {
                            when: "high INFRA error rate (over half of items errored, and `shapeErrored` is not the dominant cause, shape mismatches take the branch above)",
                            description: `signal is noisy. Flag the rate and ask the user whether to fix the env and retry, or proceed with the partial signal`,
                            next: "iterate/check-trace-id-support",
                        },
                        {
                            when: "healthy or mixed run (at least one completed item, infra errors at most half of total)",
                            description: `proceed. Carry \`infraErrored\` forward, surface as its own bucket in the final report (the share-results step, or the benchmark scorecard's Unreplayable row in \`benchmark\` mode), never folded into pass/fail`,
                            next: "iterate/check-trace-id-support",
                        },
                    ],
                },
                {
                    id: "adapt-replay-inputs",
                    kind: "branch",
                    toolCalls: ["bash", "read", "write", "edit", "ask", "readTraces"],
                    title: "Adapt recorded inputs to the current signature",
                    body: `**The recorded inputs don't fit the function's current signature.** Replay pulls each trace's inputs exactly as they were captured against the signature AT TRACE TIME, then spreads them into the live function. When the shape drifted since capture, that spread throws (the \`shapeErrored\` items from \`replay-against-dataset\`). The fix is an **input adapter**: a per-trace transform, applied inside the SDK between fetch and call, that reshapes the recorded inputs onto the current signature so replay can run. It is the SDK's \`adaptInputs\` hook (TypeScript \`replay({ adaptInputs })\`) / \`adapt_inputs\` argument (Python \`replay(adapt_inputs=...)\`). You author the transform; the SDK applies it.

**Step 0: confirm the capability.** Check the \`supportsInputAdapters\` flag from \`detect-replay-capabilities\` (the installed SDK accepts the \`adaptInputs\` / \`adapt_inputs\` option on \`replay()\`). If true, go to Step 1. If false, the installed SDK predates the input-adapter hook: tell the user to upgrade the SDK and re-run the assistant, and for this run take the **decline** branch below (these traces can't be validated without the hook). Do not hand-roll the reshape inside the function under test.

**Step 1: reuse a committed adapter if one already covers this shape (re-ask only on drift).** Adapters live in their own file next to the replay script, imported by it (recommended: TS \`scripts/replay-adapters/<name>.ts\`, Python \`scripts/replay_adapters/<name>.py\`, Ruby \`scripts/replay_adapters/<name>.rb\`; plus an optional sibling \`<name>.inputs.json\` for judgement cases). If such an adapter already exists AND maps the current recorded shapes onto the current signature (sanity-check it against a sampled input below), do not re-prompt: just confirm the replay script imports it (Step 5) and re-run. Only when the signature has drifted past what it handles do you propose a new/updated mapping and re-confirm.

**Step 2: learn the actual captured shape(s).** Pick a few \`shapeErrored\` trace IDs and call {{tool:readTraces}} with \`scope: "full"\` to read their recorded inputs. Sample MORE than one: the trace set can span several historical signatures (the function may have drifted more than once), so the adapter must tolerate each shape it actually sees, not just the newest old one. Then read the function's CURRENT signature from the code.

**Step 3: decide mechanical vs judgement.**
- **Mechanical** (rename, reorder, positional-to-options-object, drop a removed param, supply a literal/default): expressible as a pure function. Prefer this.
- **Judgement** (the new shape can't be derived by rearrangement, e.g. one freeform field must be split into two based on its content): do NOT call a model from inside the adapter at replay time (that makes replay slow and non-deterministic). Instead, materialize up-front: YOU compute the adapted inputs for each affected trace now, once, and write them to the sibling \`<key>.inputs.json\` table keyed by the original Bitfab trace ID. The adapter then just looks the trace up.

**Step 4: confirm, then write the adapter co-located with the replay script.** Use {{askVerb}} to show the concrete mapping (old shape -> new shape) and get a yes before writing. On yes, write:
- The adapter function. It receives the recorded inputs and a per-trace context (\`{ traceId, sourceSpanId }\` in TS; \`{"trace_id", "source_span_id"}\` in Python) and returns the args actually passed to the function. Make it **shape-dispatching**: branch on the input it actually receives so it normalizes each historical shape in the sample, not only one. For judgement cases, look up \`<key>.inputs.json\` by \`traceId\` first, then fall back to the mechanical branch.
- **Faithfulness is non-negotiable.** If the current signature has a genuinely new REQUIRED input with no analog in the recorded trace, do NOT invent a value. Leave those traces unmapped; they go in the decline bucket below with a stated reason. Adapting must never silently fabricate test inputs.
- These files are committed (they sit next to the replay script, in source control), so they persist across runs and are reviewable in the PR.

**Step 5: wire it in and re-run.** Write the adapter to its own file next to the replay script (TS \`scripts/replay-adapters/<name>.ts\` exporting \`adaptInputs\`; Python \`scripts/replay_adapters/<name>.py\` defining \`adapt_inputs(args, kwargs, ctx)\`; Ruby \`scripts/replay_adapters/<name>.rb\` defining an adapter lambda). Then edit the replay script to import it and pass it to this pipeline's \`replay()\` call as \`adaptInputs\` / \`adapt_inputs\` (see the Replay section for the exact import shape). Editing the replay script here is expected. Loop back to \`replay-against-dataset\`, re-run, and confirm the \`shapeErrored\` items cleared.

{{whens}}`,
                    branches: [
                        {
                            when: "an adapter is in place (user approved a new mapping, or a persisted adapter already covers the current shape) and the replay script loads it",
                            description: `re-run with the adapter applied. Loop back to \`replay-against-dataset\``,
                            next: "iterate/replay-against-dataset",
                        },
                        {
                            when: "the SDK lacks the hook (`supportsInputAdapters` false), the user declines adapting, or some inputs can't be faithfully mapped (new required input with no analog)",
                            description: `do not fabricate inputs. Carry the unmappable \`shapeErrored\` trace IDs as their own **shape-incompatible** bucket (each with a one-line reason), distinct from infra errors and never scored pass/fail/regression, and surface it in the final report (share-results, or the benchmark scorecard's Unreplayable row). If any items DID complete (partial adaptation), proceed to evaluate them; otherwise this is a terminal report path`,
                            next: "iterate/check-trace-id-support",
                        },
                    ],
                },
                {
                    id: "check-trace-id-support",
                    kind: "branch",
                    toolCalls: ["bash", "ask"],
                    title: "Check replay trace ID support",
                    body: `**Route on whether replay trace IDs are available.** Check the \`hasTraceIds\` flag from \`replay-against-dataset\` (this confirms the tentative \`supportsReplayTraceIds\` flag from \`detect-replay-capabilities\`). This determines whether verdicts can be persisted to the server and whether the experiments page in Studio will show meaningful results.

{{whens}}`,
                    branches: [
                        {
                            when: "replay trace IDs are populated (`hasTraceIds` is true)",
                            description: 'the SDK and server support trace ID mapping. In non-benchmark modes, open the experiments page in Studio first (so the user can watch verdicts populate in real time), then evaluate and persist labels. In `fix` mode with `fixReplayScope = "single-trace"`, the replay has an experiment group but do not open Studio yet; `open-experiments` self-skips so the single trace can be evaluated and persisted first, then `fix-target-replay-status` branches on whether the target passed or failed. In `benchmark` mode without the `studio` flag no Studio is open, so `open-experiments` self-skips: go straight to evaluating and persisting labels. In `benchmark` mode with the `studio` flag, `open-experiments` behaves like other modes',
                            next: "iterate/open-experiments",
                        },
                        {
                            when: "replay trace IDs are null (`hasTraceIds` is false)",
                            description: "tell the user: \"Your SDK doesn't support replay trace IDs, so experiment results can't be persisted to Studio or compared across iterations. Upgrade your SDK and run `{{cmd}}setup replay` to regenerate the script. Evaluating in-agent for now.\" Then proceed to text-only evaluation so the user still sees comparison results in-agent, without the Studio experiments page",
                            next: "iterate/evaluate-results-text-only",
                        },
                    ],
                },
                {
                    id: "evaluate-results-text-only",
                    kind: "action",
                    toolCalls: ["read"],
                    title: "Evaluate results (text only)",
                    emit: "Evaluating results",
                    body: `**Run only when replay trace IDs are unavailable** (\`hasTraceIds\` is false, you were routed here from \`check-trace-id-support\`; if trace IDs are available, use \`evaluate-results\` instead). **Evaluate results in-agent without persisting.** The agent still compares original vs new outputs and derives pass/fail verdicts, but cannot persist them via \`persistReplayLabels.js\` or show them in Studio. This is a terminal path: it does NOT continue to \`evaluate-results\` or \`verify-replay-labels\`; its \`next\` goes straight to the report (share-results, or the benchmark scorecard).

If \`replay-against-dataset\` already consumed a non-empty run \`events.jsonl\`, reuse that progress-derived work and any verdicts already produced from item files referenced by progress rows. Successful progress events identify which original traces settled, and failed events already define unreplayable candidates. Do not reclassify those failures as output regressions. If the progress file is missing or empty, fall back to the completed replay output; this is expected for old SDKs or replay scripts without \`onProgress\`. Since there are no server replay trace IDs on this path, all verdicts here stay in working context only.

For each completed (non-errored) replay item, derive a verdict by comparing the replay's new output against the original trace's label and annotation:

- **fail**-labeled original: does the replay's new output address the annotation? If yes, mark as PASS. If no, mark as FAIL.
- **pass**-labeled original: preserved means PASS, regressed means FAIL.
- Unreplayable items (\`item.error\` set) go in their own bucket.

Hold the verdicts in working context for the final report, the \`share-results\` step in \`wizard\`/\`dataset\`/\`experiment\`/\`investigate\` modes, or the **benchmark scorecard** in \`benchmark\` mode. This step's \`next\` routes there directly: it does NOT run the \`evaluate-results\` (persist) or \`verify-replay-labels\` steps. Since trace IDs are unavailable, do NOT attempt to run \`persistReplayLabels.js\` or open the experiments page; the report is the terminal step from here.`,
                    next: {
                        byMode: {
                            wizard: "iterate/share-results",
                            dataset: "iterate/share-results",
                            experiment: "iterate/share-results",
                            fix: "iterate/fix-target-replay-status",
                            "cost-optimize": "iterate/share-results",
                            investigate: "iterate/share-results",
                            benchmark: "benchmark/scorecard",
                            "add-trace": null,
                            replay: null,
                        },
                    },
                },
                {
                    id: "evaluate-results",
                    kind: "action",
                    toolCalls: ["read", "write", "bash", "agent"],
                    commandCalls: ["persistReplayLabels", "readTracesBatched"],
                    title: "Evaluate against labels & annotations",
                    emit: "Evaluating results",
                    body: `**Evaluate against labels & annotations.** Score only items where \`item.error\` is unset. Items with \`item.error\` set are unreplayable (already classified) and go in their own bucket, never pass, fail, or regression.

> 🚨 **MANDATORY, NOT A JUDGMENT CALL:** Reaching this step means replay trace IDs are available, so you MUST derive **and persist** a verdict (or one of the three explicit non-verdicts below) for **every** completed (\`item.error\` unset) replay item. \`item.error\` unset means "score this item against its criteria", it does NOT mean "it passed". Persisting is not optional, you do not get to decide a run "isn't worth scoring", "looks like noise", or "would pollute the dataset" and jump to the report with an empty scorecard. There is no "this whole run isn't meaningful, so skip labeling" path: that path does not exist, and \`verify-replay-labels\` will route you back if the persisted set doesn't cover every completed item.

**The verdict is decided by the output against its criteria, never by your opinion of how informative, useful, or production-like the run is.** Environmental doubts, a mock/stub model, no real gateway, flaky infra, an output that "looks like noise", a worry that the labels would pollute the dataset, are **caveats only**: write them into the \`annotation\` and lower the \`confidence\` (e.g. \`Low\` / \`VeryLow\`). They never suppress a verdict and never skip this step. A low-confidence verdict with a caveat is still a verdict and still gets persisted.

**Finalize the live evaluation loop; do not start from scratch.** If \`replay-against-dataset\` produced a non-empty run \`events.jsonl\`, read it before judging and reuse any prep, unreplayable buckets, verdicts, and persisted server replay trace IDs already produced during the running replay. Each progress event's \`item.traceId\` is the source/original trace id; use it to identify which dataset item settled. Enriched progress rows carry \`item.replayTraceId\` and \`item.itemPath\`; the item file has the full input, replay output, original output, and metadata needed to judge during replay. Progress-only rows are only a trigger/prep signal. If the file is missing or empty, continue from the final complete event; older scripts remain valid and simply do not get incremental evaluation. The final verdict still comes from the completed replay item files / experiment trace data, and persistence is still keyed by the **server replay** trace id.

When possible, evaluate and persist in small completed batches rather than waiting to judge every item at once: every time a progress event gives you a replay item ref with a replay trace id, full item file, and its original label/annotation, read the item file, derive that item's verdict, and append it to the pending verdict set. Once a batch has complete coverage for its expected replay trace ids, call \`persistReplayLabels.js\` for that batch. If the current replay script/SDK only exposes replay outputs at the end, use progress during the run for preparation and failed-item bucketing, then do the first persist immediately after this run's \`complete\` row appears. At the end of the replay, run the same coverage gate over the complete row's item refs; any item not already persisted must be judged and persisted before continuing. This keeps Studio's experiment view filling in as early as the available data allows while preserving the same final correctness checks.

For each completed (non-errored) replay item, derive a verdict by comparing the replay's new output against the original trace's label and annotation (from Phase 3 in \`wizard\`/\`dataset\` modes, loaded by \`pick-dataset\` in Phase 5 Setup in \`experiment\` and \`benchmark\` modes; in \`fix\` mode's single-trace pass, the failure annotation held from Phase Fix \`resolve\` is the criterion, and on a later full-dataset run the added trace plus any pre-existing dataset siblings loaded by \`fix-add-to-dataset\`):

- **fail**-labeled original: does the replay's new output address the annotation? If yes → \`label: true\` (PASS). If no → \`label: false\` (FAIL). Use the annotation as the acceptance criterion.
- **pass**-labeled original: preserved → \`label: true\` (PASS). regressed → \`label: false\` (FAIL).

**The ONLY permitted non-verdicts for a completed item are these three enumerated skip cases.** Each is per-item and must be accounted for explicitly with a \`{skip: true}\` entry. Nothing else, and never "the whole run isn't worth scoring", justifies omitting a completed item:

- **Unlabeled original** (no validated or agent label, possible in \`benchmark\` mode, where the dataset only needs ≥1 trace regardless of label mix): there is no acceptance criterion to score against, so do NOT pass/fail it. Mark it \`skip: true\` and note "unlabeled, no expected result", it counts toward \`S\` (skipped) and is excluded from \`scorable\`, never pass or fail.
- **Fail-labeled original with an unusable annotation** (the label exists and says FAIL, but the annotation is empty or contentless, e.g. "bad", "wrong", "fix this", so it states no criterion for what "fixed" looks like): the fail label tells you the direction but not what to check the new output against. This skip is narrow and applies ONLY when BOTH hold: (a) the original is **fail**-labeled (a pass-labeled original never needs the annotation, you score it by preserved-vs-regressed against the original output, so this case does not apply), and (b) you **cannot reconstruct the defect** from the code, the function's intent, or the original's own output. If you can recover what was wrong from any of those, you have a criterion, so verdict it (a thin-but-usable annotation like "returns null instead of the email" is NOT unusable). Only when the criterion is genuinely unrecoverable: \`skip: true\` and note "fail label, annotation states no checkable criterion". This is not an escape for annotations you simply find terse or inconvenient.
- **Genuinely ambiguous output** (you read the output and the code and still cannot judge it, not laziness, not an environmental doubt, which is a caveat above, not a skip): \`skip: true\` instead of guessing. Skips are recorded explicitly so the verify step knows you intentionally did not verdict.

**Unreplayable items** (\`item.error\` set) are not completed items at all, so they are excluded from the scope above and are NOT verdicted here (no \`{skip: true}\` entry either). Keep their list (trace ID + error string) for the final report (the \`share-results\` step in \`wizard\`/\`dataset\`/\`experiment\`/\`investigate\` modes, or the benchmark scorecard's Unreplayable row in \`benchmark\` mode). Carry the skipped list forward the same way.

**When \`costRun\` is set, also capture the token delta for each completed item.** The run's goal is fewer tokens, so a verdict is not finished without the cost direction. **First make sure each original's recorded token usage is actually in your context.** It normally arrives with the dataset load, but if build-dataset loaded this dataset via {{tool:readTraceLabels}} (\`costRun\` was false at build time and only flipped on afterward), that load carried labels but no token usage: read the dataset's original trace IDs once by running \`{{command:readTracesBatched}} <trace-id...> --scope summary\` (\`summary\` is enough for the token totals) and \`Read\` the \`outputFile\` it prints, before computing deltas. This is a cheap read, not a re-replay, so do it before the missing-usage recovery ladder below. Then compare each original trace's recorded token usage against the replay item's \`tokens\` from the replay output: per item, record input and output tokens baseline (original) vs new (replay) and the % change. Carry the per-item deltas plus the dataset totals (baseline vs new input/output tokens and overall % change) forward for the report. A pass that costs more tokens is a cost regression even when the output is still correct: flag it rather than burying it under a green verdict. (If the replay output reports only total tokens, report the total delta; if it splits input and output, report both.) **Compute the delta on the basis the run carries (\`costBasis\`), so the chat numbers match the page lens.** On \`costBasis = uncached\`, subtract each side's cached input first: the per-side figure is \`(input - cached) + output\`, taking \`cached\` from the original's recorded usage and from the replay item's \`tokens\` (which carries \`input\`, \`output\`, and \`cached\`), and label the reported figures "uncached" so they read the same as the cyan/orange page trend. An item with no cached tokens then equals \`input + output\` on its own, exactly as the page shows it. On \`costBasis = all\` (the default), report \`input + output\` with no subtraction. Carry the per-item figures forward already on the chosen basis so \`share-results\` and the scorecard inherit it without re-deciding.

**Never run a separate baseline replay arm.** The baseline is the original traces' recorded usage, full stop. There is exactly one replay per item (the changed-code arm). Do not replay the original/unchanged code to "establish a baseline"; the original trace already is that measurement. The only exception is the missing-usage recovery below, and even that re-runs only the affected items, never the whole dataset.

**Handle originals that aren't a usable baseline.** An original trace is not a trustworthy baseline if it errored, has a failed or incomplete status, or recorded no token usage. In any of those cases its numbers don't reflect a clean run of the unchanged code. Re-replaying the unchanged code is expensive and is the LAST resort, never the first move. For each such item, walk this order and stop at the first step that yields a baseline:

1. **Reuse a recorded run.** Check whether any other already-recorded trace or prior replay of the same item ran the unchanged code cleanly and has usage (e.g. an earlier good trace for the same input, or a prior baseline from an earlier iteration). If so, use it. No new run.
2. **Backfill only the truly unrecoverable items.** Only if no clean recorded usage exists anywhere for that item, re-replay the unchanged code for that one item.

Keep re-replays to the minimum: only items that fail step 1, one replay each, never a full baseline arm across the dataset. If even step 2 errors or yields no usage, report that item's delta as uncomputable ("no baseline, original did not complete cleanly") instead of guessing.

**The verdict you produce here is persisted onto the REPLAY trace IDs (not the originals).** That's what makes "did this fix actually pass on replay?" queryable across iterations.

${FAN_OUT_JUDGING}

**Per-item inputs for this step (however you produce the verdicts):** each completed replay item's own artifacts, the original trace's input, its original output, its label and annotation, and the replay's new output (and, when \`costRun\` is set, the original's recorded token usage plus the replay item's \`tokens\` for that item's baseline-vs-new token figures). The fixed rubric is exactly the fail-labeled / pass-labeled / unlabeled / ambiguous rules above, and each verdict is keyed by the **replay** trace ID in the shape below (\`{ traceId, label, annotation, confidence? }\` or \`{ skip: true }\`). {{#claude}}When you fan out, each subagent's prompt carries its batch's per-item inputs and returns that batch's verdict entries, which you concatenate into that batch's \`verdicts\` array. {{/claude}}However the verdicts are produced, you keep the unreplayable (\`item.error\`) and skipped buckets exactly as above, apply \`costBasis\` and roll up the dataset token totals yourself (those are cross-item and stay with you), then persist each complete batch with \`persistReplayLabels.js\` and keep a set of replay trace IDs already persisted. The final full-run coverage check below must account for every completed replay trace exactly once.{{#claude}}

**Exception that overrides the fan-out block above, parallel-worktree experiment mode:** if you ARE the worktree subagent running this experiment (parallel mode from \`pick-execution-mode\`), ignore the serial-vs-fan-out count rule and judge your items inline regardless of count, a subagent does not spawn its own judging subagents. Then hand the collected result back to the main agent as described below. The fan-out block above governs only when this step runs in the main agent itself: serial execution, benchmark, or the wizard / dataset / investigate modes.{{/claude}}

**Persist via \`persistReplayLabels.js\`.** Write verdicts to a tmp JSON file then run the script. You may do this incrementally for complete batches as replay results arrive, and again at the end for any remaining replay trace IDs that were not already persisted. Each call is one batched MCP call server-side, and the file is auto-deleted on success:

1. Pick an **absolute** tmp path. The script reads the file relative to its own process cwd, which in parallel-worktree mode is NOT the project root, so a relative path can resolve to a different directory than where you wrote the file (\`ENOENT\`). Recommended: \`<repoRoot>/.bitfab/tmp/verdicts-<testRunId>.json\` where \`<repoRoot>\` is the output of \`git rev-parse --show-toplevel\` (create the dir if missing). Falls back to an absolute path under \`os.tmpdir()\` if the project root isn't writable.
2. Use the {{tool:write}} tool to write JSON of this exact shape:

\`\`\`json
{
  "expectedTraceIds": ["<replayTraceId1>", "<replayTraceId2>", "..."],
  "verdicts": [
    { "traceId": "<replayTraceId1>", "label": true, "annotation": "Now returns the missing field; original annotation said it was empty.", "confidence": "High" },
    { "traceId": "<replayTraceId2>", "label": false, "annotation": "Output still hallucinates a tool argument.", "confidence": "VeryHigh" },
    { "traceId": "<replayTraceId3>", "skip": true }
  ]
}
\`\`\`

\`expectedTraceIds\` MUST be the full set of REPLAY trace IDs covered by this call's batch (and across all batches, every completed \`item.error\`-unset replay trace must be persisted exactly once, no fewer, per the mandatory-coverage rule above). For the final end-of-run call, use only the remaining completed replay trace IDs that were not already successfully persisted by an earlier batch. \`verdicts\` MUST have one entry per ID, either a \`{label, annotation, confidence?}\` verdict or a \`{skip: true}\` explicit skip (skips allowed only for the three enumerated skip cases above, never for an environmental doubt). \`confidence\` is optional but recommended (\`VeryLow|Low|Medium|High|VeryHigh\`); it surfaces in the labeling UI so reviewers can prioritize low-confidence verdicts. If verdict counts don't match \`expectedTraceIds\`, the script returns \`status: "missing-coverage"\` and the verify step routes you back to fill the gaps.

3. Run the script:

\`\`\`bash
{{command:persistReplayLabels}} <repoRoot>/.bitfab/tmp/verdicts-<testRunId>.json
\`\`\`

4. Read its single JSON line on stdout. Hold the parsed result for the next step.

**Spill working notes to a separate tmp file if context gets big.** Don't conflate working notes with the verdicts file, the script deletes the verdicts file on success.{{#claude}}

**If you're a worktree subagent** (parallel mode from \`pick-execution-mode\`): after the script returns, hand the parsed result + \`testRunId\` + unreplayable list back to the main agent and exit. The main agent collects results from all parallel experiments before \`open-experiments\`.{{/claude}}`,
                    next: "iterate/verify-replay-labels",
                },
                {
                    id: "verify-replay-labels",
                    kind: "branch",
                    toolCalls: ["read", "write", "bash"],
                    commandCalls: ["readTracesBatched"],
                    title: "Verify replay labels persisted",
                    body: `**Verify replay labels persisted.** Route on the \`status\` field of the JSON the script printed in \`evaluate-results\`. The script is the deterministic gate, if it didn't return \`ok\`, the agent's verdicts are NOT yet on the replay traces and the experiment delta will be wrong on the next iteration.

**This is the hard gate into the report: \`share-results\` and the benchmark scorecard are unreachable without a \`status: "ok"\` here.** \`status: "ok"\` only proves the script's \`expectedTraceIds\` were all verdicted; it does NOT prove you handed the script every completed item. Before you treat \`ok\` as a pass, **cross-check the coverage yourself**: \`persistedTraceIds\` (plus the \`item.error\` unreplayable bucket) MUST account for every item in the replay output. If any completed (\`item.error\` unset) replay item is missing from the persisted set, you under-scoped \`expectedTraceIds\` (most often by deciding the run "wasn't worth scoring") and the scorecard would be silently empty or partial: go back to \`evaluate-results\`, rebuild the verdicts file covering ALL completed items, and re-persist before routing onward.

{{whens}}`,
                    branches: [
                        {
                            when: '`status: "ok"` AND `persistedTraceIds` (plus the unreplayable `item.error` bucket) account for every completed replay item',
                            description: "labels are persisted on the replay traces and the verdicts file is gone. In `benchmark` mode continue to the benchmark scorecard (a terminal report, no iteration); in all other modes continue to share-results (experiments page was already opened before evaluation)",
                            next: {
                                byMode: {
                                    wizard: "iterate/share-results",
                                    dataset: "iterate/share-results",
                                    experiment: "iterate/share-results",
                                    fix: "iterate/fix-target-replay-status",
                                    "cost-optimize": "iterate/share-results",
                                    investigate: "iterate/share-results",
                                    benchmark: "benchmark/scorecard",
                                    "add-trace": null,
                                    replay: null,
                                },
                            },
                        },
                        {
                            when: '`status: "ok"` but a completed (`item.error` unset) replay item is absent from `persistedTraceIds` (your own coverage cross-check failed: `expectedTraceIds` was under-scoped)',
                            description: "the script passed only because you handed it a truncated `expectedTraceIds`; the report would be silently empty or partial. Go back to `evaluate-results`, rebuild the verdicts file so `expectedTraceIds` is the full set of completed replay items, and re-persist. Do NOT proceed to the report",
                            next: "iterate/evaluate-results",
                        },
                        {
                            when: '`status: "missing-coverage"` (script returned a non-empty `missingTraceIds` array)',
                            description: "you under-verdicted. Read the missing replay trace IDs (run `{{command:readTracesBatched}} <trace-id...> --scope full` and `Read` its `outputFile` if you didn't already, since you are persisting pass/fail verdicts on their span content), decide each one (PASS / FAIL with annotation, or `skip: true` if genuinely ambiguous), write a NEW verdicts file at the same path covering ALL the originally expected IDs (the script needs the full `expectedTraceIds` list each call, not just the gaps), and re-run the script. Loop back here with the new result",
                            next: "iterate/verify-replay-labels",
                        },
                        {
                            when: '`status: "invalid-input"` (malformed verdicts JSON or missing fields)',
                            description: "the verdicts file you wrote doesn't match the schema. Read the script's `message` field, fix the JSON (most common: missing annotation on a non-skip entry, missing traceId, expectedTraceIds empty), and re-run the script. Loop back here",
                            next: "iterate/verify-replay-labels",
                        },
                        {
                            when: '`status: "mcp-error"` (MCP call to update_agent_labels failed mid-batch, including invalid trace IDs)',
                            description: "Read the script's `message` field. If it says the trace IDs are invalid or not in this organization, you likely used local replay trace IDs from item files; call {{tool:getReplayStatus}}, rebuild the verdicts file with the mapped server replay trace IDs, and re-run the script. For network or auth errors, the script's `partialTraceIds` lists which IDs were already persisted; tell the user, recommend re-running the script (it's idempotent, already-persisted labels just upsert), and loop back here. If it keeps failing, stop and surface the error",
                            next: "iterate/verify-replay-labels",
                        },
                    ],
                },
                {
                    id: "open-experiments",
                    kind: "action",
                    toolCalls: ["bash", "listExperiments", "getExperimentTraces"],
                    commandCalls: ["openStudio"],
                    title: "Open experiment viewer",
                    body: `**Open experiment viewer (fallback).** This step only runs when replay trace IDs are available (routed here from \`check-trace-id-support\`). If no \`testRunId\`s were captured, skip this step and continue to evaluate.

**In \`fix\` mode with \`fixReplayScope = "single-trace"\`, skip this step entirely.** Studio should not open automatically for the targeted first replay, even though the replay is tagged with \`fixSingleTraceExperimentGroupId\`. Continue directly to \`evaluate-results\`; after labels are persisted, \`fix-target-replay-status\` branches on whether the target passed or failed before asking what to do next.

**In \`fix\` mode with \`fixReplayScope = "dataset"\` and \`fixDatasetStudio\` false (the user chose a terminal-only re-run), also skip this step entirely** (no Studio is open). Continue to \`evaluate-results\`; the dataset results are reported in chat by \`share-results\`.

**In \`benchmark\` mode without the \`studio\` flag, skip this step entirely** (no Studio is open). With the \`studio\` flag, benchmark behaves like the other modes below: skip if the experiments page was already opened via \`experimentGroupId\` in \`open-experiments-before-replay\`, otherwise navigate with the collected \`testRunId\`s. Either way this step's \`next\` goes to \`evaluate-results\`, which in benchmark mode scores the items, persists verdicts, and then routes to the terminal benchmark scorecard. (The numbered position of this step in the rendered list does not reflect run order: follow the \`next\` routing, not the list sequence.)

If the experiments page was already opened via \`experimentGroupId\` in \`open-experiments-before-replay\` (\`supportsExperimentGroups\` is true), skip this step entirely, the page is already showing live results.

If \`supportsExperimentGroups\` is false, navigate Studio to the experiments page. Build the path with **every** \`testRunId\` you've collected across iterations of this phase (comma-separated):

\`\`\`bash
{{command:openStudio}} "/studio/experiments?testRunIds=<testRunId1>,<testRunId2>,<testRunId3>"
\`\`\`

In \`benchmark\` mode (with the \`studio\` flag), append \`&mode=benchmark\` here too so the page shows benchmark terminology. Likewise, when \`costRun\` is set (classified in argument routing; see the token-cost lens note in \`open-experiments-before-replay\`), append the token-cost suffix (always with an explicit basis) so the token-cost columns show: \`&tokens=1&tokenType=all\` for \`costBasis = all\`, or \`&tokens=1&tokenType=uncached\` for \`costBasis = uncached\`, e.g. \`/studio/experiments?testRunIds=<testRunId1>,<testRunId2>&tokens=1&tokenType=uncached\`. The command navigates an existing session or opens a new one automatically.`,
                    next: "iterate/evaluate-results",
                },
                {
                    id: "fix-target-replay-status",
                    kind: "branch",
                    title: "Split on the target trace replay result",
                    body: `**Run only in \`fix\` mode.** This step is reached after replay verdicts were evaluated for a fix run.

Route on the replay scope and the targeted trace's result. This split matters: if the targeted experiment failed, do not present "run full dataset" as the normal next step. The useful next action is usually to inspect the before/after or keep iterating on the target trace.

{{whens}}`,
                    branches: [
                        {
                            when: '`fixReplayScope = "dataset"` (the user already chose the full-dataset experiment)',
                            description: "continue directly to share-results",
                            next: "iterate/share-results",
                        },
                        {
                            when: "the targeted single-trace replay verdict is PASS (`label: true`)",
                            description: "the target trace went green. Now add it to a dataset, then ask whether to re-run the full dataset, inspect the before/after, keep iterating, or stop",
                            next: "iterate/fix-add-to-dataset",
                        },
                        {
                            when: "the targeted single-trace replay verdict is FAIL (`label: false`), skipped, unreplayable, shape-incompatible, or could not be persisted because replay trace IDs were unavailable",
                            description: "the fix did not prove the target trace green this pass. If `fixAddedToDataset` is not set, nothing is in a dataset yet; if it IS set (an earlier pass already added the trace to the dataset and a later iteration regressed it), the saved dataset trace is still there and this iteration just regressed it. Ask whether to inspect the before/after, keep iterating, or stop",
                            next: "iterate/fix-single-trace-failed",
                        },
                    ],
                },
                {
                    id: "fix-add-to-dataset",
                    kind: "action",
                    toolCalls: [
                        "read",
                        "readTraces",
                        "setHumanLabels",
                        "listDatasets",
                        "createDataset",
                        "addTracesToDataset",
                        "bash",
                        "ask",
                    ],
                    commandCalls: ["readTracesBatched"],
                    title: "Add the now-green scenario to a dataset",
                    emit: "Adding the scenario to a dataset",
                    body: `**Run only in \`fix\` mode, reached after the targeted single-trace replay passed.** This step adds the now-green scenario to a dataset with a validated failing label. It is **re-entered** whenever the passed prompt loops back (the user opened the before/after, or kept iterating and the trace passed again), so it must add the trace exactly **once**.

**Idempotency guard, check this first.** If you already added this trace to a dataset earlier in this fix session (\`fixAddedToDataset\` is set, i.e. a \`datasetId\` is held and the target trace is attached to it), do **not** label, pick a dataset, attach, or re-announce anything. Skip straight to \`fix-single-trace-passed\`. Only run the add-to-dataset steps below the first time the target trace turns green.

Otherwise add it now, in order:

1. **Label the original trace as a validated fail.** Using the failure annotation you wrote in Phase Fix \`resolve\` (what the original output got wrong and what correct behavior is), call {{tool:setHumanLabels}} once with \`[{ traceId, label: false, annotation }]\` on the **original** trace. This records a **validated human verdict** (not an unapproved agent suggestion), so the trace counts as a real regression test the instant it's written, with no Studio approval step. Use \`label: false\` because the original output was the bug; the fix is what now turns it green on replay. (On the rare occasion the user is adding an already-*passing* trace to guard against future regressions, use \`label: true\`.)
2. **Pick or create a dataset (minimize prompts).** If the user named a dataset ID, use it (first confirm it's scoped to this function via {{tool:listDatasets}}; if it belongs to a different function, {{tool:addTracesToDataset}} would silently skip every trace, so {{askVerb}} and offer a correctly-scoped dataset instead). Otherwise call {{tool:listDatasets}} with the function key and pick without asking when there's no real choice:
   - **None exist:** silently call {{tool:createDataset}} with \`traceFunctionKey: <key>\` and \`name: <key>\`.
   - **Exactly one exists:** use it silently (no prompt); you'll name it in the one-line confirmation below.
   - **Two or more exist:** present them {{presentMode}} (one option per dataset: name · id · count, plus "Create new"), recommend the most recently used dataset that has traces.

   Hold the \`datasetId\`.
3. **Attach the trace.** Call {{tool:addTracesToDataset}} with the \`datasetId\` and \`[traceId]\` (idempotent, a safe no-op if already present). **Confirm the attach landed**: the trace is in the dataset when it was added now or was already present. If the tool reports it skipped the trace (e.g. a scope mismatch), do **not** claim it was added or set \`fixAddedToDataset\`, re-pick a dataset scoped to this function and attach again before continuing.
4. **Record whether the dataset has siblings, and load their labels if so.** Note whether the dataset already held other traces besides the one you just attached, and hold that as \`datasetHasSiblings\` (the next step uses it to decide whether re-running the whole dataset is meaningful). If there **are** siblings, the user may next choose to re-run the full dataset, so run \`{{command:readTracesBatched}} <trace-id...> --scope full\` over the dataset's trace IDs and \`Read\` the \`outputFile\` it prints, so every sibling's validated label + annotation is in context for that experiment. **Skip the load** when you just created the dataset or it held only this trace (no siblings).

Set \`fixAddedToDataset = true\` (only once the attach above is confirmed) so any re-entry of this step skips the add-to-dataset work. Tell the user in one line that the now-passing scenario was added to dataset \`<name>\` (\`<datasetId>\`) with a validated failing label, so it is included whenever that dataset runs. Then continue to \`fix-single-trace-passed\`.`,
                    next: "iterate/fix-single-trace-passed",
                },
                {
                    id: "fix-single-trace-passed",
                    kind: "branch",
                    toolCalls: ["ask"],
                    title: "Ask whether to re-run the full dataset after the target passed",
                    body: `**Run only in \`fix\` mode when \`fixReplayScope = "single-trace"\` and the targeted replay passed.** \`fix-add-to-dataset\` has just added the target trace to dataset \`<name>\` (\`<datasetId>\`) with a validated failing label (on a re-entry, the add already happened earlier).

Report the result first. Lead with that the target trace went green and is now saved in the dataset with a validated failing label; this pass intentionally did not run the rest of the dataset and did not open Studio. The replay labels have been persisted when trace IDs were available, so this fix is queryable on the replay trace.

Then ask what to do next. One option always lets the user **show this fix in Studio** (the single-trace before/after). **Gate the full-dataset option on \`datasetHasSiblings\` (from \`fix-add-to-dataset\`):**
- **The dataset has sibling traces:** the headline choice is whether to re-run the entire dataset now to check the fix against every trace in it (you'll then ask Studio vs terminal-only). Present all four options below and recommend **"Re-run the entire dataset"**.
- **The added trace is the only trace in the dataset (no siblings):** do **NOT** offer "Re-run the entire dataset", re-running a one-trace dataset would just replay the trace you already proved green. Present only "Show in Studio", "Keep iterating", and "Stop and wrap up", and recommend **"Stop and wrap up"**.

{{branches}}`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Re-run the entire dataset",
                                description: "**Only include this option in the question when `datasetHasSiblings` is true; omit it entirely when the added trace is the dataset's only trace** (re-running a one-trace dataset just replays the trace you already proved green). When included, continue to `fix-rerun-dataset-mode`, which asks whether to run it in Studio or terminal-only before replaying the same code change across the full dataset",
                            },
                            next: "iterate/fix-rerun-dataset-mode",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Show in Studio",
                                description: "open Studio to the targeted single-trace before/after experiment using `fixSingleTraceExperimentGroupId` (or `testRunIds` fallback), then ask this question again",
                            },
                            next: "iterate/fix-open-single-trace-studio",
                        },
                        {
                            option: {
                                letter: "C",
                                label: "Keep iterating",
                                description: 'set `fixReplayScope = "single-trace"`, set `fixSkipMakeChange = false`, and make another change against the target trace',
                            },
                            next: "iterate/open-experiments-before-replay",
                        },
                        {
                            option: {
                                letter: "D",
                                label: "Stop and wrap up",
                                description: "leave the saved dataset trace in place",
                            },
                            next: "wrap-up/summary",
                        },
                    ],
                },
                {
                    id: "fix-rerun-dataset-mode",
                    kind: "branch",
                    toolCalls: ["ask"],
                    title: "Re-run the dataset in Studio or terminal-only",
                    body: `**Run only in \`fix\` mode when the user chose "Re-run the entire dataset" from \`fix-single-trace-passed\`.** The same already-made fix will replay across every trace in the dataset. Ask whether to watch it live in Studio or run it terminal-only (results reported in chat). Either way, set \`fixReplayScope = "dataset"\` and \`fixSkipMakeChange = true\` (the fix is already made; this re-runs it across the dataset without editing again), then route to \`open-experiments-before-replay\`, which honors \`fixDatasetStudio\`.

{{branches}}`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "In Studio",
                                description: "set `fixDatasetStudio = true`: open Studio to the experiment page and stream verdicts live as the dataset replays",
                            },
                            recommended: true,
                            next: "iterate/open-experiments-before-replay",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Without Studio",
                                description: "set `fixDatasetStudio = false`: run the dataset replay terminal-only and report the results in chat, with no Studio window",
                            },
                            next: "iterate/open-experiments-before-replay",
                        },
                    ],
                },
                {
                    id: "fix-single-trace-failed",
                    kind: "branch",
                    toolCalls: ["ask"],
                    title: "Ask what to do after the target trace failed",
                    body: `**Run only in \`fix\` mode when \`fixReplayScope = "single-trace"\` and the targeted replay did not pass.**

Report the failed target result first. Say whether the replay produced a real FAIL verdict, was skipped as ambiguous, was unreplayable / shape-incompatible, or could not persist labels because replay trace IDs were unavailable, and that the original bug is not proven fixed this pass. **Get the dataset-state wording right by checking \`fixAddedToDataset\`:** if it is NOT set, nothing is in a dataset yet (the trace is added only once the fix proves the target green, unless the user explicitly saves a still-failing trace). If it IS set (an earlier pass already turned the trace green and added it to the dataset, then a later iteration regressed it), do NOT say "nothing saved", the trace is already saved in the dataset; say the saved trace just regressed and is red again.

**Distinguish "the fix failed" from "couldn't even test it," because it changes the recommended next step:**
- **Real FAIL verdict** (the replay ran and the output is still wrong): recommend **"Keep iterating"**, another code change is the right move.
- **Unreplayable / shape-incompatible** (the replay couldn't run the trace, so the fix was never actually tested): another code edit won't help. Most of these were already routed to the replay-health remedies (\`check-replay-health\`, \`adapt-replay-inputs\`) before reaching here; if one still lands here, **do not recommend "Keep iterating"**. Point the user at making the trace replayable instead (adapt the recorded inputs onto the current signature, mock the failing span, or point replay at the trace's source DB), and recommend "Stop and wrap up" if they don't want to chase the environment now.

Then ask what to do next. Offer inspection, another fix attempt, saving the trace as a failing test to revisit later, or stopping. Do not offer a full-dataset run here: there is no dataset trace to run unless it was already added earlier, and running one before the target passes would defeat the test-first add-to-dataset flow:

{{branches}}`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Show in Studio",
                                description: "open Studio to the targeted single-trace before/after experiment using `fixSingleTraceExperimentGroupId` (or `testRunIds` fallback), then ask the failed-result question again",
                            },
                            next: "iterate/fix-open-single-trace-studio",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Keep iterating",
                                description: 'recommend this only when the replay produced a real FAIL (for an unreplayable / shape-incompatible result, recommend making the trace replayable or stopping instead, per the body). Set `fixReplayScope = "single-trace"`, set `fixSkipMakeChange = false`, and make another change against the target trace',
                            },
                            next: "iterate/open-experiments-before-replay",
                        },
                        {
                            option: {
                                letter: "C",
                                label: "Save as a failing test",
                                description: "add the still-failing trace to a dataset with a validated failing label anyway, so the diagnosed bug is saved to revisit later, then stop. Continue to `fix-save-unfixed`",
                            },
                            next: "iterate/fix-save-unfixed",
                        },
                        {
                            option: {
                                letter: "D",
                                label: "Stop and wrap up",
                                description: "save nothing and report that the target is still not green",
                            },
                            next: "wrap-up/summary",
                        },
                    ],
                },
                {
                    id: "fix-save-unfixed",
                    kind: "action",
                    toolCalls: [
                        "setHumanLabels",
                        "listDatasets",
                        "createDataset",
                        "addTracesToDataset",
                        "ask",
                    ],
                    title: "Save the still-failing trace as a validated red",
                    emit: "Saving the failing scenario",
                    body: `**Run only in \`fix\` mode when the user chose "Save as a failing test" from \`fix-single-trace-failed\`.** The fix didn't prove out, but the user wants to keep the diagnosed failing scenario so they can come back to it. Add it to a dataset with a validated failing label exactly as \`fix-add-to-dataset\` does, just without a passing replay behind it.

**Idempotency guard, check first.** If \`fixAddedToDataset\` is already set (an earlier pass turned the trace green, added it to the dataset, and a later iteration regressed it), the trace is already in the dataset: do **not** re-label or re-attach. Just tell the user it is already saved in dataset \`<name>\` (\`<datasetId>\`) and is currently red again, then stop.

Otherwise add it now:

1. **Label the original trace as a validated fail** using the failure annotation from Phase Fix \`resolve\`: call {{tool:setHumanLabels}} once with \`[{ traceId, label: false, annotation }]\` on the **original** trace.
2. **Pick or create a dataset (minimize prompts):** if the user named a dataset ID, use it (first confirm it's scoped to this function via {{tool:listDatasets}}; if it belongs to a different function, {{tool:addTracesToDataset}} would silently skip every trace, so {{askVerb}} and offer a correctly-scoped dataset instead). Otherwise call {{tool:listDatasets}} with the function key, then 0 → create silently, exactly 1 → use it silently, 2+ → present them {{presentMode}} and recommend the most recently used dataset that has traces. Hold the \`datasetId\`.
3. **Attach the trace** with {{tool:addTracesToDataset}} (\`datasetId\`, \`[traceId]\`; idempotent). **Confirm the attach landed**: the trace is saved when it was added now or was already present. If the tool reports it skipped the trace (for example, the dataset is scoped to a different function or the trace is not in this org), do **not** claim save and do **not** set \`fixAddedToDataset\`; re-pick a dataset scoped to this function and attach again before stopping.

Set \`fixAddedToDataset = true\` only once the attach above is confirmed. Tell the user in one line: the still-failing scenario is saved in dataset \`<name>\` (\`<datasetId>\`) to revisit, and the bug is **not** fixed yet (the saved red turns green only once a future fix passes on replay). Then stop.`,
                    next: "wrap-up/summary",
                },
                {
                    id: "fix-open-single-trace-studio",
                    kind: "action",
                    toolCalls: ["bash"],
                    commandCalls: ["openStudio"],
                    title: "Open the targeted before/after in Studio",
                    body: `**Run only when the user chose "Show in Studio" from a post-target-replay fix prompt.** Open Studio to the single-trace replay's experiment view so the user can inspect the original failing trace, the replay trace, the verdict, and the code-change before/after.

Use the experiment group from the targeted replay whenever possible. Append \`&autoOpenFirst=1\` so the page lands straight on this one trace's before/after drawer instead of a one-row list the user has to click into:

\`\`\`bash
{{command:openStudio}} "/studio/experiments?experimentGroupId=<fixSingleTraceExperimentGroupId>&autoOpenFirst=1"
\`\`\`

If \`supportsExperimentGroups\` was false but you captured \`testRunId\`, use the fallback instead (same \`&autoOpenFirst=1\`):

\`\`\`bash
{{command:openStudio}} "/studio/experiments?testRunIds=<testRunId>&autoOpenFirst=1"
\`\`\`

If neither an experiment group nor a test run ID is available, say Studio cannot show this before/after run yet and continue. (\`autoOpenFirst=1\` opens the first trace's comparison once it loads, then respects the user dismissing it; it is meant for this single-trace view, so don't add it to full-dataset experiment opens.)

Studio-opening commands block until the user acts in Studio in some hosts. Launch this command as a background/long-running process, read its JSONL stdout incrementally, and do not block the conversation foreground waiting for the user. After opening, tell the user the before/after is open and return to \`fix-target-replay-status\` so the next prompt still reflects whether the target passed or failed.`,
                    next: "iterate/fix-target-replay-status",
                },
                {
                    id: "share-results",
                    kind: "branch",
                    toolCalls: ["ask"],
                    title: "Share results to the user",
                    body: `**Share results to the user.**

> "After N experiments these are the results: X/Y traces now pass (Z unreplayable, excluded from pass/fail).
>
> - ✅ Trace \`abc123\`: Now passes, [how the annotation's issue was resolved]
> - ❌ Trace \`def456\`: Still failing, annotation said [X], output still [Y]
> - ❌⚠️ Trace \`ghi789\`: Was passing, now failing (regression)
> - ⚠️ Trace \`jkl012\`: Unreplayable, [DB record not found / FK violation / write rejected]"

Keep \`unreplayable\` out of the pass-rate denominator. If \`unreplayable > 0\`, name the cause (missing record, write blocked, env mismatch) and note that fixing the env or trimming those trace IDs will clean up the next iteration. If \`check-replay-health\` fired in the previous iteration too, flag that infra has now blocked two runs and recommend fixing it before another experiment.

**When \`costRun\` is set, lead with the token delta.** The user's goal is cost, so report it next to the pass rate, not as a footnote:

> "Token cost across the dataset: input <baseIn> → <newIn> (±X%), output <baseOut> → <newOut> (±Y%). Pass rate held at A/B (no quality regressions)."

Append the token direction to each per-trace line too (e.g. \`✅ Trace abc123: now passes, 1,240 → 880 tokens (−29%)\`). A correct-but-more-expensive result is a cost regression: call it out even when pass/fail is green. Frame the recommended next step in cost terms (where the remaining token spend is concentrated), not just pass rate.

These figures use the run's basis (\`costBasis\`), already chosen upstream in \`evaluate-results\`. When the basis is \`uncached\`, the input numbers are the uncached input (\`input - cached\`) and the headline names it, e.g. "Uncached token cost across the dataset: input <baseIn> → <newIn> (±X%)...", so the chat matches the page's uncached trend; on \`all\` they are raw \`input + output\`. Pick one basis and hold it across the headline and every per-trace line.

Show this across the full data set, and highlight the best outcome concisely. Explain why it worked best with references to code, docs, and/or research if needed. For the best outcome:

- **If pass rate improved and no regressions**: {{askVerb}} to confirm whether they want to keep iterating or stop
- **If pass rate improved but regressions exist or no improvement**: tell the user and propose to create a plan for new experiments and continue iterating.

**If running in text-only mode** (trace IDs were unavailable): append a note that cross-iteration comparison isn't available without trace IDs. Each iteration's results are visible only in-agent for the current run. Upgrading to \`@bitfab/sdk\` 0.13.5+ and updating the server unlocks persistent experiment tracking across iterations, side-by-side comparison in Studio, and the full experiments page.

**In \`fix\` mode with \`fixReplayScope = "dataset"\`, this is the optional full-dataset experiment after the target trace already replayed.** Frame the result around whether the added trace stayed green and what the full dataset revealed: regressions, still-failing sibling traces, and unreplayable items. When the user chose a terminal-only re-run (\`fixDatasetStudio\` false), no Studio is open, so this in-chat report **is** the result surface: give the full per-trace breakdown here rather than pointing them at a Studio page. If the added trace now passes and other dataset traces are still red, do NOT auto-loop through them: frame "Keep iterating" as an explicit offer, name how many traces are still failing and ask whether to keep going or stop here. If the added trace regressed during the dataset run, "Keep iterating" means another fix attempt on it. Before continuing from a full-dataset fix result, always reset the fix loop state: set \`fixReplayScope = "single-trace"\` and \`fixSkipMakeChange = false\` so the next pass makes a fresh targeted change instead of replaying the dataset again with the previous code. The dataset keeps this scenario saved regardless of what they pick.

Ensure your question includes your recommended next step.

{{branches}}`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Keep iterating",
                                description: 'run another experiment from the plan; in `fix` mode after a dataset run, first set `fixReplayScope = "single-trace"` and `fixSkipMakeChange = false`',
                            },
                            recommended: true,
                            next: "iterate/open-experiments-before-replay",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Stop and wrap up",
                                description: "move to the final summary",
                            },
                            next: "wrap-up/summary",
                        },
                    ],
                },
            ],
        },
        {
            id: "quick-replay",
            title: "Phase Replay: Single-Trace Quick Replay",
            intro: `Reached only from \`replay\` mode. The user already has a trace ID and (usually) already made a fix; they just want to replay that one trace and hear whether it worked. This is the **minimal, atomic** path: no Studio/browser, no dataset, no labeling, no experiment groups, no server-side verdict persistence. Locate the replay script, read the trace, run replay against the single trace ID, compare the new output to the original, and report a one-line verdict in chat. The only server interaction is the replay itself (the SDK creates a test run intrinsically); nothing else is persisted.`,
            stepStyle: "list",
            steps: [
                {
                    id: "setup",
                    kind: "branch",
                    toolCalls: ["bash", "grep", "glob", "readTraces"],
                    title: "Locate the replay script and read the trace",
                    emit: "Setting up replay",
                    body: `**Both sub-steps run without user interaction. No questions, just execute.**

**1. Read the trace (and resolve the function key).** Call {{tool:readTraces}} with the trace ID argument and \`scope: "full"\`. Hold the trace's label, annotation, inputs, and output in context, these are the acceptance criteria for the verdict. **If the user gave only a trace ID and no function key** (common with free-form requests like "did my fix work on \`<id>\`"), take the trace function key from the trace itself, don't ask the user for it.

**2. Find the replay script.** Search for files matching \`scripts/replay.*\`, \`scripts/*replay*\`, or any file importing \`bitfab.replay\` / \`client.replay\`, and confirm it covers that trace function key. (You don't need to grep for capability flags here, this minimal path doesn't use code-change payloads, experiment groups, or verdict persistence.)

{{whens}}`,
                    branches: [
                        {
                            when: "replay script found and trace readable",
                            description: "continue to run the replay",
                            next: "quick-replay/run",
                        },
                        {
                            when: "no replay script found for this function",
                            description: `tell the user: "No replay script found for \`<key>\`. Run \`{{cmd}}setup replay <key>\` to create one, then re-run this command." Stop the flow`,
                            next: "cleanup/close-studio",
                        },
                        {
                            when: "trace not found or unreadable",
                            description: "tell the user the trace ID wasn't found or is inaccessible, stop",
                            next: "cleanup/close-studio",
                        },
                    ],
                },
                {
                    id: "run",
                    kind: "action",
                    toolCalls: ["bash"],
                    title: "Run replay against the single trace",
                    emit: "Running replay",
                    body: `**Run the replay against the one trace ID. No user interaction, no extra flags.**

\`\`\`bash
cd <project-dir> && npx tsx <replay-script> <function-key> --trace-ids <trace-id>
\`\`\`

This is a single-trace, in-chat path: run the replay directly, no progress-bar wrapper (one item has nothing to track). Do **not** pass \`--code-change\` or \`--experiment-group-id\`, this minimal path skips code-change payloads and experiment groups. Capture the JSON output and exit code.

**Quick health check.** If the replay crashed (non-zero exit, no items) or the single item has \`item.error\` set, hold the error for the verdict step. Otherwise hold the completed item's new output alongside the original output you read in \`setup\`.`,
                    next: "quick-replay/verdict",
                },
                {
                    id: "verdict",
                    kind: "branch",
                    toolCalls: ["ask"],
                    title: "Compare and report the verdict",
                    emit: "Evaluating result",
                    body: `**Compare the single replay result to the original and report in one line.** Nothing is persisted; this is an in-chat verdict.

**If the replay errored** (crashed or \`item.error\` set): report the error clearly. This is an infra issue (missing DB row, env mismatch, etc.), not a code failure. Offer to retry after fixing the env, or to stop.

**If the replay completed**, compare the new output against the original trace's label and annotation, then report one line:

- Original was **fail** with an annotation: does the new output address it? → "**Pass**: the fix addresses the original failure ('<annotation summary>')." vs "**Still failing**: <what's still wrong>."
- Original was **pass**: preserved → "**Pass**: output unchanged in quality." regressed → "**Regressed**: was passing, now <what broke>."
- No label on the original: show a short before/after diff and summarize whether it looks better.

{{branches}}`,
                    branches: [
                        {
                            option: {
                                letter: "A",
                                label: "Iterate",
                                description: "make another change and re-replay the same trace",
                            },
                            next: "quick-replay/iterate",
                        },
                        {
                            option: {
                                letter: "B",
                                label: "Done",
                            },
                            recommended: true,
                            next: "cleanup/close-studio",
                        },
                    ],
                },
                {
                    id: "iterate",
                    kind: "action",
                    toolCalls: ["ask", "edit", "read"],
                    title: "Make another change",
                    body: `**Make another change before re-replaying.** {{^codex}}Use \`AskUserQuestion\` to ask{{/codex}}{{#codex}}Ask{{/codex}} what to change, or let the user describe the fix. Edit the code, then loop back to run the replay again. If the user says they'll make the change themselves, wait for their message, then proceed.`,
                    next: "quick-replay/run",
                },
            ],
        },
        {
            id: "wrap-up",
            title: "Phase 6: Validate & Wrap Up",
            stepStyle: "list",
            steps: [
                {
                    id: "summary",
                    kind: "action",
                    toolCalls: ["ask"],
                    title: "Summary",
                    emit: { displayName: "Done", action: "completed" },
                    body: `**Summary.** {{^codex}}Use \`AskUserQuestion\` to present{{/codex}}{{#codex}}Present{{/codex}} the final results similar to this. You may expand where appropriate based on context from the user:

> "**Improvement summary for** \`<traceFunctionKey>\`:
>
> - Failed traces fixed: X/Y (from N% → M% pass rate on labeled failures)
> - Full replay pass rate: A/B (Z unreplayable, excluded)
> - Changes made:
>   - [File]: [Description of change]
>   - [File]: [Description of change]
>
> The changes are in your working tree (not committed). Review the diffs and commit when ready."

If \`Z > 0\`, add one line naming the infra cause (e.g. "Z traces unreplayable, missing DB rows; refresh the dataset or scope to a snapshot next pass") so the user has a next step beyond the code.`,
                    next: "cleanup/close-studio",
                },
            ],
        },
        {
            id: "benchmark",
            title: "Phase Benchmark: Scorecard",
            stepStyle: "list",
            steps: [
                {
                    id: "scorecard",
                    kind: "action",
                    title: "Benchmark scorecard",
                    emit: { displayName: "Done", action: "completed" },
                    body: `**Benchmark scorecard.** Present the results of replaying the dataset against the current code (no changes were made). {{^codex}}Print the scorecard as Markdown directly in chat (do NOT use \`AskUserQuestion\`, this is a terminal report with no decision to make, and tables don't render inside the question UI).{{/codex}}{{#codex}}Print the scorecard as Markdown directly in chat.{{/codex}} Use two tables.

**Table 1, Summary** (one row per metric):

\`\`\`markdown
**Benchmark results for** \`<traceFunctionKey>\` · dataset \`<datasetName>\`

| Metric | Count |
|---|---|
| Pass rate | X/scorable (Z%) |
| Still passing | K |
| Still failing | M |
| Regressions | N |
| Fixed | F |
| Unreplayable | U (excluded) |
| Skipped | S (excluded) |
\`\`\`

**Table 2, Per-trace breakdown** (one row per dataset trace). Sort rows by verdict in this order: regressions first, then still-failing, then fixed, then still-passing, then unreplayable, then skipped last:

\`\`\`markdown
| Trace | Label | Verdict | Detail |
|---|---|---|---|
| \`ghi789\` | pass | ❌ regression | was passing, replay now fails: [why] |
| \`jkl012\` | fail | ❌ still failing | annotation said [X], output still [Y] |
| \`def456\` | fail | ✅ fixed | replay now addresses: [annotation] |
| \`abc123\` | pass | ✅ still passing | output preserved |
| \`mno345\` | n/a | ⚠️ unreplayable | [error reason] |
| \`pqr678\` | fail | ⏭️ skipped | output genuinely ambiguous; not verdicted |
\`\`\`

Use ✅ for pass-verdict rows (fixed, still-passing), ❌ for fail-verdict rows (regression, still-failing), ⚠️ for unreplayable, and ⏭️ for skipped (an item you explicitly marked \`skip: true\` in \`evaluate-results\` because the output was genuinely ambiguous). Keep \`Detail\` to one short line per row (truncate long annotations/outputs). Keep **both \`unreplayable\` and \`skipped\` out of the pass-rate denominator.** Define the counts explicitly: \`T\` = total traces in the dataset; \`U\` = unreplayable; \`S\` = skipped; \`scorable\` = \`T − U − S\` (the items that got a real pass/fail verdict); \`X\` = the count that passed (✅ fixed + ✅ still-passing). \`Pass rate\` = \`X / scorable\` (so the summary table's "X/scorable" uses these exact numbers). **If \`scorable\` is 0** (every trace was unreplayable or skipped, so nothing got a real verdict), report \`Pass rate\` as \`N/A (0 scorable)\` instead of \`0/0\`, and add a line that no trace could be scored this run. If \`U > 0\`, add one line under the tables naming the cause (missing DB rows, FK violation, env mismatch). Omit the \`Skipped\` summary row and any skipped table rows entirely when \`S\` is 0.

**If running in text-only mode** (trace IDs were unavailable): append a one-line note under the tables that persistent results require upgrading to \`@bitfab/sdk\` 0.13.5+.

**When \`costRun\` is set, add token cost to both tables.** In Table 1, add \`Input tokens | base → new (±X%)\` and \`Output tokens | base → new (±Y%)\` rows (total tokens only if the replay output does not split them). In Table 2, add a \`Tokens\` column showing each trace's \`original → replay\` total and % change. For a cost benchmark the headline is the token delta, not the pass rate alone: report both. Baseline tokens come from each original trace's recorded usage. For originals that errored, failed, or recorded no usage, apply the same cheapest-first recovery as the evaluate step (reuse a clean recorded run, else a one-off per-item backfill replay of the unchanged code), never a dataset-wide baseline arm; new tokens come from the replay output. Where no baseline could be recovered, show the cell as "no baseline" rather than a fabricated delta. Use the run's basis (\`costBasis\`), matching the page lens: on \`uncached\`, the Input row and the Tokens column are the uncached figures (\`(input - cached) + output\`, \`input - cached\` for the input row) and the table labels the row/column "uncached"; on \`all\`, raw \`input + output\`. Don't mix bases within a scorecard.

This is a terminal step. Report the scorecard and stop. Do not offer to iterate or make changes (the user can run \`{{cmd}}assistant experiment <key>\` separately if they want to fix failures).`,
                    next: "cleanup/close-studio",
                },
            ],
        },
        {
            id: "cleanup",
            title: "Cleanup",
            stepStyle: "list",
            steps: [
                {
                    id: "close-studio",
                    kind: "action",
                    toolCalls: ["bash"],
                    commandCalls: ["closeStudio"],
                    title: "Close Studio",
                    body: `Close Studio. Run this unconditionally: it resolves the active session from disk, closes the Studio tab (the daemon ends the session and stops appending to the event file), and exits quietly (\`{"event":"no-active-studio"}\`) when nothing was opened:

\`\`\`bash
{{command:closeStudio}}
\`\`\`

No sessionId argument is needed; do not track or look up one. This is silent housekeeping: never narrate it, reason about whether a session was opened, or report the outcome to the user (no "closing Studio", no "nothing to close").`,
                    next: null,
                },
            ],
        },
    ],
});
