import { describe, expect, it } from "vitest";
import { renderMarkdown } from "./render.js";
import { Flow } from "./schema.js";
import { TARGETS } from "./targets.js";
/**
 * Build a minimal valid Flow with the given overrides applied to the base.
 * Top-level fields override the base; phases/steps replace whole.
 */
function makeFlow(overrides = {}) {
    return Flow.parse({
        schemaVersion: 1,
        id: "test",
        title: "Test",
        frontmatter: { description: "Desc" },
        entries: { all: "main/a" },
        phases: [
            {
                id: "main",
                title: "Main",
                stepStyle: "headers",
                steps: [
                    { id: "a", kind: "action", title: "A", body: "Body A", next: null },
                ],
            },
        ],
        ...overrides,
    });
}
describe("Renderer: reachability-based visibility", () => {
    it("only renders steps reachable for the current target", () => {
        const flow = makeFlow({
            entries: {
                all: { default: "main/start", codex: "main/preamble" },
            },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "preamble",
                            kind: "action",
                            title: "Codex Preamble",
                            body: "preamble body",
                            preamble: true,
                            next: "main/start",
                        },
                        {
                            id: "start",
                            kind: "action",
                            title: "Start",
                            body: "start body",
                            next: null,
                        },
                    ],
                },
            ],
        });
        const claudeOut = renderMarkdown(flow, TARGETS.claude);
        const codexOut = renderMarkdown(flow, TARGETS.codex);
        expect(claudeOut).not.toContain("Codex Preamble");
        expect(claudeOut).toContain("## 1. Start");
        expect(codexOut).toContain("## 0. Codex Preamble");
        expect(codexOut).toContain("## 1. Start");
    });
    it("numbers preamble steps as 0, others 1-indexed", () => {
        const flow = makeFlow({
            entries: { all: "main/p" },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "p",
                            kind: "action",
                            title: "P",
                            body: "p",
                            preamble: true,
                            next: "main/a",
                        },
                        { id: "a", kind: "action", title: "A", body: "a", next: "main/b" },
                        { id: "b", kind: "action", title: "B", body: "b", next: null },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("## 0. P");
        expect(out).toContain("## 1. A");
        expect(out).toContain("## 2. B");
    });
});
describe("Renderer: mode-skip prose", () => {
    it("inserts 'Skip this step if mode is X' for steps unreachable in X", () => {
        const flow = makeFlow({
            entries: { all: "main/a", lite: "main/a" },
            defaultMode: "all",
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "always",
                            next: { byMode: { all: "main/b", lite: "main/c" } },
                        },
                        {
                            id: "b",
                            kind: "action",
                            title: "B",
                            body: "all-only",
                            next: "main/c",
                        },
                        {
                            id: "c",
                            kind: "action",
                            title: "C",
                            body: "always C",
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        // Step b is reachable in `all` only; mode-skip prose should mark it as skipped in `lite`.
        expect(out).toContain("**Skip this step if mode is `lite`.**");
    });
    it("emits a fresh skip on later steps when the previous gate was only 'this step'", () => {
        // Reproduces the second Cursor Bugbot case: step b's gate is just
        // "skip this step if mode is lite" (because step c is reachable in
        // lite). Step c is also unreachable in lite, but the old logic
        // suppressed the gate at c because lite was already in prevExcluded.
        // The agent then had no instruction to skip step c in lite mode.
        const flow = makeFlow({
            entries: { all: "main/a", lite: "main/a" },
            defaultMode: "all",
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        // a: routes lite past b but through c is unreachable
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "A",
                            next: { byMode: { all: "main/b", lite: "main/d" } },
                        },
                        // b: reachable only in all (lite skips it via a.next.byMode)
                        {
                            id: "b",
                            kind: "action",
                            title: "B",
                            body: "B",
                            next: "main/c",
                        },
                        // c: also reachable only in all — but d (after c) is reachable
                        // in lite, so b's gate cannot say "everything below"
                        {
                            id: "c",
                            kind: "action",
                            title: "C",
                            body: "C",
                            next: "main/d",
                        },
                        // d: reachable in both modes
                        { id: "d", kind: "action", title: "D", body: "D", next: null },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        // Both step b AND step c need their own "skip if mode is lite" gate.
        // Previously step c's gate was suppressed (covered → silent).
        const skipMatches = out.match(/Skip this step if mode is `lite`/g) ?? [];
        expect(skipMatches.length).toBe(2);
    });
    it("uses 'and everything below' when subsequent steps share the exclusion", () => {
        const flow = makeFlow({
            entries: { all: "main/a", lite: "main/a" },
            defaultMode: "all",
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "always",
                            next: { byMode: { all: "main/b", lite: null } },
                        },
                        { id: "b", kind: "action", title: "B", body: "B", next: "main/c" },
                        { id: "c", kind: "action", title: "C", body: "C", next: null },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("**Skip this step and everything below if mode is `lite`.**");
        // The phrase only appears once at step b — subsequent steps inherit it.
        const occurrences = (out.match(/Skip this step and everything below/g) ?? []).length;
        expect(occurrences).toBe(1);
    });
});
describe("Renderer: tool registry and {{tool:id}}", () => {
    it("renders builtin tools as the bare name in backticks", () => {
        const flow = makeFlow({
            tools: { read: { kind: "builtin", name: "Read" } },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "Use {{tool:read}} to verify.",
                            toolCalls: ["read"],
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("Use `Read` to verify.");
    });
    it("prepends the target's mcpPrefix for MCP tools", () => {
        const flow = makeFlow({
            tools: { apiKey: { kind: "mcp", name: "get_bitfab_api_key" } },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "Call {{tool:apiKey}}.",
                            toolCalls: ["apiKey"],
                            next: null,
                        },
                    ],
                },
            ],
        });
        const claudeOut = renderMarkdown(flow, TARGETS.claude);
        const codexOut = renderMarkdown(flow, TARGETS.codex);
        expect(claudeOut).toContain("`mcp__plugin_bitfab_Bitfab__get_bitfab_api_key`");
        expect(codexOut).toContain("`mcp__Bitfab__get_bitfab_api_key`");
    });
    it("auto-derives allowedTools from union of step toolCalls (registry order)", () => {
        const flow = makeFlow({
            tools: {
                bash: { kind: "builtin", name: "Bash" },
                read: { kind: "builtin", name: "Read" },
                ghost: { kind: "builtin", name: "Ghost" },
            },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "x",
                            toolCalls: ["read", "bash"],
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        // Order matches registry, not toolCalls. Ghost is unused → omitted.
        expect(out).toContain('allowed-tools: ["Bash", "Read"]');
        expect(out).not.toContain("Ghost");
    });
});
describe("Renderer: branches and {{branches}}", () => {
    it("renders user-choice options as a blockquote-prefixed list", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "branch",
                            title: "Pick",
                            body: "Choose:\n\n{{branches}}",
                            branches: [
                                {
                                    option: { letter: "A", label: "Apple" },
                                    recommended: true,
                                    next: null,
                                },
                                {
                                    option: {
                                        letter: "B",
                                        label: "Banana",
                                        description: "yellow fruit",
                                    },
                                    next: null,
                                },
                            ],
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("> A) **Apple** *(recommended)*");
        expect(out).toContain("> B) **Banana** — yellow fruit");
    });
    it("filters {{branches:tag}} to only options with matching prompt tag", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "branch",
                            title: "Pick",
                            body: "First:\n{{branches:first}}\n\nSecond:\n{{branches:second}}",
                            branches: [
                                {
                                    prompt: "first",
                                    option: { letter: "A", label: "First A" },
                                    next: null,
                                },
                                {
                                    prompt: "second",
                                    option: { letter: "A", label: "Second A" },
                                    next: null,
                                },
                            ],
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("First:\n> A) **First A**");
        expect(out).toContain("Second:\n> A) **Second A**");
    });
    it("excludes runtime branches (no `option`) from the {{branches}} block", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "branch",
                            title: "Pick",
                            body: "{{branches}}",
                            branches: [
                                { when: "exit condition", next: null },
                                {
                                    option: { letter: "A", label: "User choice" },
                                    next: null,
                                },
                            ],
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("> A) **User choice**");
        expect(out).not.toContain("exit condition");
    });
});
describe("Renderer: languages block", () => {
    it("renders {{languages:imports}} as a bullet list using displayName", () => {
        const flow = makeFlow({
            languages: [
                { name: "ts", displayName: "TypeScript", imports: "`bitfab`" },
                { name: "py", displayName: "Python", imports: "`bitfab-py`" },
            ],
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "Imports:\n{{languages:imports}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("  - TypeScript: `bitfab`");
        expect(out).toContain("  - Python: `bitfab-py`");
    });
    it("renders {{languages:f1,f2}} as a multi-column table with derived headers", () => {
        const flow = makeFlow({
            languages: [
                {
                    name: "ts",
                    displayName: "TypeScript",
                    detection: "pkg.json",
                    outdated: "npm outdated",
                },
            ],
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "{{languages:detection,outdated}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("| Language | Detection | Outdated |");
        expect(out).toContain("| ts | pkg.json | npm outdated |");
    });
    it("uses languageColumnHeaders override when set", () => {
        const flow = makeFlow({
            languages: [
                { name: "ts", displayName: "TypeScript", detection: "pkg.json" },
            ],
            languageColumnHeaders: { detection: "Custom Header" },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "{{languages:detection}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("| Language | Custom Header |");
    });
});
describe("Renderer: step references", () => {
    it("resolves {{step:phase/id}} to the step's number", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "see step {{step:main/c}}",
                            next: "main/b",
                        },
                        { id: "b", kind: "action", title: "B", body: "b", next: "main/c" },
                        { id: "c", kind: "action", title: "C", body: "c", next: null },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("see step 3");
    });
    it("renders preamble step's number as 0 in references", () => {
        const flow = makeFlow({
            entries: { all: "main/p" },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "p",
                            kind: "action",
                            title: "P",
                            body: "p",
                            preamble: true,
                            next: "main/a",
                        },
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "see step {{step:main/p}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("see step 0");
    });
    it("throws on a {{step:id}} that doesn't exist", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "see step {{step:main/ghost}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        expect(() => renderMarkdown(flow, TARGETS.claude)).toThrow(/not found in flow/);
    });
});
describe("Renderer: Targetable string fields", () => {
    it("uses target-specific value when provided", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: { default: "default body", codex: "codex body" },
                            next: null,
                        },
                    ],
                },
            ],
        });
        expect(renderMarkdown(flow, TARGETS.claude)).toContain("default body");
        expect(renderMarkdown(flow, TARGETS.codex)).toContain("codex body");
    });
});
describe("Renderer: variables", () => {
    it("substitutes {{name}} from flow.variables", () => {
        const flow = makeFlow({
            variables: { greeting: "hello" },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "Say {{greeting}}!",
                            next: null,
                        },
                    ],
                },
            ],
        });
        const out = renderMarkdown(flow, TARGETS.claude);
        expect(out).toContain("Say hello!");
    });
    it("resolves a per-target variable to the target's value", () => {
        const flow = makeFlow({
            variables: {
                siteName: { default: "Bitfab", codex: "Bitfab Codex" },
            },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "Welcome to {{siteName}}.",
                            next: null,
                        },
                    ],
                },
            ],
        });
        expect(renderMarkdown(flow, TARGETS.claude)).toContain("Welcome to Bitfab.");
        expect(renderMarkdown(flow, TARGETS.codex)).toContain("Welcome to Bitfab Codex.");
    });
});
describe("Renderer: substitution ordering", () => {
    it("strips non-matching Mustache sections before resolving inner step refs", () => {
        // Reproduces the Cursor Bugbot case: a step reference inside a
        // {{#codex}} section must not be resolved against Claude's step
        // numbering when rendering for Claude. Stripping sections first
        // means the placeholder is removed before any resolution attempt.
        const flow = makeFlow({
            entries: {
                all: { default: "main/start", codex: "main/codex-only" },
            },
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "codex-only",
                            kind: "action",
                            title: "Codex Preamble",
                            body: "p",
                            preamble: true,
                            next: "main/start",
                        },
                        {
                            id: "start",
                            kind: "action",
                            title: "Start",
                            body: "before {{#codex}}see step {{step:main/codex-only}}{{/codex}} after",
                            next: null,
                        },
                    ],
                },
            ],
        });
        // Claude doesn't have main/codex-only in its reachable set; the
        // {{step:...}} reference inside the {{#codex}} block should be
        // stripped before evaluation.
        expect(() => renderMarkdown(flow, TARGETS.claude)).not.toThrow();
        const claudeOut = renderMarkdown(flow, TARGETS.claude);
        expect(claudeOut).toContain("before  after");
        // Codex sees its own preamble and the step reference resolves to 0.
        const codexOut = renderMarkdown(flow, TARGETS.codex);
        expect(codexOut).toContain("before see step 0 after");
    });
});
describe("Renderer: Mustache sections", () => {
    it("includes #target content only for the matching target", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "before {{#codex}}codex-only{{/codex}} after",
                            next: null,
                        },
                    ],
                },
            ],
        });
        expect(renderMarkdown(flow, TARGETS.claude)).toContain("before  after");
        expect(renderMarkdown(flow, TARGETS.codex)).toContain("before codex-only after");
    });
    it("inverts for ^target sections", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "{{^codex}}non-codex{{/codex}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        expect(renderMarkdown(flow, TARGETS.claude)).toContain("non-codex");
        expect(renderMarkdown(flow, TARGETS.codex)).not.toContain("non-codex");
    });
    it("throws on an unknown Mustache section name", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "{{#nottarget}}x{{/nottarget}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        expect(() => renderMarkdown(flow, TARGETS.claude)).toThrow(/Unknown target name/);
    });
});
describe("Renderer: unknown-placeholder safety net", () => {
    it("throws on an unrecognized {{name}} placeholder", () => {
        const flow = makeFlow({
            phases: [
                {
                    id: "main",
                    title: "Main",
                    stepStyle: "headers",
                    steps: [
                        {
                            id: "a",
                            kind: "action",
                            title: "A",
                            body: "see {{ghost}}",
                            next: null,
                        },
                    ],
                },
            ],
        });
        expect(() => renderMarkdown(flow, TARGETS.claude)).toThrow(/Unresolved placeholder/);
    });
});
