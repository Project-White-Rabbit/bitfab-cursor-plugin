/**
 * Shared Studio-behavior prose surfaced in more than one flow preamble.
 *
 * Studio-opening commands (`openStudioTo.js`, `startDataset.js`,
 * `openTracePlan.js`, `startTemplatePreview.js`, ...) resolve the active
 * session and navigate the live tab in place, opening a fresh window only when
 * none is active. That reuse rule applies identically to the setup and
 * assistant flows, so it lives here once instead of being copy-pasted into each
 * preamble. Interpolate these constants into a flow's `intro` template literal
 * with `${...}`; every token they use (`{{command:closeStudio}}`, literal
 * command filenames) is valid in both flows. Do NOT reference commands only one
 * flow registers (e.g. `{{command:openStudio}}` exists in assistant but not
 * setup) here.
 */
/**
 * "Never close-then-reopen Studio just to switch pages." Applies to every
 * Studio-opening command in any flow that opens Studio.
 */
export const STUDIO_REUSE_NOTE = `**Studio reuse: never close-then-reopen to switch pages (applies to every Studio-opening command).** Every Studio-opening command (\`openTracePlan.js\`, \`startDataset.js\`, \`startTemplatePreview.js\`, \`login.js\`, etc.) resolves the active session and **navigates the live tab in place** (emitting \`{"event":"navigated",...}\`); it opens a fresh window only when no session is active. So to change what Studio is showing, open a different dataset, a different trace plan, or a different page, just run the relevant open command again with the new target. **Never call \`{{command:closeStudio}}\` just to switch:** closing and reopening churns the tab, can orphan windows, and is never the way to change pages. Reserve \`{{command:closeStudio}}\` for genuine end-of-flow cleanup (the cleanup step already handles that) or when the user explicitly asks for the Studio tab to be closed.`;
