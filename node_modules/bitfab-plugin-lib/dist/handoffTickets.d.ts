interface CreateHandoffTicketResponse {
    id: string;
    expiresAt: string;
    pollIntervalMs: number;
}
export declare function createHandoffTicket(serviceUrl: string, kind: string, opts?: {
    bearer?: string;
    pkceChallenge?: string;
    pkceMethod?: string;
    ttlSeconds?: number;
}): Promise<CreateHandoffTicketResponse>;
interface PollResult<T> {
    data: T;
}
interface PollExpired {
    expired: true;
}
export declare function pollHandoffTicket<T>(serviceUrl: string, ticketId: string, opts: {
    verifier?: string;
    bearer?: string;
    timeoutMs: number;
    intervalMs?: number;
    abortSignal?: AbortSignal;
}): Promise<PollResult<T> | PollExpired>;
export {};
