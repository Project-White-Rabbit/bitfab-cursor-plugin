export interface CommandCatalogEntry {
    file: string;
}
export interface CommandDefinition {
    file: string;
    description: string;
    args?: string;
}
export interface CommandUse<CommandId extends string = string> {
    command?: CommandId;
    description: string;
    args?: string;
}
export declare function defineCommandCatalog<const Catalog extends Record<string, CommandCatalogEntry>>(catalog: Catalog): {
    defineCommands<const Defs extends Record<string, CommandUse<Extract<keyof Catalog, string>>>>(defs: Defs): { [K in keyof Defs]: CommandDefinition; };
};
//# sourceMappingURL=commands.d.ts.map